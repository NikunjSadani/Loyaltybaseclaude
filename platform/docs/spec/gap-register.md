# Gap & Inconsistency Register (living)

Accumulated while reverse-engineering the spec. Each entry: current state vs target,
and where it surfaced. Severity is a first pass, to be refined.

| # | Title | Current state | Target / resolution | Severity |
|---|-------|---------------|---------------------|----------|
| 1 | Domain mismatch | ~~Code references `loyaltybase.in`~~ | **✅ CLOSED (0.4a):** all `loyaltybase.in` → `gifsy.in` (8 files; 0 residual). | Low |
| 2 | RBAC is hardcoded | Fixed 11-value `UserRole` enum; coarse `if role !==` checks scattered in routes | **◐ ENGINE DONE (1.6a):** `lib/rbac/can.ts` — default role→permission map + per-tenant overrides. **Operating model (user-confirmed):** GIFSY_ADMIN=all (⊇ every role); CLIENT_ADMIN=all except `GIFSY_OPERATED_PERMISSIONS` (tenancy config, visibility invoicing, money settlement/UTR, activation create/delete); MIS_USER=read-only; Sales/Partner=portal+data-scoped. **Residual:** route enforcement wiring = 1.6b (flag-gated); Gifsy internal sub-roles deferred. | **High** (engine done) |
| 3 | No permission/capability catalog | No master list of taggable features/permissions | **✅ DONE (1.5):** `lib/rbac/permissions.ts` — 72 permissions / 17 groups derived from the §01 capability contexts; exhaustive `Permission` union + `PERMISSION_GROUPS` metadata. | ~~High~~ → resolved |
| 4 | Partner/Outlet level split | Modeled 1:many but operated 1:1; login+wallet on Partner, KYC+visibility on Outlet | **✅ ADDRESSED (P2.4):** kept **1:many + `isPrimary`**, operate 1:1 by convention (future 1:many free). Outlet now carries its **own `clientId`** + **nullable `partnerId`** (owner = `ChannelPartner`, attached at KYC). | ~~Medium~~ → resolved |
| 5 | "Payout" is overloaded (not duplicated) | Traced: `Credit*` = admin-**push** awards (target/focus/visibility → points or INR, offline UTR, reversals); `Payout*`+Fund+TDS = partner-**pull** redemptions (→ INR via provider from prepaid float). Two legitimate flows, but both use the word "payout" (`CreditPayoutEntry` vs `PayoutTransaction`) | Rename for clarity; keep as distinct contexts (Awards & Credits vs Redemption Payouts). Not a consolidation | Medium |
| 6 ✅ | Dynamic enrollment forms — **RESOLVED (P4.2/4.3)** | Was: feature flags + bare `SchemeEnrollment` model, no persistence | **✅ DONE (P4.2/4.3, 2026-06-17):** `SchemeEnrollmentForm` (1:1 scheme, `formSchema` JSON + `campaignType`) persisted via `PUT/GET /v1/schemes/:id/enrollment-form` with a pure structural validator (field types, `CALCULATED` formula refs, single-condition `visibleWhen` dangling/self refs, dup ids). Submission: `POST /v1/schemes/:id/enroll` — **self vs sales-on-behalf** (assignment-gated), **audience by `campaignType`** (LOYALTY_ONLY=KYC-approved / OPEN_CAMPAIGN=non-KYC / MIXED), **server-side `CALCULATED` recompute** (client value discarded), conditional pre-fill, upsert re-enroll. FE: `EnrollmentFormBuilder` + `enrollment-form-renderer` wired. | **Resolved** |
| 7 | Visibility "separate UTR / never clubbed" rule | `CreditField.isSeparatePayout` models it; visibility flagged separate, incentive parameters club into one UTR | **✅ VERIFIED (P6.3, 2026-06-18):** `createPayoutDownload` already enforces it — STANDARD downloads exclude `isSeparatePayout` fields (`NOT: { fieldId: { in: separateFieldIds } }`), SEPARATE downloads isolate a single field. Locked with jest tests. | ~~Medium~~ → **Verified (P6.3)** |
| 8 | Visibility = self-billed invoicing (resolved) | Outlet renders visibility *service*; **Gifsy auto-generates the outlet→Gifsy invoice on the outlet's behalf** (self-billing), GST logic from outlet registration. Outlet views in portal + **edits invoice number** (`AutoInvoice.invoiceNumber`). **Gifsy→Client billing is off-platform (external accounting) — out of scope by design.** | In scope: validate partner-edited invoice number (uniqueness/format/lock-after-finalize). Not a modeling gap | Low |
| 9 ✅ | KYC approval routing — **RESOLVED (P3.2, 2026-06-16)** | Was: `ROLE_PHONES` flat per-role table + hardcoded chain. Now: `KycService.resolveInitialRouting` walks the relational `SalesUser.reportingToId` tree to the submitter's first **ACTIVE** manager (`isActive`/`deletedAt`), maps the manager's hierarchy level → `PENDING_*`, and records escalation when a manager is skipped. Per-hop tenant-scoped (id+clientId), cycle + 10-hop bounded. `ROLE_PHONES` + the phone-blank hack retired (grep-clean) | **Resolved** |
| 10 | Award computation over-built vs upload reality | `SchemeRule` rule engine + `api/schemes/calculate` + auto `SchemeTarget`; "target/achievement" also in `Target`/`TargetAchievement` and `CreditBatch` rows | **DECISION MADE = PRUNE (P4.0 de-scaffold).** Canonical = **tenant computes externally → uploads FINAL amounts per parameter** (Credits / `OutletSalesRecord.kpiValues`), platform does NOT compute. Retire `lib/incentive.ts` + `api/schemes/calculate` + compute fields. Schemes = define+enroll only; Targets = tracking. See `docs/plans/MODEL-ALIGNMENT.md` + Gap #29. **✅ DONE (P4.0 reconcile → P4.4/4.5 build, 2026-06-17):** NO compute anywhere. Participation = the **per-outlet × KPI × month target Excel upload** (blank cell = not configured), stored verbatim in new **`OutletTarget`** (mirrors `OutletSalesRecord`); achievement uploads the same shape; pace = achieved÷target (÷0→null). World-A `SchemeTarget` + `lib/incentive` compute path **dropped**. `SchemeType`=canonical category, FE `IncentiveType` retired, `CampaignType`(LOYALTY/OPEN/MIXED)=enrollment audience. See `../plans/reconcile/P4-programs-targets-enrollment.md`. | ~~Medium~~ → **Resolved (P4)** |
| 11 | Fixed sales-role enum vs configurable hierarchy | `UserRole` hardcodes 5 sales levels (HO/State-Head/ASM/SO/ISR); industry norm is 6 (XSR/SO/ASM/RSM/**ZNM**/NSM) — no Zonal rung; names fixed | **✅ ADDRESSED (P2.1):** hierarchy upload persists the relational `SalesHierarchyLevel`/`SalesUser` tree (incl. **ZNM**); fine role on `SalesHierarchyLevel.code`, coarse `UserRole` bucket via `mapRoleCodeToUserRole` (no enum change). `SalesUser.clientId` + `@@unique([clientId, employeeCode])`. | ~~Medium~~ → resolved |
| 12 ✅ | GST+bank validation — **RESOLVED (P3.4)** | Built as the chosen **Excel-batch bulk-verify** (`POST /v1/kyc/bulk-verify` preview→commit) + portal per-field verify; GST reg type captured (`gstDetails`). `PENDING_AGREEMENT`/`PENDING_PENNY_DROP` folded into the 7-field grid (status stays `PENDING_GIFSY`). API automation = future option-(b) seam | **Resolved** |
| 13 ✅ | Re-KYC trigger — **RESOLVED (P3.6)** | Owner-decided **event-driven/manual**: `POST /v1/kyc/:id/re-kyc` (Gifsy) sets `RE_KYC_REQUIRED` + `reKycFlags`. No time-expiry job | **Resolved** |
| 14 ✅ | Field-level rejection — **RESOLVED (P3.3/3.4)** | `KycVerificationItem` (7 fields, per-field decision+remark+evidence); reject re-opens only the failed field(s) via `reKycFlags`; portal `POST /v1/kyc/:id/verify` + bulk both go through one bridge | **Resolved** |
| 15 ✅ | GST reg type → invoicing — **RESOLVED (capture, P3.4e)** | `entityType`/`gstRegistrationType` now persisted on `ChannelPartner` at GST validation (enum columns). `lib/invoice` consuming them = **P6** (invoicing phase) — seam noted | **Resolved (capture); P6 reads** |
| 16 | POINTS awards never credit the wallet | Credits confirm creates `CreditPayoutEntry` only for PAYOUT rows; **no `Wallet`/`PointsLedger` write anywhere in the credits module** (grep-confirmed). POINTS rows are stored + summed but inert | Post POINTS rows to `PointsLedger`/`Wallet` on confirm (and debit on reversal). **✅ RESOLVED (P6.2, 2026-06-18):** `confirmBatch` credits POINTS rows → the **partner** wallet via `walletService.creditEarn` (resolve `outletCode→partnerId`; outlets roll up to the partner; race-safe guarded `updateMany` status claim prevents double-credit; outlets with no wallet skipped+reported). Reversal approval → `walletService.clawbackAward` (a `DEBIT_ADJUSTMENT` reducing **only** `redeemablePoints` — `earnedPoints`/lifetime* stay monotonic per the locked invariant; already-redeemed shortfall surfaced in the response + ledger for ops). Independently money-path audited (no double-credit/double-debit). See `../plans/reconcile/P6-finance.md §5`. | ~~High~~ → **Resolved (P6.2)** |
| 17 | Visibility has two modes; no selector flag | App photo-capture+approval+fraud (`VisibilitySubmission*`) vs admin amount upload (`OutletVisibilityUploadBatch`/Credits). Both legitimate, **tenant-selected** | **✅ RESOLVED (P6.6, 2026-06-18):** per-tenant `features.visibilityCaptureMode` (`PHOTO_APPROVAL` default \| `AMOUNT_UPLOAD`) in `Client.features` JSON (no migration); `TenantService.resolveVisibilityCaptureMode`; mutating entry points gated by mode (photo approve/reject vs amount bulk-upload). **Follow-up:** dedicated `PUT` admin setter for the flag; photo `submit` still unported (GCS). See `../plans/reconcile/P6-finance.md §5`. | ~~Low~~ → **Resolved (P6.6)** |
| 18 | JSON-blob configs shadow relational models | `ProgramSetting` stores hierarchy/target-config/kpi/banner/gift as JSON, duplicating relational `SalesUser`/`Target`/etc.; admin flows read the blob, not the rows | **◐ HIERARCHY RESOLVED (P2.1):** hierarchy save now writes the relational `SalesHierarchyLevel`/`SalesUser` tree (authoritative) **+** the JSON snapshot (for the existing admin UI). **◐ KPI RESOLVED (P4.4, 2026-06-17):** `kpi_defs` JSON normalized → per-tenant `KpiDef` table (the relational source; KPI codes are the keys inside target/achievement uploads); **admin KPI-management UI wired to it** (P4 FE). **◐ GIFT RESOLVED (P5.3/5.5b, 2026-06-18):** the `gift` JSON blob (`admin/gift-config`/`lib/gifts.ts`) superseded by real `RewardCategory`/`RewardCatalog` CRUD; `admin/gifts` rewired off the blob. **Residual:** apply the same pick-one-source decision to target-config/banner blobs per domain. | **High** (reduced) |
| 19 | Inconsistent money units | Payouts use integer **paise** (`amountPaise`); Credits use **`Decimal` INR** (`amountInr`) | **✅ RESOLVED (P6.0, 2026-06-18): integer `BigInt` paise EVERYWHERE.** Converted the Awards rail's `Decimal`-INR money cols → `BIGINT` paise (renamed `*Inr`→`*Paise`; `totalPoints`→whole `Int`); **widened the existing paise rail `Int`→`BigInt`** (int4 max ₹21.47M would overflow large batch totals/fund balances — audit finding, fixed while tables empty); shared `money.ts` (`rupeesToPaise`/`paiseToRupees`/`toPaiseBigInt`) + global BigInt→Number JSON serialiser; killed the JS `Number()` float-sum. Conversion happens once (FE ingest edge); ÷100 for display only. Gate green (backend 596 / no new platform reds). Dead platform credit routes left on old contract (retire later). See `../plans/reconcile/P6-finance.md §4`. | ~~Medium~~ → **Resolved (P6.0)** |
| 20 | Security boundary lives in an external proxy not in the repo | JWT verification + `x-tenant-slug`/`x-user-*` injection expected from an edge proxy; repo has only fallbacks (`DEFAULT_CLIENT_ID='deoleo'`, Bearer). `clientId` not in token | **✅ ADDRESSED (P1 sessions S3–S4b):** `clientId`+`sid` now in the JWT and bound to the server-side `UserSession`; `getAuthUser` validates the session (revocable) AND enforces subdomain==session-tenant for non-Gifsy — the recommended in-app token↔tenant binding. Proxy still does coarse JWT verify. | ~~High~~ → resolved |
| 21 | Two messaging paths | `lib/msg91.ts` vs `lib/notifications.ts` (generic SMS/WhatsApp gateway env vars) | **DECIDED (0.4c):** MSG91 = sole provider for SMS/OTP/WhatsApp/**email**; retire `notifications.ts` axios senders + `nodemailer`; keep the DB template/queue model; converge the 3 OTP flows. **Phase S S3 built the backend `NotificationsService.enqueue` seam** (domains enqueue QUEUED `NotificationQueue` rows). Remaining = the **delivery worker** (templates/channels/retries/MSG91) + axios/`nodemailer` retirement: **P7**. | Low |
| 22 | Tenant config lives in code, not data | Most settings in `CLIENT_REGISTRY` (`lib/platform/`); onboarding/flag change = code change + redeploy | **✅ ADDRESSED (1.3/1.4):** DB `Client` model + migration + registry backfill; `getTenantConfig` reads the Client row server-side (registry = edge-safe fallback/seed). Admin config UI still TODO (coordinate w/ revamp). | ~~High~~ → resolved |
| 23 | No DB-level tenant isolation | Isolation is per-query `where: { clientId }` discipline; no RLS / Prisma auto-scoping / `Client` FK | **◐ PARTIAL (P1 + Phase S):** header-swap CLOSED — and in the split `clientId` rides the **signed JWT**, so there is no header to swap (the old subdomain==session check is obsoleted) + per-route `clientId` fixes (1.2a/1.7) + the 1.7 isolation audit test. **Phase S S5** added the app-layer `TenantGuard` (asserts a tenant is resolved on every authed request → loud 403 instead of a silent unscoped query; stamps `req.tenantId`, the seam RLS will hook). **Residual:** still per-query discipline — no Postgres RLS / Prisma auto-scoper. **A strict Prisma assert was measured-and-deferred:** ~28 relation-scoped + 41 id-only of 236 query sites carry no direct `clientId` filter, so a naive throw false-positives on ~¼ of sites (each miss = a prod 500) — it must land **with** RLS, sharing one cross-tenant escape-hatch taxonomy. **Resolution: P8.6** (Prisma auto-scope/extension + RLS, `lib/prisma`+infra, audit-gated) → **finalized P9.6** (hardening before first-tenant go-live). | **High** (reduced) |
| 24 | Erasure vs audit tension (DPDP) | `DataRequest` DELETION vs append-only audit/ledger + soft-delete | Define retention + anonymisation policy (hard-delete vs pseudonymise) | Medium |
| 25 | TDS section not differentiated | `TdsRecord` exists but doesn't distinguish incentive (194R) vs visibility service (194C/194J) | Encode section by relationship type; align with §00 direct vs indirect | Medium |
| 26 | Pagination unconfirmed on lists | Risk of unbounded `findMany` on outlets/transactions/tickets | Verify/add pagination on all tenant-scoped list endpoints | Medium |
| 27 | No observability baseline | Only `console.error`; no structured logs/metrics/tracing/alerting documented | Define prod observability baseline | Medium |
| 28 | Points expiry/locking is inert — no `PointsLedger` writes | `lib/wallet.ts` `creditPoints`/`debitPoints` update wallet aggregate counters + `WalletTransaction`, but **never write `PointsLedger`**. Expiry/locking (`PointExpiryConfig`, `lockedUntil`, `expiresAt`) read `PointsLedger` → effectively dead for all credited points | **✅ RESOLVED (P5.2, 2026-06-18):** backend `WalletService` writes `PointsLedger` atomically on **every** path (`creditEarn`=EARN · `debitRedeem`=REDEEM · `reverse`=REVERSE · `adjust`=ADJUST · `expireDuePoints`=EXPIRE) alongside `WalletTransaction`; EARN stamps `expiresAt` from `PointExpiryConfig` + a concurrency-safe expiry sweep. Holding (`lockedUntil`/`lockedPoints`) **deferred** (fields retained; ~½-day, no migration). See `../plans/reconcile/P5-wallet-points-rewards.md`. | ~~Medium~~ → **Resolved (P5)** |
| 31 | **Architecture: built full-stack vs deployed-as-separated (FOUNDATIONAL)** | Code built **full-stack** (Next.js `platform/`, 119 Prisma routes, 0 calls to `api/`) while `terraform/` deploys a **stateless frontend + NestJS `api/` as DB owner** (frontend has no prod `DATABASE_URL` → **couldn't run in prod**). | **✅ DECIDED (2026-06-16) → executing as Phase S.** Code-verified audit (not the unreliable builder docs): the `api/` is real, **well-structured** NestJS but a **100% World-A domain model** (`Sku`/`SkusModule`, partner-class, tiers, scheme-compute; `programName`/`programCategory` = 0) — the model P2 proved wrong; the **platform** has weaker structure but the **correct real model** in **portable `lib/`** (60/63 framework-agnostic). **Decision: API-first dedicated backend; FOUNDATION = platform `lib/`+schema (NOT `api/`).** Stand up the NestJS backend **in place in the `api/` dir** — reuse its proven framework shell (the `gifsy-api` deploy target; build context is hard-wired to `./api`), **delete its World-A domain**, rebuild the real domain from the platform's `lib/`+schema (folding the de-scaffold in), thin the frontend. Now, while greenfield/P2. Rejected: full-stack (wrong shape), revive-`api/` (wrong model). Plan: `../plans/BACKEND-SPLIT-PLAN.md`; arch: `04-architecture.md §1/§6/§8`. **✅ EXECUTED (Phase S, S0–S8, 2026-06-16):** backend stood up in `api/` (124 `/v1` routes, 17 modules, real-model 66-model schema); World-A domain deleted; `TenantGuard` (S5); frontend thinned via a `next.config.ts` proxy `/api/*`→backend `/v1/*` (S6, e2e-verified); infra dead-fallback removed (S7). **The architecture is realigned and the FE is prod-deployable against the backend.** Residual = **retirement only** (not the architecture): ~112 shadowed-but-inert local `src/app/api/*` handlers + platform schema persist until the **16 unported routes** (wrong-model/retired — `admin/skus`, `admin/tiers`, `schemes/calculate`, `reports/billing-trends`, `sales/{invoices,upload,returns,last-upload,leaderboard}`, `admin/sales/*`) and the **4 deferred groups** are ported/reworked — then the whole platform backend retires as one unit. **RE-HOMED (2026-06-17): physical retirement → ~P6, NOT P4.** 120 platform files still use Prisma (auth/session/client-config + the proxy-excluded `rewards/redeem`[P5], `visibility/submit`+`partner/invoices`[P6], `admin/kyc`); deleting the schema before those port breaks the running platform. P4 holds a **backend-only invariant** (no new platform Prisma reads of P4 cols) instead. | **High → ✅ RESOLVED (Phase S); physical retirement → ~P6** |
| 30 | **Two diverged Prisma schemas (platform vs api)** | `platform/prisma/schema.prisma` (80 models) vs `api/prisma/schema.prisma` (74; lacks `Client`+Credits+P2 cols). CI **was** generating the platform client from api's schema → `tsc` failed (fixed 2026-06-16, each app generates from its own). | **✅ RESOLVED by Phase S:** the **backend (in the `api/` dir) owns the single canonical schema** — the platform schema is de-scaffolded and becomes `api/prisma/schema.prisma`, replacing api's World-A 74-model schema. No shared-schema/ownership question remains. (Greenfield — no prod data to reconcile; dev `gifsy_dev` = the canonical schema.) **Nuance (Phase S S8):** the platform's own `schema.prisma` **physically lingers** — the ~112 shadowed local routes + 16 unported + 4 deferred local handlers still import it in dev. It's no longer authoritative (the proxy routes ported traffic to the backend); it retires with the platform backend. **RE-HOMED (2026-06-17) → ~P6** (still-live platform Prisma usage gates it; see #31). | ~~Medium~~ → resolved (Phase S); platform schema retires ~P6 |
| 29 | **Inherited loyalty engine vs the real model (model-alignment, 2026-06-16)** | The codebase is **3 disconnected layers** (full detail: `docs/plans/MODEL-ALIGNMENT.md`). Three inherited concepts contradict the owner-confirmed model: **(a) partner class** (`PartnerClassConfig`/`ChannelPartnerClass`/`PartnerClassCode`, `applicableClasses`, `eligibleClasses[]`) — already decorative (eligibility actually keys off outlet TYPE); **(b) point-tiers** (`TierConfig`/`pointsMultiplier`/`PartnerTierHistory`) — never applied; **(c) SKU/catalog** (`Sku`/`Category`) — nothing consumes it. Real model: **segmentation = PROGRAM** (`Outlet.programName/programCategory`); **sales = parameter upload (no compute)**. | **✅ SCHEMA DE-SCAFFOLD DONE (Phase S S2, 2026-06-16):** canonical `api/prisma/schema.prisma` (66 models) dropped tiers + partner-class (full) + SKU/catalog + invoice-line + `Target`/`TargetAchievement` + `SchemeRule` + compute fields + `SalesInvoice`/`SalesUpload`; migration applied to `gifsy_dev` (80→66). **Remaining:** World-A routes not ported into the backend (S3/S4). ~~net-new program-based targeting (P4)~~ → **SUPERSEDED (2026-06-17):** program is a reporting/filter facet, NOT a targeting dimension (owner). P4 builds per-outlet-per-parameter target/achievement upload (participation = non-blank cells), not program targeting. **✅ BUILT (P4.1–4.5, 2026-06-17):** `KpiDef`/`OutletTarget`/achievement/pace/enrollment all on the real upload model; `SchemeTarget`+compute dropped. See `../plans/reconcile/P4-programs-targets-enrollment.md`. | ~~High~~ → **Resolved (schema S2 + P4 build); program-targeting dropped, upload model shipped** |

| 32 | **Phase-S→P4 route reconciliation: 16 platform routes unported (proxy 404s them)** | After the S6 proxy (`/api/*`→backend `/v1/*`, minus 4 deferred groups), a route-coverage cross-check (independently reviewed) found **16 platform routes with no backend equivalent** → the proxy forwards them to a backend 404. Breakdown: **(a) wrong-model/retired (14)** — `admin/skus`, `admin/tiers`, `schemes/calculate`, `reports/billing-trends`, `sales/{invoices,invoices/[id],upload,returns,last-upload,leaderboard}` (all read dropped `Sku`/`TierConfig`/`SalesInvoice`/`SalesUpload`/`InvoiceReturn`/`Target` or do compute — correctly absent, await P4.x rework; not prod-functional on the FE anyway, no `DATABASE_URL`); **(b) `admin/sales/*` (4 handlers)** — models (`SalesUploadBatch`/`OutletSalesRecord`) **survive** in the backend schema but no controller was built → the **clean near-term port candidate** if the admin sales-upload feature is in scope; **(c) `auth/logout`+`logout-all`** — no backend equivalent **and no UI caller** (logout clears cookie/localStorage client-side), so **server-side session revocation is never invoked** — a real (currently-latent) auth-UX gap. | **Decision (S8): leave as honest 404s + track.** Don't band-aid via proxy-exclusion (handlers need Prisma the prod FE lacks → masks the gap). **✅ `admin/sales/*` PORTED (P4.5):** achievement upload now lives in the backend `AchievementsController` (`/v1/admin/achievements/{upload,batches,pace}` → `OutletSalesRecord`/`SalesUploadBatch`); the FE `admin/sales` screen is repointed to it. The wrong-model routes (`admin/skus`/`tiers`/`schemes/calculate`/`sales/invoices` etc.) stay dropped (no rework needed — the upload model replaces them); `auth/logout` server-side revocation spans the auth/session port (~P6, with the platform-schema retirement). | ~~Medium~~ → **`admin/sales/*` resolved (P4.5); wrong-model dropped; `auth/logout`→~P6** |

**Severity tally:** 17 resolved (#1, #3, #4, #11, #20, #22, #30, **#31 via Phase S**, **#9/#12/#13/#14/#15 via P3**, **#6/#10/#29 via P4**, **#28 via P5**) · High open (#2 engine-done, #16 [P5 wallet primitive built; credit→wallet wiring → P6], #18-reduced [KPI+gift done, target-config/banner residual], #23-reduced→P8.6) · #32 (admin/sales done P4.5; `auth/logout`→~P6) · plus Medium/Low (#5,#7,#8,#17,#19,#24,#25,#26,#27). **Next-phase magnets: #16 (credits→wallet)→P6; #7/#8/#19/#25 (finance)→P6; #21/#27→P7/P8.** P5 landed (#28 resolved; #16 primitive ready). **P6 reconcile drafted (2026-06-18) — decisions locked: #19 = integer paise everywhere (6.0, first); #16 = partner-wallet aggregation; #8 invoicing = included, built last; ⚠️ #25 TDS = ON HOLD pending owner design review. Build not started. See `../plans/reconcile/P6-finance.md`.** | (P5 also surfaced a **P4 follow-up**: 4 `src/lib` admin/targets+sales redesign reds — see `../plans/reconcile/baseline-red-snapshot.txt`.)

### Also noted (to formalize)
- Gifsy's own post-first-approval KYC steps (penny-drop, agreement, final validation) — to be
  documented in §02 Workflows.
- "Inactive manager" signal is currently *blank phone*; prefer an explicit `SalesUser.isActive`/status flag.
- ~~Stale `ROLES` constant in `auth.ts`~~ → **removed in 0.4b** (was dead + inconsistent with the
  `UserRole` enum).
- `clientId` is **not** in the JWT (`TokenPayload`); tenant is resolved from the
  `x-tenant-slug` header. A valid token on the wrong subdomain could mismatch tenant scope
  — verify the proxy/middleware binds them. (Tenant-isolation review, Phase 2.)
