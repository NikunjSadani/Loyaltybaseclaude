# Gap & Inconsistency Register (living)

Accumulated while reverse-engineering the spec. Each entry: current state vs target,
and where it surfaced. Severity is a first pass, to be refined.

| # | Title | Current state | Target / resolution | Severity |
|---|-------|---------------|---------------------|----------|
| 1 | Domain mismatch | ~~Code references `loyaltybase.in`~~ | **✅ CLOSED (0.4a):** all `loyaltybase.in` → `gifsy.in` (8 files; 0 residual). | Low |
| 2 | RBAC is hardcoded | Fixed 11-value `UserRole` enum; coarse `if role !==` checks scattered in routes | **◐ ENGINE DONE (1.6a):** `lib/rbac/can.ts` — default role→permission map + per-tenant overrides. **Operating model (user-confirmed):** GIFSY_ADMIN=all (⊇ every role); CLIENT_ADMIN=all except `GIFSY_OPERATED_PERMISSIONS` (tenancy config, visibility invoicing, money settlement/UTR, activation create/delete); MIS_USER=read-only; Sales/Partner=portal+data-scoped. **Residual:** route enforcement wiring = 1.6b (flag-gated); Gifsy internal sub-roles deferred. **✅ A4 ENFORCEMENT-COVERAGE AUDIT DONE (2026-06-19):** audited all 35 controllers — tenant isolation solid (service-layer `clientId` everywhere + TenantGuard); **most "ungated" endpoints already self-scope** (tickets→own, wallet→own partner, rewards orders→own, banners/banner-config/admin-visibility→service denylist, targets fully gated). Added `@Roles` to the partner/sales self-service surface (partner.* , sales.*, wallet read, rewards redeem/confirm/catalog, schemes enroll/my-enrollment, kyc first-approve/reject) for defense-in-depth + honest 403. **Fixed a real intra-tenant PII read leak: `kyc.ledger` was tenant-scoped with no owner guard → a partner could read any tenant submission's wallet ledger; now partner-callers are restricted to their own (`getOne` was already leak-safe via its post-fetch owner guard, left unchanged).** Fixed `visibility.listSubmissions` (added the partner denylist its sibling `outletStatuses` already had). jest 803/803 + independent audit (SHIP) + runtime role-matrix 12/12 (partner own-ledger 200 / other-ledger 404 / sales-team 403 / partner-me 200 / client-admin wallet 403 / rewards-orders 200). **Deferred (documented):** JWT↔`x-tenant-slug` binding stays at **Gap #23 / P8.6** (RLS) per TenantGuard's design; the `getOne`/`ledger` sales-access asymmetry is **RESOLVED (owner decision 2026-06-19: "sales team can review")** — `getOne`'s post-fetch guard now blocks only PARTNER roles from non-owned (admins + MIS + SALES get tenant-wide read, consistent with `list()`'s stage-queue + `ledger`; partners own-only; cross-tenant still blocked by `kycTenantFilter`; sensitive PII masked for non-admin non-owner). Runtime-verified (sales reviews non-owned detail+ledger 200, blocked cross-tenant 404; partner blocked from other partner 403). | ~~High~~ → **✅ A4 coverage audit done + sales-review granted; full RBAC enforcement engine still flag-gated (1.6b) + x-tenant-slug → #23** |
| 3 | No permission/capability catalog | No master list of taggable features/permissions | **✅ DONE (1.5):** `lib/rbac/permissions.ts` — 72 permissions / 17 groups derived from the §01 capability contexts; exhaustive `Permission` union + `PERMISSION_GROUPS` metadata. | ~~High~~ → resolved |
| 4 | Partner/Outlet level split | Modeled 1:many but operated 1:1; login+wallet on Partner, KYC+visibility on Outlet | **✅ ADDRESSED (P2.4):** kept **1:many + `isPrimary`**, operate 1:1 by convention (future 1:many free). Outlet now carries its **own `clientId`** + **nullable `partnerId`** (owner = `ChannelPartner`, attached at KYC). | ~~Medium~~ → resolved |
| 5 | "Payout" is overloaded (not duplicated) | Traced: `Credit*` = admin-**push** awards (target/focus/visibility → points or INR, offline UTR, reversals); `Payout*`+Fund+TDS = partner-**pull** redemptions (→ INR via provider from prepaid float). Two legitimate flows, but both use the word "payout" (`CreditPayoutEntry` vs `PayoutTransaction`) | **✅ RESOLVED-BY-DESIGN (P6, 2026-06-18):** confirmed **two distinct contexts, NOT a consolidation** — the decision that drove all of P6 (Awards/Credits `api/src/credits` vs Redemption Payouts `api/src/payouts`, kept separate per #19/#25). The module separation makes the contexts unambiguous; the optional cosmetic class rename was judged unnecessary. | ~~Medium~~ → **Resolved (P6)** |
| 6 ✅ | Dynamic enrollment forms — **RESOLVED (P4.2/4.3)** | Was: feature flags + bare `SchemeEnrollment` model, no persistence | **✅ DONE (P4.2/4.3, 2026-06-17):** `SchemeEnrollmentForm` (1:1 scheme, `formSchema` JSON + `campaignType`) persisted via `PUT/GET /v1/schemes/:id/enrollment-form` with a pure structural validator (field types, `CALCULATED` formula refs, single-condition `visibleWhen` dangling/self refs, dup ids). Submission: `POST /v1/schemes/:id/enroll` — **self vs sales-on-behalf** (assignment-gated), **audience by `campaignType`** (LOYALTY_ONLY=KYC-approved / OPEN_CAMPAIGN=non-KYC / MIXED), **server-side `CALCULATED` recompute** (client value discarded), conditional pre-fill, upsert re-enroll. FE: `EnrollmentFormBuilder` + `enrollment-form-renderer` wired. | **Resolved** |
| 7 | Visibility "separate UTR / never clubbed" rule | `CreditField.isSeparatePayout` models it; visibility flagged separate, incentive parameters club into one UTR | **✅ VERIFIED (P6.3, 2026-06-18):** `createPayoutDownload` already enforces it — STANDARD downloads exclude `isSeparatePayout` fields (`NOT: { fieldId: { in: separateFieldIds } }`), SEPARATE downloads isolate a single field. Locked with jest tests. | ~~Medium~~ → **Verified (P6.3)** |
| 8 | Visibility = self-billed invoicing (resolved) | Outlet renders visibility *service*; **Gifsy auto-generates the outlet→Gifsy invoice on the outlet's behalf** (self-billing), GST logic from outlet registration. Outlet views in portal + **edits invoice number** (`AutoInvoice.invoiceNumber`). **Gifsy→Client billing is off-platform (external accounting) — out of scope by design.** | **✅ RESOLVED (P6.7, 2026-06-18):** real `api/src/invoices` module (replaced the all-mock invoicing) — automatic, idempotent per-outlet-per-month generation (`@@unique([clientId,outletCode,period])`); partner-editable `invoiceNumber` validated (regex/≤60/uniqueness) + **locked once PAID** (#8); KYC-complete guard; partner + admin FE wired. Money-path + idempotency audited (re-run can't mutate a PAID invoice). See `../plans/reconcile/P6-finance.md §6`. | ~~Low~~ → **Resolved (P6.7)** |
| 9 ✅ | KYC approval routing — **RESOLVED (P3.2, 2026-06-16)** | Was: `ROLE_PHONES` flat per-role table + hardcoded chain. Now: `KycService.resolveInitialRouting` walks the relational `SalesUser.reportingToId` tree to the submitter's first **ACTIVE** manager (`isActive`/`deletedAt`), maps the manager's hierarchy level → `PENDING_*`, and records escalation when a manager is skipped. Per-hop tenant-scoped (id+clientId), cycle + 10-hop bounded. `ROLE_PHONES` + the phone-blank hack retired (grep-clean) | **Resolved** |
| 10 | Award computation over-built vs upload reality | `SchemeRule` rule engine + `api/schemes/calculate` + auto `SchemeTarget`; "target/achievement" also in `Target`/`TargetAchievement` and `CreditBatch` rows | **DECISION MADE = PRUNE (P4.0 de-scaffold).** Canonical = **tenant computes externally → uploads FINAL amounts per parameter** (Credits / `OutletSalesRecord.kpiValues`), platform does NOT compute. Retire `lib/incentive.ts` + `api/schemes/calculate` + compute fields. Schemes = define+enroll only; Targets = tracking. See `docs/plans/MODEL-ALIGNMENT.md` + Gap #29. **✅ DONE (P4.0 reconcile → P4.4/4.5 build, 2026-06-17):** NO compute anywhere. Participation = the **per-outlet × KPI × month target Excel upload** (blank cell = not configured), stored verbatim in new **`OutletTarget`** (mirrors `OutletSalesRecord`); achievement uploads the same shape; pace = achieved÷target (÷0→null). World-A `SchemeTarget` + `lib/incentive` compute path **dropped**. `SchemeType`=canonical category, FE `IncentiveType` retired, `CampaignType`(LOYALTY/OPEN/MIXED)=enrollment audience. See `../plans/reconcile/P4-programs-targets-enrollment.md`. | ~~Medium~~ → **Resolved (P4)** |
| 11 | Fixed sales-role enum vs configurable hierarchy | `UserRole` hardcodes 5 sales levels (HO/State-Head/ASM/SO/ISR); industry norm is 6 (XSR/SO/ASM/RSM/**ZNM**/NSM) — no Zonal rung; names fixed | **✅ ADDRESSED (P2.1):** hierarchy upload persists the relational `SalesHierarchyLevel`/`SalesUser` tree (incl. **ZNM**); fine role on `SalesHierarchyLevel.code`, coarse `UserRole` bucket via `mapRoleCodeToUserRole` (no enum change). `SalesUser.clientId` + `@@unique([clientId, employeeCode])`. | ~~Medium~~ → resolved |
| 12 ✅ | GST+bank validation — **RESOLVED (P3.4)** | Built as the chosen **Excel-batch bulk-verify** (`POST /v1/kyc/bulk-verify` preview→commit) + portal per-field verify; GST reg type captured (`gstDetails`). `PENDING_AGREEMENT`/`PENDING_PENNY_DROP` folded into the 7-field grid (status stays `PENDING_GIFSY`). API automation = future option-(b) seam | **Resolved** |
| 13 ✅ | Re-KYC trigger — **RESOLVED (P3.6)** | Owner-decided **event-driven/manual**: `POST /v1/kyc/:id/re-kyc` (Gifsy) sets `RE_KYC_REQUIRED` + `reKycFlags`. No time-expiry job | **Resolved** |
| 14 ✅ | Field-level rejection — **RESOLVED (P3.3/3.4)** | `KycVerificationItem` (7 fields, per-field decision+remark+evidence); reject re-opens only the failed field(s) via `reKycFlags`; portal `POST /v1/kyc/:id/verify` + bulk both go through one bridge | **Resolved** |
| 15 ✅ | GST reg type → invoicing — **RESOLVED (capture P3.4e; consumed P6.7)** | `entityType`/`gstRegistrationType` persisted on `ChannelPartner` at GST validation; **P6.7 invoicing now consumes them**: GST only for REGULAR retailers; intra/inter-state from the retailer **GSTIN first-2-digits vs 19 (Tech Gifsy/WB)** → CGST 9%+SGST 9% / IGST 18%; GST-exclusive base; integer-paise. | **Resolved (P6.7)** |
| 16 | POINTS awards never credit the wallet | Credits confirm creates `CreditPayoutEntry` only for PAYOUT rows; **no `Wallet`/`PointsLedger` write anywhere in the credits module** (grep-confirmed). POINTS rows are stored + summed but inert | Post POINTS rows to `PointsLedger`/`Wallet` on confirm (and debit on reversal). **✅ RESOLVED (P6.2, 2026-06-18):** `confirmBatch` credits POINTS rows → the **partner** wallet via `walletService.creditEarn` (resolve `outletCode→partnerId`; outlets roll up to the partner; race-safe guarded `updateMany` status claim prevents double-credit; outlets with no wallet skipped+reported). Reversal approval → `walletService.clawbackAward` (a `DEBIT_ADJUSTMENT` reducing **only** `redeemablePoints` — `earnedPoints`/lifetime* stay monotonic per the locked invariant). Already-redeemed **shortfall** persisted on `CreditReversal.shortfallPaise` for the reversal report (supposed=`approvedPaise` / reversed=`approved−shortfall` / pending=`shortfall`); the client settles `pending` **off-platform** (owner-decided — platform does nothing with it). Independently money-path audited (no double-credit/double-debit). See `../plans/reconcile/P6-finance.md §5`. | ~~High~~ → **Resolved (P6.2)** |
| 17 | Visibility has two modes; no selector flag | App photo-capture+approval+fraud (`VisibilitySubmission*`) vs admin amount upload (`OutletVisibilityUploadBatch`/Credits). Both legitimate, **tenant-selected** | **✅ RESOLVED (P6.6, 2026-06-18):** per-tenant `features.visibilityCaptureMode` (`PHOTO_APPROVAL` default \| `AMOUNT_UPLOAD`) in `Client.features` JSON (no migration); `TenantService.resolveVisibilityCaptureMode`; mutating entry points gated by mode (photo approve/reject vs amount bulk-upload). **Gifsy-admin toggle added** (`PUT /v1/admin/settings/visibility-capture-mode`, `tenancy:manage_flags`, merges `features` without clobber; segmented control on the admin settings screen). Residual: photo `submit` still unported (GCS). See `../plans/reconcile/P6-finance.md §5`. | ~~Low~~ → **Resolved (P6.6)** |
| 18 | JSON-blob configs shadow relational models | `ProgramSetting` stores hierarchy/target-config/kpi/banner/gift as JSON, duplicating relational `SalesUser`/`Target`/etc.; admin flows read the blob, not the rows | **◐ HIERARCHY RESOLVED (P2.1):** hierarchy save now writes the relational `SalesHierarchyLevel`/`SalesUser` tree (authoritative) **+** the JSON snapshot (for the existing admin UI). **◐ KPI RESOLVED (P4.4, 2026-06-17):** `kpi_defs` JSON normalized → per-tenant `KpiDef` table (the relational source; KPI codes are the keys inside target/achievement uploads); **admin KPI-management UI wired to it** (P4 FE). **◐ GIFT RESOLVED (P5.3/5.5b, 2026-06-18):** the `gift` JSON blob (`admin/gift-config`/`lib/gifts.ts`) superseded by real `RewardCategory`/`RewardCatalog` CRUD; `admin/gifts` rewired off the blob. **Residual:** apply the same pick-one-source decision to target-config/banner blobs per domain. | **High** (reduced) |
| 19 | Inconsistent money units | Payouts use integer **paise** (`amountPaise`); Credits use **`Decimal` INR** (`amountInr`) | **✅ RESOLVED (P6.0, 2026-06-18): integer `BigInt` paise EVERYWHERE.** Converted the Awards rail's `Decimal`-INR money cols → `BIGINT` paise (renamed `*Inr`→`*Paise`; `totalPoints`→whole `Int`); **widened the existing paise rail `Int`→`BigInt`** (int4 max ₹21.47M would overflow large batch totals/fund balances — audit finding, fixed while tables empty); shared `money.ts` (`rupeesToPaise`/`paiseToRupees`/`toPaiseBigInt`) + global BigInt→Number JSON serialiser; killed the JS `Number()` float-sum. Conversion happens once (FE ingest edge); ÷100 for display only. Gate green (backend 596 / no new platform reds). Dead platform credit routes left on old contract (retire later). See `../plans/reconcile/P6-finance.md §4`. | ~~Medium~~ → **Resolved (P6.0)** |
| 20 | Security boundary lives in an external proxy not in the repo | JWT verification + `x-tenant-slug`/`x-user-*` injection expected from an edge proxy; repo has only fallbacks (`DEFAULT_CLIENT_ID='deoleo'`, Bearer). `clientId` not in token | **✅ ADDRESSED (P1 sessions S3–S4b):** `clientId`+`sid` now in the JWT and bound to the server-side `UserSession`; `getAuthUser` validates the session (revocable) AND enforces subdomain==session-tenant for non-Gifsy — the recommended in-app token↔tenant binding. Proxy still does coarse JWT verify. | ~~High~~ → resolved |
| 21 | Two messaging paths | `lib/msg91.ts` vs `lib/notifications.ts` (generic SMS/WhatsApp gateway env vars) | **DECIDED (0.4c):** MSG91 = sole provider for SMS/OTP/WhatsApp/**email**; retire `notifications.ts` axios senders + `nodemailer`; keep the DB template/queue model; converge the 3 OTP flows. **Phase S S3 built the backend `NotificationsService.enqueue` seam** (domains enqueue QUEUED `NotificationQueue` rows). Remaining = the **delivery worker** (templates/channels/retries/MSG91) + axios/`nodemailer` retirement: **P7**. | Low |
| 22 | Tenant config lives in code, not data | Most settings in `CLIENT_REGISTRY` (`lib/platform/`); onboarding/flag change = code change + redeploy | **✅ ADDRESSED (1.3/1.4):** DB `Client` model + migration + registry backfill; `getTenantConfig` reads the Client row server-side (registry = edge-safe fallback/seed). Admin config UI still TODO (coordinate w/ revamp). | ~~High~~ → resolved |
| 23 | No DB-level tenant isolation | Isolation is per-query `where: { clientId }` discipline; no RLS / Prisma auto-scoping / `Client` FK | **◐ PARTIAL (P1 + Phase S):** header-swap CLOSED — and in the split `clientId` rides the **signed JWT**, so there is no header to swap (the old subdomain==session check is obsoleted) + per-route `clientId` fixes (1.2a/1.7) + the 1.7 isolation audit test. **Phase S S5** added the app-layer `TenantGuard` (asserts a tenant is resolved on every authed request → loud 403 instead of a silent unscoped query; stamps `req.tenantId`, the seam RLS will hook). **Residual:** still per-query discipline — no Postgres RLS / Prisma auto-scoper. **A strict Prisma assert was measured-and-deferred:** ~28 relation-scoped + 41 id-only of 236 query sites carry no direct `clientId` filter, so a naive throw false-positives on ~¼ of sites (each miss = a prod 500) — it must land **with** RLS, sharing one cross-tenant escape-hatch taxonomy. **Resolution: P8.6** (Prisma auto-scope/extension + RLS, `lib/prisma`+infra, audit-gated) → **finalized P9.6** (hardening before first-tenant go-live). | **High** (reduced) |
| 24 | Erasure vs audit tension (DPDP) | `DataRequest` DELETION vs append-only audit/ledger + soft-delete | Define retention + anonymisation policy (hard-delete vs pseudonymise) | Medium |
| 25 | TDS section not differentiated | `TdsRecord` exists but doesn't distinguish incentive (194R) vs visibility service (194C/194J) | **◐ SPEC DRAFTED (2026-06-18) — awaiting owner sign-off; NO code yet.** Owner-confirmed model: **194R** (incentive; deductor=client; per-tenant/FY; 10%/20%-no-PAN; ₹20k threshold) vs **194C** (visibility; deductor=Gifsy; platform-level per-PAN; 1%/2%/20%; >₹30k single or >₹1L agg; GST-exclusive base; two columns w/ & w/o threshold). **Grossed-up (payer-borne)**, PAN-keyed (incl. off-platform unknown PANs), FY-scoped; compute+track+export only (filing/16A off-platform; §206AB removed). Spec: `../plans/reconcile/P6.5-TDS-SPEC.md`. **◐ 6.5a BUILT (2026-06-18):** foundation schema (`TdsOffPlatformEntry`/`TdsDeposit`, `RedemptionOrder.valuePaise`, `tds_records.section`) + `tds.ts` calc (grossed-up; round-to-rupee) + **aggregation engine** (`src/tds`: 194R & 194C per-PAN/FY, thresholds, two columns, deposits→outstanding) + redemption value frozen at confirm (= points÷conversionRate); read endpoints. Money-path audited (2 HIGH fixed: fractional-rate value, 194C cross-tenant outletCode key). **◐ 6.5b BUILT:** off-platform 194R upload + TDS deposit upload (194R=client, 194C=Gifsy-only) — template/preview/apply, rupees→paise, PAN-required, `uploadBatchId` dedup; liability-tracker read (liability−deposited=outstanding). **✅ RESOLVED (P6.5 complete, 2026-06-18):** 6.5a engine + 6.5b uploads/liability + 6.5c exports (194R reference Excel + 194C two-column report) + 6.5d admin FE + the redemption→payout settlement bridge. Compute+track+export only; filing/Form-16A off-platform. Money-path audited. | ~~Medium~~ → **Resolved (P6.5)** |
| 26 | Pagination unconfirmed on lists | Risk of unbounded `findMany` on outlets/transactions/tickets | Verify/add pagination on all tenant-scoped list endpoints | Medium |
| 27 | No observability baseline | Only `console.error`; no structured logs/metrics/tracing/alerting documented | Define prod observability baseline | Medium |
| 28 | Points expiry/locking is inert — no `PointsLedger` writes | `lib/wallet.ts` `creditPoints`/`debitPoints` update wallet aggregate counters + `WalletTransaction`, but **never write `PointsLedger`**. Expiry/locking (`PointExpiryConfig`, `lockedUntil`, `expiresAt`) read `PointsLedger` → effectively dead for all credited points | **✅ RESOLVED (P5.2, 2026-06-18):** backend `WalletService` writes `PointsLedger` atomically on **every** path (`creditEarn`=EARN · `debitRedeem`=REDEEM · `reverse`=REVERSE · `adjust`=ADJUST · `expireDuePoints`=EXPIRE) alongside `WalletTransaction`; EARN stamps `expiresAt` from `PointExpiryConfig` + a concurrency-safe expiry sweep. Holding (`lockedUntil`/`lockedPoints`) **deferred** (fields retained; ~½-day, no migration). See `../plans/reconcile/P5-wallet-points-rewards.md`. | ~~Medium~~ → **Resolved (P5)** |
| 31 | **Architecture: built full-stack vs deployed-as-separated (FOUNDATIONAL)** | Code built **full-stack** (Next.js `platform/`, 119 Prisma routes, 0 calls to `api/`) while `terraform/` deploys a **stateless frontend + NestJS `api/` as DB owner** (frontend has no prod `DATABASE_URL` → **couldn't run in prod**). | **✅ DECIDED (2026-06-16) → executing as Phase S.** Code-verified audit (not the unreliable builder docs): the `api/` is real, **well-structured** NestJS but a **100% World-A domain model** (`Sku`/`SkusModule`, partner-class, tiers, scheme-compute; `programName`/`programCategory` = 0) — the model P2 proved wrong; the **platform** has weaker structure but the **correct real model** in **portable `lib/`** (60/63 framework-agnostic). **Decision: API-first dedicated backend; FOUNDATION = platform `lib/`+schema (NOT `api/`).** Stand up the NestJS backend **in place in the `api/` dir** — reuse its proven framework shell (the `gifsy-api` deploy target; build context is hard-wired to `./api`), **delete its World-A domain**, rebuild the real domain from the platform's `lib/`+schema (folding the de-scaffold in), thin the frontend. Now, while greenfield/P2. Rejected: full-stack (wrong shape), revive-`api/` (wrong model). Plan: `../plans/BACKEND-SPLIT-PLAN.md`; arch: `04-architecture.md §1/§6/§8`. **✅ EXECUTED (Phase S, S0–S8, 2026-06-16):** backend stood up in `api/` (124 `/v1` routes, 17 modules, real-model 66-model schema); World-A domain deleted; `TenantGuard` (S5); frontend thinned via a `next.config.ts` proxy `/api/*`→backend `/v1/*` (S6, e2e-verified); infra dead-fallback removed (S7). **The architecture is realigned and the FE is prod-deployable against the backend.** Residual = **retirement only** (not the architecture): ~112 shadowed-but-inert local `src/app/api/*` handlers + platform schema persist until the **16 unported routes** (wrong-model/retired — `admin/skus`, `admin/tiers`, `schemes/calculate`, `reports/billing-trends`, `sales/{invoices,upload,returns,last-upload,leaderboard}`, `admin/sales/*`) and the **4 deferred groups** are ported/reworked — then the whole platform backend retires as one unit. **RE-HOMED (2026-06-17): physical retirement → ~P6, NOT P4.** 120 platform files still use Prisma (auth/session/client-config + `partner/invoices`[P6]); the `next.config.ts` proxy-exclusion list is now EMPTY — `rewards/redeem`[P5] + `visibility/submit`[P0.6, 2026-06-19] are ported and their dead local routes deleted, and `admin/kyc` was a no-op (KYC already at `/v1/kyc/*`). Deleting the schema before the remaining platform-Prisma usage (auth/session/client-config) ports still breaks the running platform. P4 holds a **backend-only invariant** (no new platform Prisma reads of P4 cols) instead. | **High → ✅ RESOLVED (Phase S); ✅ PHYSICAL RETIREMENT DONE (D2, `60b5a76`, 2026-06-20):** deleted 113 dead `app/api` routes + dead-transitive `lib` + `lib/prisma.ts` + `platform/prisma/` + `prisma.config.ts` + the `@prisma/*`/`prisma`/`pg` deps; rewired the one live importer (`layout.tsx` tenant-config → in-code `CLIENT_REGISTRY`, behavior-identical); fixed the `Dockerfile`/`ci.yml` `prisma generate` deploy breakers. 5 independent audits; tsc 0 · next build green · harness 59 · vitest 0-new-reds. ⚠️ Docker image build not run locally (no Docker) → first build = staging deploy. |
| 30 | **Two diverged Prisma schemas (platform vs api)** | `platform/prisma/schema.prisma` (80 models) vs `api/prisma/schema.prisma` (74; lacks `Client`+Credits+P2 cols). CI **was** generating the platform client from api's schema → `tsc` failed (fixed 2026-06-16, each app generates from its own). | **✅ RESOLVED by Phase S:** the **backend (in the `api/` dir) owns the single canonical schema** — the platform schema is de-scaffolded and becomes `api/prisma/schema.prisma`, replacing api's World-A 74-model schema. No shared-schema/ownership question remains. (Greenfield — no prod data to reconcile; dev `gifsy_dev` = the canonical schema.) **Nuance (Phase S S8):** the platform's own `schema.prisma` **physically lingers** — the ~112 shadowed local routes + 16 unported + 4 deferred local handlers still import it in dev. It's no longer authoritative (the proxy routes ported traffic to the backend); it retires with the platform backend. **RE-HOMED (2026-06-17) → ~P6** (still-live platform Prisma usage gates it; see #31). | ~~Medium~~ → resolved (Phase S); platform schema retires ~P6 |
| 29 | **Inherited loyalty engine vs the real model (model-alignment, 2026-06-16)** | The codebase is **3 disconnected layers** (full detail: `docs/plans/MODEL-ALIGNMENT.md`). Three inherited concepts contradict the owner-confirmed model: **(a) partner class** (`PartnerClassConfig`/`ChannelPartnerClass`/`PartnerClassCode`, `applicableClasses`, `eligibleClasses[]`) — already decorative (eligibility actually keys off outlet TYPE); **(b) point-tiers** (`TierConfig`/`pointsMultiplier`/`PartnerTierHistory`) — never applied; **(c) SKU/catalog** (`Sku`/`Category`) — nothing consumes it. Real model: **segmentation = PROGRAM** (`Outlet.programName/programCategory`); **sales = parameter upload (no compute)**. | **✅ SCHEMA DE-SCAFFOLD DONE (Phase S S2, 2026-06-16):** canonical `api/prisma/schema.prisma` (66 models) dropped tiers + partner-class (full) + SKU/catalog + invoice-line + `Target`/`TargetAchievement` + `SchemeRule` + compute fields + `SalesInvoice`/`SalesUpload`; migration applied to `gifsy_dev` (80→66). **Remaining:** World-A routes not ported into the backend (S3/S4). ~~net-new program-based targeting (P4)~~ → **SUPERSEDED (2026-06-17):** program is a reporting/filter facet, NOT a targeting dimension (owner). P4 builds per-outlet-per-parameter target/achievement upload (participation = non-blank cells), not program targeting. **✅ BUILT (P4.1–4.5, 2026-06-17):** `KpiDef`/`OutletTarget`/achievement/pace/enrollment all on the real upload model; `SchemeTarget`+compute dropped. See `../plans/reconcile/P4-programs-targets-enrollment.md`. | ~~High~~ → **Resolved (schema S2 + P4 build); program-targeting dropped, upload model shipped** |

| 32 | **Phase-S→P4 route reconciliation: 16 platform routes unported (proxy 404s them)** | After the S6 proxy (`/api/*`→backend `/v1/*`, minus 4 deferred groups), a route-coverage cross-check (independently reviewed) found **16 platform routes with no backend equivalent** → the proxy forwards them to a backend 404. Breakdown: **(a) wrong-model/retired (14)** — `admin/skus`, `admin/tiers`, `schemes/calculate`, `reports/billing-trends`, `sales/{invoices,invoices/[id],upload,returns,last-upload,leaderboard}` (all read dropped `Sku`/`TierConfig`/`SalesInvoice`/`SalesUpload`/`InvoiceReturn`/`Target` or do compute — correctly absent, await P4.x rework; not prod-functional on the FE anyway, no `DATABASE_URL`); **(b) `admin/sales/*` (4 handlers)** — models (`SalesUploadBatch`/`OutletSalesRecord`) **survive** in the backend schema but no controller was built → the **clean near-term port candidate** if the admin sales-upload feature is in scope; **(c) `auth/logout`+`logout-all`** — no backend equivalent **and no UI caller** (logout clears cookie/localStorage client-side), so **server-side session revocation is never invoked** — a real (currently-latent) auth-UX gap. | **Decision (S8): leave as honest 404s + track.** Don't band-aid via proxy-exclusion (handlers need Prisma the prod FE lacks → masks the gap). **✅ `admin/sales/*` PORTED (P4.5):** achievement upload now lives in the backend `AchievementsController` (`/v1/admin/achievements/{upload,batches,pace}` → `OutletSalesRecord`/`SalesUploadBatch`); the FE `admin/sales` screen is repointed to it. The wrong-model routes (`admin/skus`/`tiers`/`schemes/calculate`/`sales/invoices` etc.) stay dropped (no rework needed — the upload model replaces them); `auth/logout` server-side revocation spans the auth/session port (~P6, with the platform-schema retirement). | ~~Medium~~ → **`admin/sales/*` resolved (P4.5); wrong-model dropped; ✅ `auth/logout` RESOLVED (D2, 2026-06-20):** owner decision = **stateless per-user logout (B1)** — the dead platform `auth/logout*` routes are deleted; the live backend `POST /v1/admin/force-logout-all` is untouched (break-glass); a per-user backend logout is tracked in `POST-GO-LIVE-BACKLOG.md §B`. |

| 33 | **Auth/login broken end-to-end (runtime, 2026-06-18)** | After the Phase-S proxy cutover the thin FE was never reconciled to the backend auth contract. **4 breaks (verified live):** (1) login action posts `{mobile}`, backend `SendOtpDto` requires `{phone}` (whitelist → 400 `"property mobile should not exist"`); (2) `verify-otp` omits required `clientId`; (3) action reads `data.data.token`, backend returns `accessToken` → empty cookie; (4) session set as httpOnly cookie but `lib/api-client.ts:20` reads `localStorage.token`. No `middleware.ts` route guard. **No real user can log in** (the empty dev DB is downstream of this). Backend login works with the correct contract. | **✅ RESOLVED (P0.5 Wave 0, 2026-06-18):** login contract fixed (`phone`+`clientId`); token→`localStorage`(`accessToken`); `getRoleDashboard` corrected (was partner→`/dashboard` 404 & sales→admin); `RequireAuth` client route guard added to all 4 portal layouts; `logout()` helper clears token + redirects. Runtime-verified per role (login→OTP→token→role redirect→logout); independent audit "sound to ship"; gate green (0 new vitest reds). CI smoke-login per role still recommended (deferred to test-infra). See `../plans/reconcile/P0.5-make-it-runnable.md §6`. | ~~Critical~~ → **Resolved (P0.5 W0)** |
| 34 | **Pervasive FE auth-attachment inconsistency (runtime)** | Many FE pages don't attach the Bearer token on their `/api` fetch → 401 → silent demo/mock fallback. Verified-401: admin Overview, KYC Submissions, Approvals, Visibility, Payouts, partner Wallet (real wallet=50k shown as demo 8,550). Pages routed through `api-client`/`authHeader` DO attach + show real data (TDS, credits, targets, invoices, schemes, sales/kyc). | **✅ RESOLVED (P0.5 Wave 1A, 2026-06-18):** added `authHeader()` to 14 bare `fetch('/api/...')` calls across `platform/src/app` (admin Overview/KYC/Visibility/Payouts, partner Wallet + more). Runtime-verified: admin Overview now fires `dashboard/kpis`→200 with real data. **◐ CORRECTED (2026-06-19) — only PARTIALLY resolved:** Wave 1A fixed *auth-attachment* (the fetches now authenticate), but comprehensive role-matrix runtime testing showed many pages **still render fabricated demo data beneath the now-authenticated fetch** (→ #40) or are **role-gated out** (→ #41). Only 1 of 14 pages (admin Overview) was originally verified — the "pages revived to real data" claim was overstated. "Auth attaches" ✅; "shows real scoped data" ◐. | ~~Critical~~ → **◐ Partial (auth ✅ / data-real ◐); see #40, #41, #46** |
| 35 | **Admin Gift Catalogue 500 — uncoerced pagination param (runtime)** | `api/src/rewards/rewards.service.ts:932-950` `adminListCatalog` does `take: limit` with the raw query-string (`q.limit ?? 50`); FE sends `?limit=200` → `take:"200"` → `PrismaClientValidationError` → 500. Admin catalogue breaks once any reward row exists; tests missed it (numeric args, not HTTP). Concrete instance of #26. | **✅ RESOLVED (P0.5 Wave 1C, 2026-06-18):** root cause was an inline-intersection `@Query()` type on `admin-rewards.controller` erasing to `Object` → ValidationPipe skipped transform → string `limit` reached Prisma `take`. Fixed with a concrete `AdminListCatalogQueryDto` (+`@Max(200)`); swept all 14 list DTOs (no others uncoerced); regression test added. Runtime-verified: `catalog?limit=200`→200 (was 500), `?limit=999`→clean 400. Closes the concrete instance of #26. | ~~High~~ → **Resolved (P0.5 W1C)** |
| 36 | **Dashboards fabricate data + genuinely-unwired pages (runtime)** | Admin Overview fetches `dashboard/kpis` → 401 → fabricates "4,821 partners / ₹2.14 Cr"; the **4 sub-dashboards make NO fetch** (hardcoded 248/221 over an empty DB). No-backend-call mock pages: admin Tickets (local store), orphan `/admin/outlets` (mock array), Invoice-Upload (mock flow + dead template link), partner Visibility status, partner Support (localStorage). Plus the dead write-paths (KYC approve, redemption, visibility submit, invoice gen — success UI, no persist). | **P0.6** — wire dashboards to real aggregation endpoints (or hide); wire the dead write-paths to existing backend endpoints; demo chrome stripped P0.7. | ◐ **PARTIAL (2026-06-19):** dashboards real (#47), redemption write FIXED + **visibility-submit PORTED** (#50, harness write-persistence green); KYC *write* endpoints exist at `/v1/kyc/*` but the **Gifsy cross-tenant access is a real bug** → fix in **#38/A1**; **STILL dead:** invoice-gen (B2), sales `/catalogue` redemption fake (B1) |
| 37 | **Broken seed + empty dev DB (runtime)** | `api/prisma/seed.ts:24` `new PrismaClient()` lacks the Prisma-7 driver adapter → throws; not wired into `prisma.config.ts`. `gifsy_dev` had zero users/business data → app un-runnable + "complete" features render empty. | **✅ RESOLVED (P0.5 Wave 1B, 2026-06-18):** `seed.ts` rebuilt with the pg adapter (was throwing under Prisma 7) + `current_database()='gifsy_dev'` safety guard; wired into `prisma.config.ts` (ts-node); extended to a realistic idempotent demo dataset (deoleo: CLIENT_ADMIN, 2 partners+wallets+outlets, sales hierarchy/user/assignment, reward category+2 items, 2 pending KYC). Verified idempotent (ran twice, identical row counts). `npx prisma db seed` now works. | ~~High~~ → **Resolved (P0.5 W1B)** |

| 38 | **KYC approval flow broken end-to-end (runtime, 2026-06-19)** | The canonical Gifsy approval is the **BULK** flow at `/admin/kyc/approvals` (in nav; `review-queue`+`bulk-verify`+per-field `:id/verify`, built P3) — NOT the orphan `/admin/approvals` (no nav; the P0.6-D agent was mis-scoped onto it). Runtime found the single-record path broken for **everyone**: `POST /api/kyc/:id/approve` is `@Roles('GIFSY_ADMIN')` **and** tenant-scopes the lookup to the *caller's* clientId → CLIENT_ADMIN **403**, GIFSY_ADMIN (clientId `gifsy`) **404** on a tenant's KYC (the cross-tenant Gifsy operator can't reach the records it must approve). Also: sales `submit-for-review`/`resubmit` have **no backend endpoint**. Same gap-class as the tickets bug (`VERIFICATION-PROTOCOL.md`: tenant-scoped lookup blocks the cross-tenant operator). | **NEXT VERIFIED UNIT:** verify the canonical **bulk** flow per `VERIFICATION-PROTOCOL.md`; make KYC approval/review endpoints **Gifsy-cross-tenant-aware** (scope by the *record's* tenant, GIFSY exempt from caller-tenant filter); re-do + runtime-verify `sales/kyc/[id]` first-approve (approve→`first-approve`, reject→`reject {reason}`, submit/resubmit = honest error until a route exists); delete the orphan `/admin/approvals` (P0.7). **CODE-CONFIRMED 2026-06-19:** every KYC service lookup is `user: { clientId: caller.clientId }` (getOne/firstApprove/approve/verifyField/reject) → GIFSY 404 on tenant records; this is a **bug fix, not a verify**, and it is **systemic** — `visibility` approve/reject (`partner: { user: { clientId: caller } }`) and `payouts` (`clientId: caller`) have the same class. Fix = one shared "GIFSY exempt from caller-tenant filter; scope by record's tenant" rule. **✅ RESOLVED (A1, 2026-06-19):** `kycTenantFilter()`/`submissionTenantFilter()` make GIFSY_ADMIN exempt (else clientId-scoped) across all KYC operator methods (list/getOne/update/firstApprove/approve/verifyField/reject/ledger/reKyc/gstDetails/bulkVerify/reviewQueue/reviewDump) + visibility (listSubmissions/approve/reject/listFraudLog); reviewQueue emits each record's own clientId; FE `/admin/kyc/approvals` got a brand column+filter; clientb seeded a PENDING_GIFSY KYC. Runtime-verified (6 checks: Gifsy sees both brands, getOne deoleo+clientb 200 was 404, verifyField persists, CLIENT_ADMIN 403, tenant isolation holds both ways) + api jest 783/783 + harness 39/39 + independent audit PASS-WITH-NOTES (no leak; notes = slaMetrics/outletStatuses same-class siblings → A3). | ~~High~~ → **✅ RESOLVED (A1); slaMetrics/outletStatuses residual → A3** |

| 39 | **GIFSY_ADMIN cannot log in (runtime, 2026-06-19)** | The login `resolveClientId(host)` never yields `gifsy`: localhost + `www`/`platform`/bare-domain all → `DEFAULT_CLIENT_ID='deoleo'`. So a GIFSY operator (user `clientId='gifsy'`) sends `clientId:'deoleo'` to `verify-otp` → **401 "No account found"**. The platform operator can't log in at all (the demo persona switcher hid this; real-login was never exercised for GIFSY). Found by comprehensive Wave-0 role-matrix testing (CLIENT_ADMIN/partner/sales work; GIFSY ✗). | **P0.5 follow-up (auth):** decide how the Gifsy operator resolves `clientId='gifsy'` (a `gifsy`/`platform` subdomain mapping, or a dedicated operator login) + a localhost-dev path. Fold into the auth+tenant-resolution hardening. | ~~High~~ → **✅ RESOLVED (2026-06-19, `a073ac7`):** dev `clientId` override on the login form + prod subdomain plan; GIFSY (+clientb) login works, harness-verified |
| 40 | **Pervasive FE fabricated/demo data still rendered (runtime, 2026-06-19)** | Even with auth attaching (#34), many pages render **hardcoded demo leftovers** from the pre-backend demo era: partner/wallet **statement** ("Bal 8,550" over an empty ledger while the balance is the real 50k), partner/dashboard **"Available 2,947 pts" (contradicts real 50k) + "#12 of 248 partners"**, the 4 admin sub-dashboards (no fetch), the **DEMO persona switchers** (`useAdminSession` + partner/sales WS/SSS toggles — identities decoupled from the real JWT). The app is a confusing **mix** of real + fabricated, so users can't tell what's real. By design: every page shows ONLY real backend data scoped to role — nothing hardcoded (`DATA-VISIBILITY.md`). | **P0.6 (per-flow, as each page is wired) + P0.7 (strip persona switchers).** Remove ALL hardcoded demo data; show real-or-honest-empty. Verified by the E2E harness asserting no fabricated values appear (#46). | ~~High~~ → **✅ RESOLVED (2026-06-19):** partner/sales identity via `/partner/me` (`54e9238`), admin KPIs real (`2538294`); harness fabricated fail-list green. Residual (P0.7): partner numeric demo fields + the sales role-switcher chrome |
| 41 | **Role/audience mismatches — endpoints role-gate the page's intended audience (runtime)** | The same gap-class as tickets(#36, fixed) and KYC(#38): a page sits in a role's portal/nav but its endpoints exclude that role. Confirmed: **admin/payouts → 403** for CLIENT_ADMIN (`@Roles('GIFSY_ADMIN','MIS_USER')`) though "Payout Management" is in the CLIENT_ADMIN nav. Likely more across wallet/credits/visibility/invoices. Needs a per-page **who-should-see-this** decision (`DATA-VISIBILITY.md`) then align the backend role guards + tenant scoping. | **P0.6 (per-flow).** For each page: confirm intended audience (owner decision in `DATA-VISIBILITY.md`); align `@Roles`/scoping; honest 403 where genuinely gated. | ~~High~~ → **✅ RESOLVED (2026-06-19):** FE role guards (`8025587`) + Q1 payouts GIFSY-only (`9cbf841`); cross-portal scoping harness-verified. (Per-page audience = `DATA-VISIBILITY.md` Q1–Q6.) |
| 42 | **`payouts.processBatch` un-transactioned + no guarded claim (static backend audit)** | `api/src/payouts/payouts.service.ts:280-408` runs mark-PROCESSING→create-TdsRecords→flip-INITIATED→finalize as independent auto-commit calls with a read-then-write status check (not the guarded `updateMany` claim used elsewhere) → concurrent calls duplicate TdsRecords/double-flip; mid-pipeline crash leaves partial state, no rollback. Also recomputes TDS inline (flat rate) vs the canonical TDS engine → reconciliation drift. | **P0.6-I:** wrap in `$transaction` + guarded claim; reuse the canonical TDS engine (or document the divergence). Money path — audit hard. | ~~High~~ → **✅ RESOLVED (A3, 2026-06-19):** `processBatch` now does a guarded atomic claim (`payoutBatch.updateMany` with `status: { in: [DRAFT,SUBMITTED,FAILED] }` → 0-count = BadRequest, no read-then-write race); disbursement + finalisation wrapped in `$transaction`; on any post-claim throw the batch is reset to FAILED (re-claimable, never stranded in PROCESSING). **Owner decision (2026-06-19): payouts disburse the FULL amount — the inline flat-194R TDS withholding + `TdsRecord` write were REMOVED entirely** (TDS is owned by the P6.5 engine, which reads `RedemptionOrder.valuePaise` directly). Plus the **batch-from-pending sweep** (`POST /v1/payouts/batches/:id/assign-pending`) — the missing consumer that assigns unbatched redemption `PayoutTransaction`s into a DRAFT batch (tenant + payoutMode scoped). jest 19/19 + independent adversarial audit (SHIP, no money/cross-tenant defect) + **runtime-verified end-to-end** (assume-tenant deoleo → unbatched list shows only deoleo not clientb → create batch → assign-pending=1 → process: SUBMITTED, required=full gross 500000, flagged 1; DB: deoleo INITIATED+batched, clientb still PENDING+unbatched, **0 TdsRecords**). |
| 43 | **TDS small gaps (runtime + audit)** | (a) admin TDS page calls `/admin/tds/liability` **without the required `section`** → 400; tiles fall back to ₹0. (b) `processBatch` duplicates TDS compute vs the canonical engine (see #42). | (a) **P0.6** add the `section` param. (b) **pre-launch payout hardening** reconcile. | ~~Med~~ → **✅ RESOLVED:** (a) **was already in the backend** — `GET /v1/admin/tds/liability?section=194R|194C&fy=` exists (`tds.controller.ts:246`, `LiabilityQueryDto`, 194C-GIFSY-only guard); if FE tiles still 400 the FE must pass `section` (FE wiring → A4/B sweep). (b) RESOLVED by A3 (#42) — the inline duplicate TDS compute was removed; the canonical P6.5 engine is the sole 194R source. |
| 44 | **Excel round-trip gaps (static + runtime)** | Invoice-Upload page is a mock (hardcoded outlet registry, no POST, dead "Download sample template"); Enrollments export ships **MOCK** data; "Download Final Targets" export emits a single-header shape the upload parser can't re-ingest (view-only, latent trap). | **P0.6-F** (invoice generation + visibility) wires invoice-upload; **P0.7** fixes enrollments-export + final-targets header. | **◐ B2 — invoice slice DONE (`e07c06a`, 2026-06-20):** invoice Excel **export** (`GET /v1/{admin,partner}/invoices/export`, filter+tenant+partner scoped, GST CGST/SGST split that reconstitutes gstPaise exactly) + **template** + real **upload page** (drives the no-compute period-generation, not amount-ingest — amount-ingest would contradict the `CreditPayoutEntry` model). Audit SHIP-WITH-FIXES → all resolved (list-shape HIGH · P2002-mis-attribution MED · Excel-formula-injection LOW); jest 52 + runtime-verified (valid xlsx, list contract, cross-tenant scope, gated routes). **Remaining → D1/P0.7:** enrollments-export MOCK + "Download Final Targets" re-ingest-header trap. |
| 45 | **Cleanup bucket — dead code, dup pages, dead links, small display bugs (P0.7)** | Dead `app/api/*` routes (incl. legacy `auth/send-otp`/`verify-otp` on the old `{mobile}` contract; `partner/targets`/`banners`; `admin/credits/payout-downloads` using wrong `amountInr`); duplicate orphan `/admin/outlets` + `/admin/approvals`; dead dropdown/footer links (now mostly wired in W0); dormant `proxy.ts` `ROLE_ROUTES['/sales']` non-enum role names; stale `clawbackAward` comment; in-memory invoice-number counter; **stale partner-class filter** ("Platinum/Gold/Silver") on admin/kyc (retired concept); display bugs — admin/approvals "Approved by — on Invalid Date", a "1 Issue" console error on sales/catalogue. **CONFIRMED live (2026-06-19 browser run of `/admin/dashboard`):** the **"NEEDS ATTENTION" strip is hardcoded** ("38 KYC SLA breaches / 23 Failed payouts / 147 Visibility pending" — real data is 2 KYC / 5 visibility); the **"Client / Gifsy" demo role-switcher** in the admin sidebar (decoupled from the real JWT); **partner-class filter chips** ("Gold/Silver/Platinum") + "Billing by partner class" on the admin dashboard (retired concept); Growth-Trends MoM% strip (demo, also #48). **MORE confirmed live (2026-06-20 WAVE-NOW UI/UX pass):** fabricated **Top Territories** (Mumbai West ₹12.4 Cr etc.) + **Scheme Activity** (3,716 enrolled) on the dashboard; the **partner-class "CLASS" column + Platinum/Gold/Silver filter on `/admin/kyc`** (retired concept); the cross-tenant KYC queue (GIFSY) shows multi-tenant rows but with **NO brand/tenant column** to disambiguate them at a glance (A1 polish); and a **React hydration mismatch in the partner Sidebar footer** (server `<div className="px-2 py-2 mb-1">` vs client `<button>` in the logout area — shared `partner/layout` Sidebar, NOT from B2; possibly extension-aggravated but smells like a genuine SSR/CSR conditional). | **P0.7 cleanup** (rides with the platform-retirement unit #31 where overlapping). | **◐ D1 DONE — admin demo-chrome (`d947d55`, 2026-06-20):** removed the NEEDS-ATTENTION strip, Growth-Trends, Top Territories, Scheme Activity, partner-class Billing + filter pills, the mock Target-Achievement card + filter bar, the "Client/Gifsy" role-switcher, and the `/admin/kyc` partner-class CLASS column/filter; **fixed the partner-Sidebar footer hydration mismatch** (gated the container on `collapsed`, not localStorage-derived `userName`). Kept the real KPI cards + Payout Summary (#47). Harness 59 passed. **REMAINING → D1.1/D2:** dead `app/api/*` routes (→ **D2**, overlaps retirement); the **admin header notifications dropdown** (fabricated "14 KYC pending…"); the **partner `DemoSwitcher` + `lib/partner-session` demo personas** (touches identity wiring — dedicated follow-up); the GIFSY cross-tenant KYC **brand column** (A1 polish); the small display bugs (Invalid Date, sales/catalogue "1 Issue"). |
| 46 | **Verification debt + the enforcement mechanism (2026-06-19)** | "Done" was repeatedly claimed on `tsc`/unit-tests/one-happy-path; comprehensive role-matrix runtime testing keeps finding gaps (#39/#40/#41). NOT yet runtime-verified under `VERIFICATION-PROTOCOL.md`: the remaining Wave-1A pages (partner leaderboard/rewards-orders, sales profile/visibility/kyc-new, gifsy outlet-types/users), the admin Gift-Catalogue page render, G escalate/internal-note/cross-tenant. **Root fix: docs are passive — build an automated E2E harness (role × page × data) that fails CI when a page fabricates or scoping breaks, runnable local + staging.** | **Build the E2E harness** (`DATA-VISIBILITY.md` spec → Playwright tests → CI) + `GO-LIVE-READINESS.md`; then close every cell. The harness *is* the enforcement; the comprehensive sweep runs through it. | ~~High~~ → **✅ RESOLVED (2026-06-19) + EXTENDED by C1 (`547fa03`, 2026-06-20):** Playwright harness `platform/e2e` (`npm run e2e`), now **59 passed** (was 36/40). C1 added coverage for the wave: B3 gifsy overview/detail (real tallies + no-MSG91-leak-vs-the-API), B2 admin/partner invoice render + the `{invoices,pagination}` list-shape regression guard, Q1 payouts→dashboard redirect, sales catalogue (A4 catalog-gate guard), sales-KYC-review scaffold, A1 rendered cross-tenant workspace. **C1 also caught two defects the harness surfaced because it had NOT been re-run after A4:** (a) a flaky OTP-box fill in `helpers/login.ts` (dropped a digit → intermittent auth-setup failure for any role; fixed: refill+assert-6 before submit); (b) A4's (correct) partner-denylist on tenant-wide `GET /visibility/submissions` had invalidated the visibility-write persistence test's partner-side read — re-pointed it at GIFSY_ADMIN (the cross-tenant operator that CAN list), guarantee preserved. Coverage still expanding (not every page/flow; A3 populated payouts + B2/​#53 populated paths await the seed enrichment). |
| 47 | **Configurable RBAC admin portal + first-admin provisioning chain (2026-06-19)** | Owner confirmed the model = two parties (Gifsy operator, Tenant) each with sub-roles whose permissions should eventually be **configurable in the admin portal** (heads define custom sub-roles). Today the RBAC engine exists (decorators + permissions table) but is OFF (`RBAC-ENABLEMENT.md`) and roles are a **fixed built-in enum**. Also undefined in code: the provisioning chain (seed-bootstrap a Gifsy super-admin → Gifsy creates tenant admins at client onboarding → tenant admin creates sub-users). | **Deferred, NOT blocking the harness/go-live.** Near-term: enable the fixed built-in roles + seed the role→permission map + the bootstrap super-admin. Later dedicated unit: the configurable role/permission management UI + invite/provisioning flow. Decisions captured in `DATA-VISIBILITY.md §3`. | **Med — OPEN (deferred; near-term = fixed roles + bootstrap)** |
| 48 | **Admin dashboard trend analytics still demo (2026-06-19)** | `#47` (task) made the `/admin/dashboard` **KPI card values** real (live `/admin/dashboard/kpis`, no mock base, `—` until loaded; dropped the Gifsy-only Fund card; omitted faked MoM/YoY badges). STILL demo on that page: the **MoM/YoY trend deltas** (need time-series we don't aggregate), the **Growth Strip**, **Scheme Activity**, **Billing Volume chart**, and the non-overlaid parts of the **Payout Summary**. Also the state/region/partner-class **filters no longer drive the KPIs** (real backend totals; filtered aggregates not implemented). None of these trip the harness fail-list, but they are not real. | **Analytics-realization unit (P8-ish):** add time-series/aggregation endpoints (MoM/YoY, scheme enrollment, billing) + filtered KPI params; then wire the lower dashboard sections + re-enable the trend badges with real data. | **Low/Med — OPEN (analytics, post-go-live)** |
| 50 | **Stale proxy-exclusion broke the redemption money path — FIXED (2026-06-19)** | The E2E redemption test caught that `POST /api/rewards/redeem` returned "Failed to initiate redemption" — partners could NOT redeem at all. Root cause: `next.config.ts` `beforeFiles` rewrite EXCLUDED `rewards/redeem` from the backend proxy (negative lookahead), routing it to a stale local `src/app/api/rewards/redeem/route.ts` that uses the **retired platform Prisma** (`@/lib/prisma`/`@/lib/auth`) → throws. But the backend HAD ported `/v1/rewards/redeem` in P5; the exclusion was just never updated. **FIX:** removed `rewards/redeem` from the exclusion + deleted the dead local redeem/confirm routes → now proxies to the backend. Verified end-to-end (redeem→confirm→**500 pts debited & persisted**, UI + API; harness 36/36). **✅ visibility/submit PORTED (2026-06-19):** `POST /v1/visibility/submit` added to the backend (`VisibilityController`/`Service` — multipart image → `StorageService` GCS upload, partner resolved from JWT, outlet resolved from partner, PHOTO_APPROVAL gate, partner-only `@Roles`); the dead local route deleted; **`admin/kyc` was a NO-OP exclusion** (the KYC writes already live at `/v1/kyc/*` — verify/bulk-verify/first-approve/approve/reject — and the FE calls `/api/kyc/*`, which never matched the lookahead). **Both exclusions dropped → `next.config.ts` now has ZERO exclusions** (`/api/:path*` forwards everything). Runtime-verified: boot-smoke (201 + persist + 404 bad-program + 403 sales) and a harness write-persistence test (real partner login → submit through the :3000 proxy → persists); harness **37/37**. Still (b) **sales `/catalogue` redemption** is mock (hardcoded OTP `999999`). **✅ B1 DONE (2026-06-19):** real sales-assisted redemption — `POST /v1/rewards/redeem-for-outlet` + `/redeem-for-outlet/confirm` (`@Roles(SALES)`), reusing the audited partner redeem/confirm core but targeting an ASSIGNED outlet: assignment-scoped (active `salesUserAssignment` by partnerId OR the partner's outletIds — the latter covers the production master-upload shape where partnerId is null), the OUTLET's wallet is debited, **OTP sent to + verified against the OUTLET** (owner consent decision), order against the outlet, sales user recorded for audit; `/sales/outlets` now returns `partnerId` + `balance`. FE `sales/catalogue` wired (real send/confirm + real balances; the `otp==='999999'` fake removed; `tsc` clean). jest 813/813 + independent money-path audit (**SHIP** after fixing the assignment-keying MEDIUM) + **runtime-verified API** (outlet wallet 46000→45500; non-assigned → 403; outletId-only assignment authorizes) **AND full UI click-through browser-verified 2026-06-20** (sales login → real catalog → select outlet "Test Kirana Store" real balance → confirm → OTP to outlet ****0002 → confirm → "Redemption Successful" → DB: wallet 45500→**45000**, order CONFIRMED, SALES_ASSISTED_REDEEM audit logged). **The UI pass caught 2 real bugs unit+API tests missed:** (a) A4 had over-gated `/rewards/catalog` to partner-only → sales catalogue 403/empty (FIXED: added SALES to the catalog read `@Roles`); (b) the FE mapped non-existent `available`/`brand`/`category` catalog fields → false "Out of stock" blocking redeem (FIXED: derive `available` from status+stock, cosmetic fields from GIFT_CATALOGUE/defaults). GIFT_CATALOGUE cosmetic (emoji/gradient) reliance remains → D1. | **✅ redemption + visibility-submit RESOLVED; sales-redemption ✅ B1; KYC cross-tenant → #38/A1** |
| 49 | **GIFSY operator console reads a static mock registry (2026-06-19)** | `#39` fixed GIFSY login (dev clientId override + prod subdomain), unblocking the `/gifsy/*` portal — but its pages (`/gifsy`, `/gifsy/clients`, `/gifsy/clients/[slug]`) read a hardcoded `lib/platform/client-registry.ts` `CLIENT_REGISTRY`, NOT the real `clients` table. So "GIFSY sees BOTH tenants" (deoleo + clientb) can't be proven against real data, and the operator can't see real onboarded tenants. (Harness covers gifsy login + a console smoke; the real-data cross-tenant assertion is deferred.) | **Gifsy-portal real-data unit:** add a backend `GET /v1/gifsy/clients` (list real tenants, GIFSY-only) + the per-client summary aggregates; wire the gifsy pages to it; retire `CLIENT_REGISTRY`. Then add the harness "GIFSY sees both tenants" assertion. | **✅ B3 DONE (`745d573`, 2026-06-20):** the gifsy **Clients list** (`1a526c8`) + the **Overview dashboard** + **per-client detail** all read the real `clients` table now. Added `GET /v1/gifsy/overview` (real status tallies + per-client cards) and `GET /v1/gifsy/clients/:slug` (full detail projected from the Client JSON blobs), both GIFSY_ADMIN-only; rewired both pages off `CLIENT_REGISTRY`; the fabricated "N partner classes" metric (column dropped in S2) replaced with a real modules-on count. Audit SHIP (secret-leak/authZ/route-order/empty-blob/fabrication all clean) + runtime-verified (overview real tallies 2/1/1/0, detail carries **no** secret, non-gifsy → 403). `CLIENT_REGISTRY` itself stays (still used by `clients/new` + shared libs → retires in **D2**). The admin-dashboard demo-chrome is a separate page → **D1/#45**. |
| 52 | **`payouts/fund/receive` 400s — `ReceiveFundDto.paymentDate` not whitelisted (found during A3 runtime, 2026-06-19)** | `ReceiveFundDto.paymentDate` (`api/src/payouts/dto/payouts.dto.ts`) carries only `@Type(() => Date)` and **no class-validator decorator**, so under the global `forbidNonWhitelisted` ValidationPipe a real `POST /v1/payouts/fund/receive` is rejected with **400 "property paymentDate should not exist"** — the offline fund-receipt cannot be recorded via the API. Pre-existing (NOT introduced by A3; A3 did not touch this DTO/method). Found because the A3 runtime happy-path needed a fund balance (worked around by seeding a `FundLedger` row directly). | Add `@IsDate()` (or `@IsDateString()`) to `paymentDate` so it is whitelisted; re-verify `fund/receive` records a receipt + ledger entry at runtime. Small — fold into **A4** (enforcement/DTO sweep) or **B2** (payouts/invoices money-path FE). Money path — but read-side correct; this only blocks the receive write. | ~~Med~~ → **✅ RESOLVED (A4, 2026-06-19):** added `@IsDate()` to `paymentDate` (with `@Type(() => Date)`; ValidationPipe `transform:true` converts the ISO string then validates) — runtime-verified `fund/receive` now returns 201. |
| 53 | **`schemes.submitEnrollment` SALES mode keys on `salesUserAssignment.partnerId` only (found during B1 audit, 2026-06-19)** | The sales-assisted ENROLLMENT path (`schemes.service.ts:282-288`) authorizes via `salesUserAssignment.findFirst({ salesUserId, partnerId, unassignedAt: null })`. But the production master outlet-upload assigns reps by **`outletId` only** (`admin-outlets.service.ts:~306`; the partner attaches later at KYC and `partnerId` is never backfilled on the assignment). So for a real rep, sales-on-behalf enrollment fails closed (403) — the SAME class B1 fixed for redemption (#50-E). Tests/E2E miss it because the seed sets both ids. Fail-closed = safe direction, so it's a functional gap, not a vuln. | Apply the same fix B1 used in `rewards.requireAssignedPartner`: authorize by `partnerId` OR the partner's `outletId`s (`outlet.findMany({ partnerId }) → OR: [{ partnerId }, { outletId: { in } }]`). Small. → fold into **B-wave** or a dedicated follow-up. (Better long-term: backfill `salesUserAssignment.partnerId` at KYC approval, fixing both call-sites at the source.) | **✅ DONE (`72a77f1`, 2026-06-20):** mirrors `rewards.requireAssignedPartner` exactly — authorize by `partnerId` OR the target partner's `outletId`s, with the empty-list spread guard; tenant-verified target, active+salesUserId scoping, SELF path all unchanged. Added a positive test (outletId-only authorizes) + a negative over-auth guard (foreign-outlet rep refused; outletId set pinned to the target's outlets). Independent audit SHIP. **Runtime-verified** (partnerId nulled → assigned partner gets past the assignment check, unassigned → 403). Long-term partnerId-backfill-at-KYC still a nice-to-have. |
| 51 | **No operator-context: Gifsy can't get a tenant-scoped session for per-brand operations (2026-06-19)** | Gifsy operates in two modes (`DATA-VISIBILITY §3.1`): cross-tenant **oversight** (see-all; A1, done) and per-brand **operation** (payouts). Operation needs a **tenant-scoped `GIFSY_ADMIN` session**, but `auth.service.verifyOtp` finds the user by `{ phone, clientId }` and binds the JWT `clientId` to the **user record's** tenant (`:106`,`:149`). The Gifsy operator lives under `clientId='gifsy'`, so they CANNOT authenticate into a tenant (`'deoleo'`) → "No account found". Without this, payouts (A3) can't run (every payout query is `clientId: user.clientId`, and the fund/batch are per-tenant). | **A2 — operator-context switcher:** an in-console "Work in brand ▾" mints an **assume-tenant token** `{sub:operator, role:GIFSY_ADMIN, clientId:tenant}` (GIFSY-only, audited, scoped, "Working in <Brand>" banner; `sub` stays the real operator for attribution). Industry pattern (AWS AssumeRole / Salesforce Login-As / Stripe switch). Prerequisite for **A3** payouts. The partner/sales self-lookup edge (a GIFSY token hitting `/partner/me` → null) is closed by **A4** role-gating. | **◐ A2 BACKEND DONE (`edb8b39`, 2026-06-19):** `POST /v1/auth/assume-tenant` mints the scoped token (GIFSY-only, target-ACTIVE-only, sub-preserved, audited, 8h, `assumed` flag); refresh preserves the assumed scope; jwt-strategy matches session by userId+clientId. Runtime-verified (6 checks) + api jest 790/790 + independent audit (PASS after 2 fixes). **✅ A2-FE DONE (`1a526c8`, 2026-06-19):** `<BrandSwitcher/>` (real `GET /api/gifsy/clients`) + `assumeTenant/exitTenant` in auth-client + global `<OperatorBanner/>` ("Working in &lt;Brand&gt; · Exit") in the gifsy+admin shells + `PORTAL_ROLES.admin` admits GIFSY_ADMIN. Operator-switch round-trip runtime-verified through the real UI (new harness test; 40/40) **AND manually browser-verified 2026-06-19** (login as Gifsy → switcher shows real brands → assume Deoleo → amber "Working in Deoleo India Pvt. Ltd." banner + admin shell in deoleo scope with real KPIs (2 partners/2 KYC) → Exit restores `clientId=gifsy`). **A2 COMPLETE — payouts (A3) unblocked.** | **✅ RESOLVED (A2 backend+FE; browser-verified)** |

| 54 | **No automated DB migration → staging was never provisioned (2026-06-20)** | The deploy pipeline had **no migration step** (master-plan 9.3/9.5 said so explicitly); every DB had been migrated **by hand**, and the migration model was a **hybrid** (6 formal Prisma migrations frozen at June-6/P3 + ~10 hand-written `migrations-manual/*.sql` for P4–P6). Result: `gifsy_staging` had **no tables** → the staging OTP/login outage; staging was simply skipped by the manual process. The shared instance is **private-IP**, so migrations can't run from a laptop or a GitHub Actions runner. | **✅ STAGING RESOLVED (2026-06-20):** Prisma history **squashed to one clean baseline** (`api/prisma/migrations/00000000000000_baseline/` = the full 72-table/51-enum schema; the 6 old migrations → `migrations-archive/`; `migrations-manual/` is now LEGACY/source-record only). Migrations apply via `prisma migrate deploy` run as an **in-VPC Cloud Run Job**. **Staging auto-migrates** on every `develop` push (`gifsy-migrate-staging` wired into `deploy-staging.yml`, before the new revision serves; a failed migration fails the deploy). **◐ PROD PENDING** the gated cutover (`gifsy-migrate` job + a one-time P3005 baseline reconcile — `runbooks/PROD-DB-MIGRATION.md` Step 1.5). Canonical model: `../plans/MIGRATIONS.md`. | ~~High~~ → **✅ staging RESOLVED (CI-wired); ◐ prod pending cutover** |
| 56 | **E2E money/auth write coverage was missing — and the first build tested the WRONG money rail (E2E Wave 1, 2026-06-21)** | The pre-Wave-1 harness had no write-persistence coverage for the launch-critical money/auth paths. Wave 1 added them, but the independent audit caught that **W9 was built against `credits.confirmBatch` (points rail, zero-row batch)** while the #42 transactional double-process guard lives on **`payouts.processBatch` (the INR bank-disbursement rail)** — so the real INR double-disbursement guard had ZERO E2E coverage. Also: **W5 KYC stage-1 (sales first-approve) was permanently SKIPPED** (no `PENDING_SO_APPROVAL` seeded), the clientb cross-tenant specs hit `/api/admin/*` (all 404) so never asserted, and a `login.ts` OTP-fill race dropped a digit (salesManager couldn't authenticate). | **✅ RESOLVED (2026-06-21):** added `gifsy/payout-process-write.e2e.ts` — a GIFSY **assume-tenant** spec processing a seeded DRAFT `PayoutBatch` → SUBMITTED+INITIATED (persists), re-process → flags 0 (no double-disbursement), COMPLETED → 400 (claim guard); seeded the payout pipeline (FundLedger + `seed-pb-1`/`seed-pb-2` + PENDING txn, seed §3.10). Seeded `seed-kyc-4` (PENDING_SO_APPROVAL) so sales first-approve RUNS. Rewrote clientb cross-tenant to the real list endpoints (isolation confirmed live). Hardened `login.ts` OTP entry (keystroke). Full suite **83/83 green, 0 skipped**. Money/auth/irreversible + negative-matrix slice covered; read-coverage Waves 2–4 still open. | ~~High~~ → **✅ RESOLVED (Wave-1 slice; Waves 2–4 open)** |
| 57 | **Admin read-only pages render MOCK/empty data — real #40/data-wiring gaps (found during E2E Waves 2-4, 2026-06-21)** | The role×page E2E expansion caught several read-only admin surfaces never wired to real data: (a) `/admin/dashboards/{payments,engagement,redemptions}` render **hardcoded demo values** ("4,821" active-partners, "Kumar General Store") — P0.6-H/#47 wired only the main `/admin/dashboard`, not the sub-dashboards; (b) `/admin/outlets` **Outlet Master** is a mock constant with demo rows; (c) `/admin/hierarchy` is **empty for the tenant** — `GET /api/admin/hierarchy-config` returns `employees: []` despite seeded sales users (EMP001 reports to EMPASM1); (d) GIFSY cannot write a setting into a **tenant's** settings namespace (PUT scopes by `user.clientId='gifsy'`; no `?forClient=`); (e) the **sales shell** renders hardcoded demo notifications/role-names ("Kumar General Store"/"Rajesh Kumar"); (f) **MIS_USER is denied KPI read** (403 on `GET /api/admin/kpis`) although `/admin/targets` is in its read column (RBAC gap). Real product/#40 gaps — admin/sales-facing, so **a UAT tester hits them as visible "fake/empty/unfinished" defects** even though they're outside the core money/auth/scope loop. | **🔴 PRE-UAT BLOCKER (owner re-prioritised 2026-06-21 — NOT a post-launch fast-follow):** the admin-visible subset MUST be wired to real data BEFORE UAT: **(a) sub-dashboards → real aggregations, (b) Outlet Master → real outlets list, (c) `hierarchy-config` → the tenant's sales users, (e) retire the sales-shell demo notifications.** Lower priority (roles NOT in Deoleo's first UAT — can trail): (d) tenant-settings write path, (f) MIS KPI read (RBAC). The blocking E2E tests are `test.fixme(...)` + cite this gap → re-enabling is trivial once wired (they then re-assert real data). Core money/auth/scope/write paths ARE covered (E2E Waves 0–1). | ~~Medium~~ → **(b)(c)(e) ✅ RESOLVED (2026-06-21, task #77 — 2 independent audits + E2E 290/0/9): (b) orphan `/admin/outlets` mock removed → redirect to the already-real `/admin/users/outlets`, + real per-outlet KYC-status join (was hardcoded `NOT_STARTED`; derived from the owning-partner's latest `KycSubmission` since KYC is partner-keyed, not outlet-keyed); (c) `getHierarchyConfig` stays snapshot-fed per P2.1's deliberate design — the empty page was a SEED-FIXTURE gap, so the seed now writes the `employee_hierarchy` snapshot (no new relational read path); (e) notification bells hidden in both shells until P7 (#21). (a) sub-dashboards = DEFERRED post-launch (owner 2026-06-21, "do later") — ⚠️ still render mock ("4,821"/"Kumar General Store"); hide-the-nav-or-wire-them is an open pre-UAT call. (d) tenant-settings write + (f) MIS KPI read = lower, still open.** |
| 58 | **A new tenant has NO outlet types enabled → outlet creation is impossible until manually provisioned (found during E2E Wave 2, 2026-06-21)** | `AdminOutletsService.upsert()` maps an outlet's type CODE → id via the **per-tenant** `OutletTypeClientConfig` table (which outlet types a tenant has enabled). A freshly-provisioned tenant has **ZERO** such rows, so every outlet create/upload fails "Unknown outlet type". There is **no auto-creation on tenant provisioning** — the only path is the manual GIFSY operator endpoint (`gifsy.service.ts` `updateOutletTypeConfig`, one type at a time). Band-aided via `seed.ts §3.2b` for the demo tenant (dev+staging only). | **🔴 PRE-PROD PREREQUISITE (task #78):** make tenant provisioning (`gifsy.service` create-client + the `/gifsy/clients/new` flow) **auto-create `OutletTypeClientConfig` rows for the default outlet types (enabled)**. Required before any real tenant (incl. prod Deoleo #76) can have outlets. **NOT a staging-UAT blocker** (seed covers the demo tenant) but **IS** a go-live/data-load prerequisite. Verify: a fresh tenant's admin can create an outlet with no manual operator step. | ✅ **RESOLVED (2026-06-21, task #78 — audit SHIP + runtime-verified):** `GifsyService.createClient` + a `provisionOutletTypeConfigs(tx, clientId)` chokepoint auto-create one enabled `OutletTypeClientConfig` per active `OutletType` inside the client-create `$transaction`; exposed as `POST /v1/gifsy/clients` (GIFSY_ADMIN, `tenancy:write`) + the `/gifsy/clients/new` wizard wired to it; `seed.ts` routed through the SAME helper (retired the §3.2b band-aid). Runtime: fresh tenant → 5 enabled configs; dup slug → 409 (race-safe via P2002 catch). **Unblocks #76.** |
| 55 | **Admin shell showed a demo persona for EVERY real admin (found during E2E-Wave-0 audit, 2026-06-21)** | The admin analog of #54-FE/#40 (which was only fixed for the partner/sales shells): `lib/admin-session.ts` `getAdminSession()` fell back to `DEMO_SESSIONS['CLIENT_ADMIN']` ("Rahul Agarwal") whenever `admin_role_demo` was absent — which is **always** for a real login (the layout removes that key on mount). Result: every real CLIENT_ADMIN/MIS_USER saw **"Rahul Agarwal / Client Admin"** in the sidebar (MIS doubly wrong — wrong name AND wrong role label); the SSR pass + the `useAdminSession` initial state also emitted the demo name into the server HTML + first paint. Prod-facing fabricated identity; the harness missed it because the token wasn't in the #40 fail-list. | **✅ RESOLVED (2026-06-21):** `getAdminSession()` now reads the REAL JWT user (`getStoredUser()`) first → real name + role; added `adminRoleLabel()` (gives MIS "MIS User"); admin header uses it for both labels + computes avatar initials from the real name; a neutral no-name `LOADING_SESSION` replaces the demo session in the SSR branch + the hook's initial state (no flash, no hydration mismatch). Pinned: `'Rahul Agarwal'` added to the `e2e/fixtures/fabricated.ts` #40 fail-list; 5 new unit tests cover the real-user path. tsc + vitest (30) green; runtime-verified for clientAdmin + mis (real identity renders, no demo). | ~~Medium~~ → **✅ RESOLVED** |

**Severity tally:** **~33 resolved.** Earlier (pre-2026-06-19): #1,#3,#4,#11,#20,#22,#30, **#31 via Phase S**, **#9/#12/#13/#14/#15 via P3**, **#6/#10/#29 via P4**, **#28 via P5**, **#5/#7/#8/#16/#17/#19/#25 via P6**; **P0.5 runtime: #33 login, #35 catalogue-500, #37 seed**. **Resolved 2026-06-19 (P0.5/P0.6 remediation, ALL harness-verified — `platform/e2e` 36/36):** **#39** GIFSY login · **#40** fabricated FE data · **#41** role/portal scoping + Q1 payouts · **#46** the E2E harness (enforcement) · **#47** admin dashboard real KPIs · **#50** the partner REDEMPTION money path (was fully broken — stale proxy-exclusion) **+ the visibility/submit port (2026-06-19)**. · **#34** auth-attachment (subsumed by #40/#41). · **#36 ◐ PARTIAL** (dashboards+redemption+**visibility-submit** done; KYC writes already at `/v1/kyc/*` → cross-tenant runtime-verify #38; invoice-gen + sales-redemption still dead). · **OPEN — re-scoped into P0.6 Phases A–D (`00-MASTER-PLAN §P0.6` + `reconcile/P0.5-make-it-runnable.md`):** **A1** = **#38** Gifsy cross-tenant oversight (a real **bug** — KYC + visibility approve/reject all `clientId: caller`-scope → GIFSY 404; `VERIFICATION-PROTOCOL §72`) **✅ DONE** · **A2** = **#51** operator-context switcher (tenant-scoped GIFSY session; payouts prerequisite) · **A3** = **#42/#43** payouts completion **✅ DONE (2026-06-19)** (batch-from-pending sweep + `$transaction`+guarded-claim + **inline TDS REMOVED** — full-amount payout, TDS owned by P6.5 engine; runs in the A2 tenant session — money path; jest 795/795 + audit SHIP + runtime-verified) · **A4** = **#2** enforcement coverage audit **✅ DONE (2026-06-19)** (audited all 35 controllers; gated the partner/sales self-service surface; **fixed a real kyc.ledger PII read-leak** + visibility.listSubmissions denylist; **#52** fund-receive DTO + slaMetrics cross-tenant resolved; JWT↔x-tenant-slug deferred to #23/P8.6; jest 803/803 + audit SHIP + runtime 12/12) · **B1** = **#50-E** sales-assisted redemption → real backend **✅ DONE (2026-06-19;** redeem-for-outlet, OTP-to-outlet, assignment-scoped by partnerId|outletId, jest 813/813 + audit SHIP + runtime-verified API + **full UI click-through browser-verified**; the UI pass caught+fixed 2 bugs: A4 catalog over-gate + FE catalog-field mismatch) · **B2** = **#44** invoices + Excel round-trips **✅ invoice slice DONE (2026-06-20)** · **B3** = **#49** gifsy console real `clients` **✅ DONE (2026-06-20)** · **C2** = staging harness env-support **✅ DONE (2026-06-20)** · **D1** = **#45** cleanup (P0.7) · **D2** = **#31/#32** platform-Prisma retirement + auth/logout. **▶ WAVE-NOW DONE 2026-06-20 (4 parallel agents, each audited + runtime-verified, committed to `develop` — NOT yet pushed): B2 `e07c06a` · B3 `745d573` · C2 `4e08477` · #53 `72a77f1`.** Remaining open: **#53-longterm** (partnerId backfill), **C1** (harness coverage for A/B fixes), **D1/D2** + the C2 **staging OTP source** — **owner decision 2026-06-20: use `FIXED_OTP` on the staging backend for now (interim)**; the real-OTP read-back endpoint (test-only, non-prod-only, secret-guarded) is **DEFERRED → P8-ops / staging-hardening** (template/endpoint not yet built; run staging with `E2E_OTP_STRATEGY=fixed` until then). See `00-MASTER-PLAN §P8` + `e2e/README.md`. **Note: the payouts gaps are a documented P6 hold (6.5 ON HOLD) + a Q1 consequence, NOT P6 build errors.** **Deferred:** **#48** trend-analytics → P8. · **OPEN (earlier, non-runtime):** **#2** (RBAC route-enforcement), **#18-residual** (JSON blobs), **#21** (notification worker → **P7**), **#23/#24/#26/#27** (→ **P8**), **#47-RBAC** (configurable roles + provisioning). · **#32** (`auth/logout` revocation sliver → platform-retirement). **Plus: STAGING harness env-support TODO. The `next.config.ts` proxy-exclusion list is now EMPTY — no remaining dead writes via that mechanism (harness 37/37).**

### Also noted (to formalize)
- Gifsy's own post-first-approval KYC steps (penny-drop, agreement, final validation) — to be
  documented in §02 Workflows.
- "Inactive manager" signal is currently *blank phone*; prefer an explicit `SalesUser.isActive`/status flag.
- ~~Stale `ROLES` constant in `auth.ts`~~ → **removed in 0.4b** (was dead + inconsistent with the
  `UserRole` enum).
- `clientId` is **not** in the JWT (`TokenPayload`); tenant is resolved from the
  `x-tenant-slug` header. A valid token on the wrong subdomain could mismatch tenant scope
  — verify the proxy/middleware binds them. (Tenant-isolation review, Phase 2.)
