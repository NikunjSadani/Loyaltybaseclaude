# Go-Live Issue List — authoritative master tracker (updated 2026-06-22)

> **STATUS: FIX WAVE COMPLETE + pushed. NOW = OWNER-DRIVEN UAT on staging — fix-as-you-find (see the UAT-FOUND block below).**
> Originally NOT go-live ready (6 blockers + 4 majors). The GO-LIVE FIX WAVE (2026-06-21→22) closed them via disjoint streams
> (GLM · GL-Money · GL-RBAC · GL-FE-enroll · GL-FE-settle), each executor → INDEPENDENT adversarial audit → Opus gate →
> runtime-verify. The money re-audit caught a real BLOCKER the first pass missed (GLM-2 never implemented = lost awards) plus
> a payouts-rail resolver gap — both then fixed and re-verified. See the FIX WAVE RESULTS block below.

## 🟢 2026-06-26 SESSION — RD/NAV/STATE/GST/PREFILL/TEAM (UAT batch: voucher visibility, XSR redeem nav, KYC form, team list) (**pushed `9bdab06`+`96e529b`, deployed to staging, audited SHIP**. Gate: api jest **1136** · FE vitest **1561** · tsc 0 both sides. **Staging runtime-verified (API):** RD = partner catalog serves Amazon Voucher w/ mode=GIFT_CARD+category; TEAM = /sales/team returns employeeCode+mobile (Anil Sharma XSR-M001·9900000041); PREFILL = existingKyc.accountHolderName served + address/pincode populated for real-submit outlets. NAV/STATE/GST = gate+audit-verified, FE UI by owner. **+ TEAM-AGG** aggregated My-Team tiles (pushed `3956a29`→`eecdbac`; independent audit caught+fixed an `outlet.isActive` filter BLOCKER; api jest **1138**; runtime-verify pending deploy). **+ KYC-NOTFOUND diagnosed** (assignment-vs-submitter scope divergence) — awaiting owner decision.)
| # | Found | Fix | Status |
|---|---|---|---|
| **RD** (A) 🔴 | An ACTIVE GIFT_CARD reward ("Amazon Voucher", category "Gift Vouchers") configured by the Gifsy admin did NOT appear in the partner's redemption catalogue "Vouchers" tab ("No items found"). | Two layered bugs: the partner FE bucketed tabs by `category === 'Vouchers'` but (1) the partner `listCatalog` API never returned `category` (no Prisma include) → always undefined, and (2) the reward's category was "Gift Vouchers" not "Vouchers". The admin classifies vouchers by **`redemptionMode === 'GIFT_CARD'`**. **Fix:** FE `partner/rewards/page.tsx` now buckets by `redemptionMode` (GIFT_CARD→Vouchers, PHYSICAL_GIFT→physical; BANK_TRANSFER/UPI cash tab untouched), matching the admin; backend `listCatalog` adds `include:{category:{select:{name:true}}}` + surfaces the flat label. Affordability still SHOWS unaffordable cards (disabled), channel-gate still enforced at redeem time. Audit: redemptionMode is non-nullable (no null-mode coverage loss); tenant-scoped; no money-path bypass. | ✅ done (awaiting push) |
| **NAV** (B) | An XSR had **no way to navigate** to "redeem on behalf of an outlet" — the only entry point was a buried Quick-Action on the KYC detail page, and it was **hidden for every outlet** (incl. wholesalers) because `kyc.outletType` was never populated, so with the default `redeemGiftWholesalerOnly:true` the `showRedeem` gate always collapsed to false. | **Owner decision:** surface redeem ONLY on the outlet-detail view (reached by tapping an outlet = the KYC detail page), prominently — not on the list, not a nav tab. **Fix:** backend KYC-detail outlet select now includes `outletType.code`; FE reads it; added a prominent full-width "Redeem for Outlet" button (gate corrected: wholesaler→show, restriction-off→show, non-wholesaler+restriction-on→hide), de-duped the old Quick-Action. Real redeem authz stays backend-enforced (`requireAssignedPartner`: caller-is-salesUser + same tenant + active assignment). | ✅ done (awaiting push) |
| **STATE** (C) | KYC form "State" was a free-text input. | Owner: make it a **searchable dropdown** of all 28 states + 8 UTs. New `lib/indian-states.ts` + reusable `components/ui/searchable-select.tsx` (type-to-filter combobox), wired into `sales/kyc/new` + `components/kyc/kyc-form`. Preserves prefilled value (legacy free-text values kept verbatim on submit). | ✅ done (awaiting push) |
| **GST** (D) | KYC GST field was WRONG — capped at **12** chars ("12 more needed"), no format check; a GSTIN is **15**. | New `lib/gstin.ts` (regex + `isValidGstin`/`panFromGstin`). FE: 15-char cap, live valid/invalid feedback, PAN auto-extract fixed, helper text → 15. Backend DTO `@ValidateIf`+`@Matches` GSTIN (validates ONLY a non-empty value — GST stays optional, blank/undefined/whitespace pass). **+ Audit-found fix:** `kyc.service.ts` normalised a blank GST to NULL (was writing `''`, which collided on `@@unique([clientId,gstNumber])` → a 2nd no-GST outlet got a spurious "GST collision" 400). Regression test added. | ✅ done (awaiting push) |
| **TEAM** | The sales "My Team" member list showed each member's internal **CUID** (`cmqp6qtu…`) below their name instead of useful identity. | Owner: show **employee ID + phone** there. Backend `getTeam` now selects `user.phone` + returns `mobile` per member (already returned `employeeCode`); FE renders `{employeeCode · mobile}` (falls back to territory, never the raw id). Consistent with the member-detail page which already shows `employeeId`/`mobile`. Scoped to the caller's own direct subordinates (no PII over-exposure). Test added. | ✅ done (pushing) |
| **TEAM-AGG** | The "My Team" summary tiles (Outlets / KYC Done / KYC Pending / Avg Target) showed **0** for every SO-and-above manager despite a real downline — should be an aggregated team view. | The FE tiles sum the member rows, but `getTeam` returned members WITHOUT outlet/KYC/target counts → all defaulted to 0. **Fix:** `getTeam` now enriches each direct member with their **whole-subtree rollup** (`outlets`, `kycDone`, `kycPending`, `targetValue`, `targetPct`) computed in-memory (4 queries, no N+1), reusing the leaderboard's subtree primary-KPI logic; kycDone/kycPending mirror getMember; tenant-scoped. No FE change needed. **Independent audit caught a BLOCKER (`3956a29`→`eecdbac`):** the rollup filtered `outlet.isActive:true`, but outlets are created `isActive:false` (PENDING) until KYC approval — so it under-counted Outlets vs the /sales/outlets list AND zeroed out "KYC Pending" (defeating the fix's purpose). Fixed: aligned to buildOutlets' assignment-only scoping + added a regression guard. Gate api jest **1138**. | ✅ done (pushed `eecdbac`; runtime-verify pending deploy) |
| **KYC-NOTFOUND** | Clicking a reactivated/reassigned outlet ("abc"/OUT-2026-002111 — which HAS an APPROVED KYC, NOT a never-KYC'd seed) → "KYC not found". | **Root cause (real bug):** the outlets/targets LIST authorizes by the outlet's CURRENT assignment (`buildOutlets`), but the KYC-detail endpoint (`assertCanViewSubmission`) authorizes by who **submitted** the KYC. After a reassignment these diverge → outlet visible in the list but the detail 404s. Real-world: re-tagging an outlet to a new rep makes the new rep's manager see it but unable to open its KYC. **Proposed fix:** align KYC-detail view-scope to the outlet's current assignment (preserve "unrelated SO can't view") + FE guard for empty `kycId` → route to Start-KYC instead of dead "/sales/kyc/". The specific "abc" outlet is throwaway test data (assigned to `XSR-M00100` from inject/K29 testing). **Owner chose (a).** **BUILT (`2d5ab8c`):** `assertCanViewSubmission` now also allows a sales caller when the submission's outlet is **actively assigned into their subtree** (READ-only — getOne+ledger; approvals stay submitter-chain-only); tenant-scoped, fail-closed, unrelated-SO/partner still denied. **Independent security audit: SHIP** (read-only, no cross-tenant leak, PII masking unchanged, aligns exactly with the list). FE: empty-`kycId` outlet cards now route to `/sales/kyc/new?outletId=…` (Start KYC) instead of dead "/sales/kyc/". Gate api jest 1143 · FE vitest 1567 · tsc 0. | ✅ done (pushed `2d5ab8c`) |
| **ADMIN-KYC** (B/D/E) | Gifsy "KYC Submissions" page: (B) **Pending card 6 vs 5 visible rows**; (E) remove **SLA Breached** card; (D) **signature image invisible** in Document Review. | (B) The card counted `PENDING+UNDER_REVIEW` but the list filter matched only exact `PENDING`, and the page loaded just the first 20 (default limit) → mismatch. Shared `isPending()` predicate for card+filter + fetch `?limit=500` (no truncation). (E) Removed the SLA-Breached card (per-row SLA badge/age kept). (D) Signatures are **transparent-bg PNGs** (near-black strokes) shown on a `bg-gray-900` panel → invisible. Preview + zoom now render signatures on white; new captures composite an opaque white bg (offscreen canvas) so they're self-visible. Pushed `a6b6265`. | ✅ done (pushed) |
| **REJECTED-EXPORT** (C) | Owner: the Rejected-view "Export Excel" should report rejected outlets with all fields + **which fields the admin rejected**. | New `GET /v1/kyc/rejected-export` (GIFSY_ADMIN) → one row per REJECTED submission: identity (Outlet ID/Name/Owner/Mobile/Rep/Submitted/Rejected date/Rejected-by) · overall rejection reason · **"Rejected Fields" summary** · per-field **Verdict+Remark** for all 7 KYC fields (reuses `dumpFieldStates`) · KYC values (GST/PAN/address/bank/UPI) · SLA age. Tenant-scoped like the list (`kycTenantFilter`); every cell `cellSafe`'d (AF-5). FE Export button downloads this on the Rejected filter, else keeps the PENDING_GIFSY review-dump. Gate api jest **1151** · FE vitest 1567 · tsc 0. Pushed `e036625`. | ✅ done (pushed; runtime-verify on deploy) |
| **OUTLET-ERR-REPORT** | Owner: the error report from the Outlet Management upload only had Row/Outlet ID/Status — "very difficult to trace where and what the error was". Wants **the uploaded file + a Remarks column** noting each row's error. | The shared `ValidationPanel` built the report from only `result.rows` (rowNum/outletId/status/errors), discarding every data column. Added 3 pure builders in `outlet-upload.ts` (`buildOutletUploadErrorReport`/`buildReKYCErrorReport`/`buildDeactivateErrorReport`) that join validation results to the parsed rows by `rowNum` and reproduce the **full file** (every original column + a leading `Row` locator + ALL rows, OK rows blank) with a per-row **Remarks** annotation column (Re-KYC uses **"Validation Errors"** to not clobber its admin-authored Remarks column). Threaded an `errorReport` prop through `UploadSection`→`ValidationPanel`→`DownloadErrorReportButton`. Re-uploadable; every cell still `cellSafe`'d via `aoaToSheetSafe` (AF-5). Applies to all 3 tabs. Unit + **end-to-end xlsx round-trip** tests (`outlet-upload-error-report.test.ts`). Gate FE vitest **1576** · tsc 0. | ✅ built + gated + audited (awaiting push) |
| **TASKS-PARITY** | Sales dashboard shows a **"KYC to be done"** group but the Tasks "View all" page omits it → counts disagree (dashboard 3 vs Tasks badge 1). | The dashboard builds a `kyc_to_do` group from `NOT_STARTED` outlets; the Tasks page's field-role block was never updated to mirror it, so those outlets silently vanished on "View all". Added the identical group (id `kyc_to_do`, label "KYC to be done", emerald, `href=/sales/kyc?status=NOT_STARTED`) as the first field-role group in `tasks/page.tsx`, items deep-link to the start wizard (`/sales/kyc/new?outletId=<id>`). Now the Tasks badge equals the dashboard. Regression test `tasks/__tests__/kyc-to-be-done-parity.test.tsx`. Gate FE vitest **1568** · tsc 0. | ✅ built + gated (awaiting push) |
| **PREFILL** (E) | On re-KYC (rejected→resubmit), Account Holder Name, Street Address & Pincode came back BLANK. | **Account Holder Name** was a genuine bug — dropped at all 4 layers (backend `select`/`existingKyc`, FE interface/prefill). Fixed: `sales.service.ts` selects `bankAccountHolder` + sets `existingKyc.accountHolderName`; FE prefills it. **Street Address/Pincode**: the FE/BE mapping is already correct — they're sourced from `outlet.addressLine1`/`pincode`, which the submit path writes UNCONDITIONALLY (`kyc.service.ts:902-911`). So for any real submit→reject→re-entry they DO prefill; the screenshot's blank is a **seed/UAT outlet** whose city/state came from the outlet-master upload but which never had a real KYC submit writing street/pincode. **To runtime-verify on staging** (real submit→reject→re-enter). NOTE: `KycSubmission` does not snapshot these fields, so a truly bulletproof "always shows last-submitted" would need a schema change — deferred unless the owner wants it. | ✅ accountHolderName done; address/pincode verify-at-runtime |


| # | Found | Fix | Status |
|---|---|---|---|
| **LB** | The sales-leaderboard FE page existed but its backend was missing (404) and it fetched the wrong (partner) board. | New `GET /v1/sales/leaderboard?scope=&period=` ranks the caller against **same-level peers** (same `hierarchyLevelId`); each candidate scored by their **own team (subtree self+downline) PRIMARY-KPI achievement %** (round(100·ach/target), 0 when target=0). Scope widens the net: **rm** = peers under the caller's reporting manager (same `reportingToId`); **state** = same level + region; **national** = same level tenant-wide. Returns `{name,territory,achievementPct,activeOutlets,change(vs last month),isMe}` sorted best-first (tie-break activeOutlets desc, name asc). Reuses the getTargets join + `descendantSalesUserIds`; tenant-scoped; loads per-tenant data once + rolls subtrees in memory. FE page pointed at the real endpoint. **Owner decision (design):** option1+3 — everyone ranked, by team total, but only vs same-level peers; rm = same-reporting-manager peers. **Independent audit: SHIP** (no cross-tenant leak / clientId-trust / div-by-zero; RBAC safe) — applied its 2 hardening fixes (sanitise `period` to YYYY-MM; `orderBy` the KpiDef pick for a deterministic primary). **Runtime-verified (`1bc9315`):** ISR login → rm=2 peers, state/national=484 same-level peers; real **47%** from 3 outlets' actual target/achievement; tie-break + isMe + change=0 (no prior month) all correct. **Follow-up (`a272dca`, owner decision): `territory` = the rep's nearest ZNM (Zonal Manager) ancestor name** — walk up the reporting chain to the `hierarchyLevel.code==='ZNM'` node; falls back to the rep's own region/zone (SalesUser has no zone of its own). Runtime-verified: 484/484 entries now show the ZNM name. Pushed `a525739`+`a272dca`. | ✅ done |
| **PC** | The allowed Outlet "Program Name"/"Program Category" values were HARDCODED FE constants (`VALID_PROGRAMS`/`VALID_CATEGORIES`) — not owner-editable, and a go-live risk if Deoleo's real programs differ. | Made both lists **per-tenant editable**: `outletPrograms[]`/`outletCategories[]` in `TenantSettingsService` (program_settings, no migration, default = the prior hardcoded values), new `stringList()` overlay (trim/dedupe; empty/non-array→defaults so a tenant can't lock itself out). New **"Outlet Programs & Categories"** card in the Gifsy Settings panel (GIFSY-only, add/remove chips). Outlets page + upload validator + template read each tenant's lists (DEFAULT fallback pre-hydrate). **Independent audit (privilege/lockout/hooks/cache/types CLEAN) found a HIGH — FORMULA/CSV INJECTION:** owner-typed values reached spreadsheet cells unsanitised in the FE template AND backend exports (a **pre-existing** gap on other free-text cols too — Owner Name, Distributor, City, Remarks). **Fixed at the serialisation boundary:** `cellSafe` (=,+,-,@,tab,CR → leading apostrophe; idempotent) now applied to **every string cell in `buildXlsx`** (closes all backend exports at once) + the FE template matrix. **Runtime-verified (`1bc9315`):** custom list round-trips; `[]` → defaults (lockout guard holds); non-GIFSY PUT → 403; reverted Deoleo to defaults. Pushed `1bc9315`. **NOTE for go-live: confirm Deoleo's real program/category values** — if not in the default list, set them via this card before loading the outlet master. | ✅ done |
| **OID** (UAT) | Owner: **Outlet ID must accept ANY character/special character — no restriction.** The Outlet Master upload (CREATE) + Deactivate flows rejected anything outside `^[A-Za-z0-9-]+$` (real distributor/outlet codes carry `/`, `.`, spaces, `&`, `:`, etc.), blocking legit uploads. | Removed the outlet-ID format regex from `validateOutletUpload` (CREATE) **and** `validateDeactivateUpload` (dropped `OUTLET_ID_RE`) — only **required + unique-in-file** remain; existence/active checks run unconditionally. **Mirrors the K26 employee-hierarchy precedent** (same "safe: xlsx exports + exact-match key only" reasoning). Backend already persisted it verbatim (`@IsString()` only; no `@Matches`; upsert stores `outletId.trim()` on `clientId_outletCode`). Template Dos&Don'ts + HTML ops guide reworded; tests O16/O50/O54 flipped to assert acceptance + new O54b (duplicate special-char still rejected). **Independent adversarial audit: SHIP** — the two protections the old regex implicitly gave (formula-injection, unencoded-URL routing) are already covered (cellSafe everywhere via AF-5; detail routes key off the CUID, not the code). Audit found **one real regression my change enabled** → **fixed**: a comma in a code shattered the visibility-status lookup (FE comma-joins → backend `split(',')`). Now the FE `encodeURIComponent`s each code before the join and the backend `decodeURIComponent`s each segment (handles `,`/`&`/spaces/any char); backend spec adds a comma-bearing round-trip test. Gate: **api jest 1124 · FE vitest 1526 · tsc 0 both sides**. | ✅ done (awaiting owner push) |
| **OSZ** (UAT) | Owner: **Outlet-addition file size limit should equal employee-hierarchy creation.** | **Already identical — no change needed.** Both flows parse the xlsx client-side (SheetJS, no FE byte/row cap) and POST parsed JSON through the same `/api/*` proxy → same Cloudflare Worker (no body limit) → same NestJS body-parser. **K28** already raised that parser to **25 MB globally** (`main.ts` json+urlencoded) for the hierarchy upload, and the outlet upsert (`/admin/outlets/upsert`) rides the exact same parser → both are bounded only by the shared 25 MB limit. Traced FE→proxy→worker→backend; confirmed no smaller per-flow cap exists on the outlet path. | ✅ already satisfied (verified, no code change) |

## 🟢 2026-06-25 SESSION (cont.) — VIS: per-tenant Visibility on/off toggle (pushed `d5d175e`; gate api jest **1096** · FE vitest **1515** · tsc 0; independently audited; runtime-verified on staging)
| # | Found | Fix | Status |
|---|---|---|---|
| **VIS** | Owner wanted a per-tenant master on/off for the whole Visibility module. Did not exist (`visibilityCaptureMode` = mode-select; `visibilityPhotoEnabled` unenforced; `OutletTypeClientConfig.visibilityEnabled` = per-outlet-type display flag only). **Owner decision: default OFF (opt-in per tenant).** | Added `visibilityEnabled` to `TenantSettingsService` (DB-backed program_settings, default **false**, boolean-only overlay) + `getVisibilityEnabledUncached()` — read **UNCACHED** so an ON/OFF flip is honoured **immediately across all Cloud Run instances** (the 5-min per-instance settings cache would otherwise leave a stale kill-switch). `TenantService.resolveVisibilityEnabled()` fails **CLOSED**. Master gate on **all 8** visibility endpoints (photo submit/list/approve/reject/outlet-statuses/fraud-log + amount bulk-upload/records) → **403** for a tenant's own users when OFF; the **GIFSY operator is exempt only in true platform context** (`!assumed`) so cross-tenant approval of ENABLED tenants still works. FE: `visibilityEnabled` in GifsySettings + `/v1/settings` plumbing (gate with `=== true`, fail-safe), ON/OFF toggle in the Gifsy Settings panel (GIFSY-only), and **all 11 surfaces hidden** when OFF (sales dashboard task + `/sales/tasks` + `/sales/visibility`, `/partner/visibility`, `/admin/visibility` + the Visibility-Approval & Visibility-Invoices nav links, KYC photo action). **Independent adversarial audit found+fixed 2 real issues:** HIGH cross-instance stale cache (→ uncached read), MEDIUM ungated `/reports/visibility-status` export (→ gated, was leaking a disabled tenant's residual submissions to MIS_USER); also suppressed the dashboard `pendingVisibility` KPI when OFF. **Runtime-verified on staging (`d5d175e`):** Deoleo (default OFF) → admin/sales 403 + `/settings` shows false; GIFSY exempt → 200; assume-Deoleo → flip ON → 200 (immediate) → flip back OFF → 403. **Deoleo launches with visibility OFF (owner decision 2026-06-25).** Pushed `d5d175e`. | ✅ done |

## 🟢 2026-06-25 SESSION (cont.) — K20-K28 (all pushed `develop`; latest api jest **1081** · FE vitest **1514** · tsc 0)
| # | Found | Fix | Status |
|---|---|---|---|
| **K20** | Targets template From/To defaulted to This-Quarter. | Default BOTH to the current month (`monthOptions[0]`). Pushed `2cc7b3e`. | ✅ fixed |
| **K21** 🔴 | A partner whose outlet(s) were deactivated could still **log in**. | `assertPartnerNotDeactivated` (auth.service) blocks login when the ChannelPartner is deactivated or has 0 active outlets; runs on verifyOtp **AND** refreshToken; `deactivate()` revokes the partner's sessions when their last active outlet goes. Independent audit found+fixed the refresh bypass → SAFE. Pushed `c389a77`. | ✅ fixed |
| **K22** | Sales My-Outlets **table** view wasn't approved-first (cards were). | Table maps `sortedVisibleOutlets`. Pushed `c389a77`. | ✅ fixed |
| **K23** 🔴 | Sales **Wallet** page "This page couldn't load" (route 500). | Ledger page deref'd the outlet title before its loading guard → SSR threw. Added a spinner guard. Pushed `c389a77`. | ✅ fixed |
| **K24** | Sales dashboard vs Tasks page: **Approval Required count differed** + dashboard lacked Visibility. | Extracted `lib/sales-tasks` (`buildKycSubRows`+`buildVisibilityTaskItems`) consumed by both → counts match + Visibility on the dashboard. Pushed `fae0446`. | ✅ fixed |
| **K25** | Target-Achievement pace badge read the all-KPI **average** (skewed green). | Reads the PRIMARY KPI: "{remaining} {unit} to go · N days left", coloured by primary pace. Pushed `fae0446`. | ✅ fixed |
| **K26** | Employee-hierarchy upload: invalid-char rule rejected IDs like `Brahma:DSR Bangalore 1`; row-validation phase had **no Excel error report**. | Removed the char rule (safe: xlsx exports + exact-match key only); added the consolidated row-error report (shared `buildHierarchyErrorSheet`). Pushed `fae0446`. | ✅ fixed |
| **K27** | **Outlet Master export** was 17 cols; owner finalized **56**. | Rewrote `GET /v1/admin/reports/outlet-master` to the 56-col spec (hierarchy chain, geo, enroll/approval-by, statuses, GST/PAN, 3 doc links, Deactivated At). New **PUBLIC `GET /v1/kyc/documents/view?token`** = 30-day, single-doc, single-tenant, typ-gated signed-token doc link (no login). Independent audit: token model SAFE; **found+fixed a HIGH stored-XSS** (inline client-mime → safe-mime allowlist + nosniff + HS256 pin). Pushed `ba93768`. | ✅ fixed |
| **K28** 🔴 | Employee-hierarchy **Confirm Upload → "Internal server error"** for a 567-row file. | Root cause (staging log): `PayloadTooLargeError` — Express default **100KB** body limit. Raised the API body-parser to **25MB** (`main.ts`; multipart unaffected). Pushed `ba93768`. | ✅ fixed |
| **K29** | Re-uploading a **deactivated outlet** via the Outlet Master upload did NOT reactivate it (FE labelled it "REACTIVATE" but the backend `buildOutletUpdate` left `isActive` untouched) → partner stayed locked out (K21) + targets empty (`getTargets` filters isActive). | `upsert()` now reactivates a genuinely-DEACTIVATED outlet (existing, inactive, `deactivatedAt` set): isActive→true + `deactivatedAt` cleared + counted `reactivated`. A still-PENDING outlet (`deactivatedAt` null, never KYC-approved) is NOT activated — re-upload can't bypass KYC. `/reactivate` also clears `deactivatedAt`. Deactivation deletes nothing → targets/KYC/partner/geo/wallet preserved; the sales assignment re-creates from the reactivation row's XSR. Tests: REACTIVATE vs no-pending-activation. **Runtime-verified:** reactivated OUT-2026-002111 → Nnnn partner login restored + its configured targets (Monthly 100 / Consistency 30) reappeared. Pushed `ba1e0c1`. | ✅ fixed |

> **K27 finalized Outlet Master format (56 cols, owner-locked 2026-06-25):** Zone · Outlet Code · Outlet Name · Outlet Type · Program Name/Category · Distributor Code/Name · Metro · Beat · {XSR,SO,ASM,RSM,ZNM,NSM}×(ID,Name,Phone) · Owner Name · Phone Number · Address · City · State · Pincode · Lat/Long of Outlet Board · Enrollment-by Employee (ID/Name/Phone) · Enrollment Date/Status · Profile Status · Approval-by Employee (ID/Name/Phone) · Approval by Gifsy (Y/N) · Approval Date of Gifsy · Lat/Long of Bank-Details Collection · Remarks · GST · PAN · GST Certificate (link) · Address Proof (link) · Self-Declaration (link) · Deactivated At. **Dropped/excluded:** District, bank account/UPI/IFSC. Address Proof = SHOP_ESTABLISHMENT doc. Doc links = 30-day tokenized URLs (the file IS the credential — handle as sensitive).
> **⭐ VISIBILITY toggle — ✅ DELIVERED 2026-06-25 (see the VIS block at the top).** Owner chose **default OFF** (opt-in per tenant); Deoleo launches OFF. Built, gate-green, independently audited, pushed `d5d175e`, runtime-verified on staging.

## 🟢 2026-06-24 SESSION — KYC sales-assisted submit + identity display (all pushed `develop`, gate api jest 1024/1024 · FE vitest 1483 · tsc 0, each independently audited + runtime-verified on gifsy_dev)
| # | Found | Fix | Commit | Status |
|---|---|---|---|---|
| **K1** 🔴 | **Sales rep blocked from submitting KYC** after one pending ("You already have a pending KYC submission"); submission orphaned (partnerId null), no owner created | `POST /v1/kyc` `create()` was self-enrol-only (dup-guard on `userId:user.sub`=rep; ignored `dto.outletId`). Rewrote **outlet-driven**: resolve outlet from outletId (added to DTO+FE), find-or-create owner `ChannelPartner`+`User`(PENDING_VERIFICATION; login only at GIFSY approval), dup-guard scoped to the **outlet's partner** (rep enrols UNLIMITED outlets), approval activates the **owner** not the submitter; `assertPhoneAvailable` guards the owner phone (H1). Runtime: 1 rep→5 outlets 5/5 201. | `25bc2da` | ✅ pushed (task #110) |
| **K2** | KYC list/detail/approvals showed the **rep's** name/phone as the store identity (side-effect of K1) | Sales (`2b91484` #111) + admin/Gifsy (`00e1d56` #112): TITLE=outlet name, Outlet-ID=code, Phone=outlet/owner — never `s.user`. Backend `getOne`/`list` return `partner.outlets{name,code,phone}`; reviewQueue/export were already partner-first. Removed dead `KYC_DATA`+`MOCK_PENDING` mocks. | `2b91484`,`00e1d56` | ✅ pushed (#111/#112) |
| **K3** | **Owner name** missing — Gifsy reviewer must validate it against submitted documents | Added a distinct **"Owner Name"** field = `partner.ownerName` to sales+admin KYC detail/list/approvals (`list()` select +ownerName). Title stays outlet name; "Submitted by"=rep. Runtime-verified on admin detail. | `3b5cabe` | ✅ pushed (#113) |
| **K4** | AF-2/AF-3 sales fabricated data | approve/reject persist + KYC re-entry flow (P3 prefill was unwired) + retired OUTLET_ACHIEVEMENTS + retired dup `/sales/kyc/[id]/edit` + leaderboard/support real identity + backend phone-uniqueness + sales-role cleanup; sales `/profile` 5 UI tweaks | `e998282`,`02f6d52` | ✅ pushed (#102–109) |
> **OWNER DECISIONS (2026-06-24):** `admin/dashboards/kyc` is 100% hardcoded mock (gap-#57a) → **owner: "let it be"** (do NOT hide/wire). ~~Sales **"Ranks" leaderboard** has NO sales-rep backend (`/v1/leaderboard` is partner-based) → recommend hide (DEFERRED).~~ **✅ Leaderboard BUILT 2026-06-26 (`a525739`+`a272dca`)** — real live-computed `GET /v1/sales/leaderboard` (same-level peers, ZNM territory); tab is live. **NEXT (owner-ordered, updated 2026-06-26 — LB + AF-5 since done):** AF-6 JWT-localStorage 🔴 → AF-7/8 GSTIN/invoice 🟠 → #76 prod data load → #74 owner ops → go-live.

## 🟢 2026-06-24 SESSION (cont.) — KYC review depth + SALES HIERARCHY SCOPING + targets read-path (all pushed `develop` `ff7660f..16ba382`; gate api jest 1047 · FE vitest 1489 · tsc 0; K6/K7 independently security-audited; runtime-verified on gifsy_dev)
| # | Found | Fix | Commit | Status |
|---|---|---|---|---|
| **K5** | KYC **Document Review showed placeholder boxes** + the "Tier" chip was dead; **photos = "demo mode"** stub; docs listed blank labels + raw "PENDING" | `getOne` returns a **signed `viewUrl` per document** (private GCS signed via StorageService; data-URL pass-through; `pending://`→null, fail-closed). Sales+admin KYC detail render real doc labels + status + a **View** link + store/owner **photo thumbnails**; removed dead Tier chip. + **Owner Name** kept distinct. | `4066edd` | ✅ pushed |
| **K6** 🔴 | **Sales hierarchy access NOT scoped** — an SO couldn't do KYC for outlets tagged to their XSRs; an XSR's submission didn't reach the SO's approval queue; every SO saw every other SO's queue (cross-team leak) | **Subtree scoping** (Q4 + owner): `GET /sales/outlets` returns the caller + **whole downline's** outlets; `GET /kyc` scopes a sales caller to submissions filed by their subtree (replaced the tenant-wide `PENDING_<level>` filter); `getOne`/`ledger` gain the subtree view-guard; **first-approve/reject require the ROUTED approver** (submitter's first ACTIVE manager up the chain, skip vacant). **MIS_USER kept tenant-wide read** via `canReadTenantWide` (audit caught this regression). New helpers `descendantSalesUserIds`+`firstActiveApproverId`. Runtime: SO sees the XSR's outlet + the XSR submit→PENDING_SO→SO sees+approves→PENDING_GIFSY. | `4066edd` | ✅ pushed |
| **K7** | **Targets/achievements didn't reflect** in the sales app (outlets list, dashboard card, team drill-down) "across the hierarchy" | (a) `getTargets`+`getOutletTargets` read the caller's **whole downline's** outlets (`subtreeOutletCodes`), not just own. (b) FE `PERIODS` was a stale hardcoded list ending "May '26" → **generated dynamically (last 12 months)** + outlets page defaults to the **current month**. (c) **Team-member drill-down** (`/sales/team/[memberId]/outlets`) was 100% mock (`resolveConfig`/`DEMO_*`) → wired to a new `GET /sales/team/:memberId/outlet-targets` (IDOR-guarded) with real per-outlet KPI data. Runtime: SO `/outlet-targets` returns own+downline rows; `/targets` outletCount spans downline. | `d517e5e`,`121fe28` | ✅ pushed |
| **K8** | **UPI ID missing** on KYC detail (form captures bank OR UPI) | Sales ("Payment Details") + admin KYC detail render a **UPI ID** row from `partner.upiId`. | `d517e5e` | ✅ pushed |
| **K9** | **New-KYC outlet picker listed every outlet** regardless of status | Picker now lists **only not-yet-submitted** outlets (NOT_STARTED + draft); submitted/under-review/approval-pending/Gifsy-review/approved/rejected/re-upload/re-KYC/not-interested all excluded. Re-entry of rejected/re-KYC outlets unchanged (KYC-list "Edit & Resubmit" deep-link bypasses the filter via the full roster). | `d517e5e` | ✅ pushed |
| **K10** | **Same outlet appeared multiple times** in the sales KYC list (a rejected attempt + its resubmission are 2 rows) | The list **collapses to one row per outlet = the latest (current) status** (`dedupeByOutlet` by outletCode, max updatedAt); NOT_STARTED are disjoint; no-outlet submissions kept individually. | `16ba382` | ✅ pushed |
> **STILL OPEN (flagged honestly):** the **"Anil Sharma / Awaiting SO" no-outlet KYC entry** (a submission whose partner has no linked outlet → falls back to the rep name) is a separate data oddity, not addressed.

## 🟢 2026-06-25 SESSION (cont.) — SO KYC review screen: address / GST-PAN / View
| # | Found | Fix | Status |
|---|---|---|---|
| **K17.1** | **Address blank (", ,")** on the SO's KYC review. Root cause: `ChannelPartner` has no address columns and `create()` NEVER persisted the submitted `dto.address` (the schema even says `Outlet.addressLine1 // captured at KYC`); the page read the non-existent `partner.address`. | `create()` now writes the KYC-captured address onto the **Outlet** (addressLine1/city/state/pincode, tenant-scoped) inside the existing tx; `getOne` returns the outlet's address fields; sales + admin detail pages read address from `partner.outlets[0]`. (Existing pre-fix submissions show city/state only until re-submitted.) | ✅ fixed |
| **K17.2** | **GST/PAN (and bank) masked** (`****FFCC`) for the SO reviewer — they can't validate against the documents. | New `canSeeFullKycPii(role)` = admins **+ sales-chain reviewers**; getOne un-masks PAN/GST/**bank** for them (owner decision 2026-06-25). Still gated by `assertCanViewSubmission` (own/downline only) → no cross-tenant/out-of-subtree exposure; **MIS_USER stays masked** (observer). Tests added. | ✅ fixed |
| **K17.3** | **"View" on a document → blank page.** Since K12 inlines docs as `data:` URLs, browsers BLOCK opening a `data:` URL as a top-level tab. | "View" is now a button → `openDocument()` converts the `data:` URL to a `blob:` URL (renders images + PDFs). **Self-audit caught a stored-XSS**: a doc's mime is client-supplied at upload, so an SVG/HTML "document" opened as a blob would run script — hardened to render ONLY a safe-mime allowlist (jpeg/png/webp/gif/pdf), else download (octet-stream); non-`data:` URLs must be http(s). Tests added (incl. the SVG-XSS guard). | ✅ fixed |
> **K17 audit note:** self-audited at push time (the audit agent was rate-limited), then **independently RE-AUDITED by an agent on 2026-06-25 → VERDICT: SAFE TO SHIP** (no defects). The agent verified all three parts against the actual files (not just the diff): (1) PII-unmask is subtree-gated and runs **before** the response is built — `assertCanViewSubmission` (kyc.service.ts:1008) precedes the `masked=` decision (:1017); cross-tenant is impossible (`kycTenantFilter` hard-scopes non-Gifsy to `clientId`); **MIS_USER stays masked** (`canReadTenantWide` yet not `canSeeFullKycPii`); role strings match the `UserRole` enum. (2) Address write is tenant-scoped (`findFirst {id, clientId}` → 404 on a foreign id, no IDOR), in-transaction, and DTO-validated. (3) `openDocument` allowlist correctly excludes `image/svg+xml`/`text/html` (→ octet-stream download), confines non-`data:` to http(s), sets `noopener`. One **LOW** residual noted (accepted): a malicious inline PDF's JS can run in the new tab, but it's `noopener` + blob-origin so it can't reach the app session. Gate: api jest **1059** · FE vitest **1502** · tsc 0.

## 🟢 2026-06-25 SESSION (cont.) — K21/K22/K23 (pushed `c389a77`, both staging SHAs verified)
| # | Found | Fix | Status |
|---|---|---|---|
| **K21** 🔴 | A partner whose outlet(s) the admin **deactivated could still log in** (saw an empty app — closed sales assignments, "No sales team", no targets). Root cause: `admin-outlets.deactivate()` flips `Outlet.isActive=false` but never touched `User.status`/`ChannelPartner.isActive`, and `verifyOtp` gated only on `User.status`. | New `assertPartnerNotDeactivated(userId, clientId)` (auth.service): blocks a partner login when the ChannelPartner is deactivated **or** has **no active, non-deleted outlet** (no-op for sales/admin; a multi-outlet partner with ≥1 active outlet is never stranded; tenant-scoped). Applied on BOTH `verifyOtp` **and** `refreshToken` (after the atomic claim) so the 30-day refresh token can't keep minting. `deactivate()` also **revokes the live sessions** of partners left with 0 active outlets (same tx, 3 set-based queries). **Independent agent audit: found+fixed the refresh bypass → SAFE TO SHIP** (no false-lockout — KYC approval activates user+outlet atomically). 8 new tests. Runtime: active WHOLESALER `7795096288` still logs in (no over-block); block is unit+audit-proven. **Design: blocks only when ALL outlets inactive (reversible to any-outlet).** Gate: api jest **1071**. | ✅ fixed |
| **K22** | Sales **"My Outlets" table view** showed raw order; approved outlets should be first (the cards view already did). | Point the table `.map` at `sortedVisibleOutlets` (the same approved-first list the cards use) → table + cards match. | ✅ fixed |
| **K23** 🔴 | Tapping **Wallet** on a sales outlet → **"This page couldn't load"** (route 500). The ledger page rendered the outlet title (`o.name`) **before** its inline loading spinner, so while `outlet` was null — the whole loading window, and SSR (the effect never runs server-side) — `const o = outlet!` was dereferenced and threw. | Return a spinner until `outlet` is loaded. Swept siblings: `team/[memberId]` uses the same pattern **safely** (deref inside the guard). Regression tests **LG1/LG2** (LG1 throws against the old code). Runtime: ledger API 200+meta for SO-EX2. Gate: FE vitest **1511** · tsc 0. | ✅ fixed |

## 🟢 2026-06-25 SESSION (cont.) — K18: show full captured location on the KYC review screen
| # | Found | Fix | Status |
|---|---|---|---|
| **K18** | Owner: the KYC review screen should show **all** the captured location detail — address, city, state, pincode **and lat/long** — but only address/city/state/pincode were shown; the **KYC-captured geo was never surfaced**. | FE-only (the geo already rides in the `getOne` response — `boardPhotoLat/Lng` = store-board capture, `paymentLat/Lng` = payment capture, on the submission). Added a **"Location (lat, long)"** row (6 dp) to BOTH the sales and admin/Gifsy review screens, preferring the store-board geo and falling back to the payment geo; renders nothing when neither was captured. Also fixed the admin Address render to filter-join (no more `", , - "` for null fields). `parseGeo` requires both halves present + finite. Tests **SKGEO1–3**. Runtime: staging `getOne` confirmed returns the geo (e.g. the screenshot's APPROVED outlet = `22.530046, 88.368797`). Gate: api jest **1059** · FE vitest **1505** · tsc 0. | ✅ fixed |

## 🟢 2026-06-25 SESSION (cont.) — K19: tappable maps link + REAL Target-vs-Achievement chart (pushed `a57cd91`, both staging SHAs verified)
| # | Found | Fix | Status |
|---|---|---|---|
| **K19.1** | Owner: make the K18 location **coordinates a tappable Google-Maps link**. | The "Location" row (sales + admin KYC review) is now an `<a>` → `google.com/maps/search/?api=1&query=<lat>,<lng>` (new tab, `noopener,noreferrer`). | ✅ fixed |
| **K19.2** | Owner: the partner dashboard **"Target vs. Achievement" chart was hardcoded ₹-lakh mock** (`monthlyData`/`yoyData` in `achievement-chart.tsx`), unrelated to the real primary metric (Litre hero). | New **`GET /v1/partner/targets/trend`** → `PartnerService.getTargetTrend`: the partner's PRIMARY KPI target-vs-achieved per month (last N, default 24), summed across their outlets, from `OutletTarget`/`OutletSalesRecord`; tenant+caller scoped exactly like `getTargets`; a month with no data stays **null** (honest gap, not a fake 0). New `lastNMonths()` helper. `AchievementChart` is now **data-driven + unit-aware** (Litre, not ₹L): monthly = last 6, YoY = Indian-FY buckets, honest empty state; `buildChartSeries()` lib shapes it. **Chart trends the SAME KPI the hero resolved** (passed `?kpi=`) so the two never disagree. **Independent agent audit → SAFE TO SHIP** (no tenant/caller leak; sums can't be corrupted by `__names`/non-numeric; rollover/clamp/FY-bucketing correct); the one MED finding (chart↔hero KPI divergence) was fixed pre-ship. Tests: `target-trend` lib (4) + `getTargetTrend` service (4). Runtime (staging, partner `7795096288`): trend returns `kpiName=Monthly unit=Litre`, 2026-06 = 100/100 — matches the hero. Gate: api jest **1063** · FE vitest **1509** · tsc 0. | ✅ fixed |

## 🟢 2026-06-25 SESSION (cont.) — sales dashboard "Approval Required" count mismatch
| # | Found | Fix | Status |
|---|---|---|---|
| **K16** | The SO **Dashboard → Tasks → "Approval Required"** showed **1**, but the **KYC Submissions** list ("Approval Pending") showed **2** (Patel Kirana + Anil Sharma, both "Awaiting SO"). Root cause: the dashboard derived ALL its KYC task counts from `/api/sales/outlets` (`outlets.filter(kycStatus===PENDING_SO_APPROVAL)`), while the KYC list is built from `/api/kyc` **submissions**. A submission whose partner has **no linked outlet** (Anil Sharma — the flagged no-outlet oddity) never appears in the outlets list, so the dashboard silently dropped it. (Same root as the old broken approval-item hrefs, which pointed at outlet ids instead of submission ids.) | Dashboard now fetches `/api/kyc` and builds the **Approval Required** + **Rejected KYC** groups from **submissions** (mirroring the list's map + per-outlet dedupe, keeping no-outlet submissions individually); href is now the submission id (fixes the detail-link too). NOT_STARTED / draft / Re-KYC groups stay outlet-based (they're outlet-anchored). Regression test added. Gate: FE vitest **1500** · tsc 0. | ✅ fixed (pending push runtime-verify) |
> **K16 note:** "Approval Required" counts submissions at the viewer's OWN approval level (PENDING_SO_APPROVAL for an SO) = "needs my approval". The KYC list "Approval Pending" filter is currently broader (SO+ASM+RSM); in the reported case both are SO-level so they match. If an SO-approved-awaiting-ASM submission ever makes them differ, that's the list being broad, not the dashboard — reconcile then.

## 🟢 2026-06-25 SESSION (cont.) — outlet-master upload "Failed to read file" (numeric cell)
| # | Found | Fix | Status |
|---|---|---|---|
| **K15** | Uploading a filled outlet-master template (to add outlets in Outlet Management) failed with **"Header Error — Failed to read file — please ensure it is a valid XLSX file"**. Diagnosed against the owner's actual file: it IS a valid xlsx (2382 rows, correct headers) — but a `Distributor ID` typed as a bare number (`304077`) comes back from SheetJS as a **number**, and `parseOutletUploadRows` did `(raw['Distributor ID'] ?? '').trim()` → `.trim is not a function` → caught by the handler's generic `catch` and mis-reported as "Failed to read file". (Same numeric-cell class as the earlier hierarchy `.trim` crash.) | (a) shared `parseXlsx` now reads with `{ raw: false }` → SheetJS returns formatted **strings** for every cell (fixes all 3 uploads: outlet / re-KYC / deactivate at the source, makes the `Record<string,string>` type honest); (b) hardened the three row parsers with a `cell(v)=String(v??'').trim()` coercion (defense-in-depth vs the whole numeric-cell class). Runtime-verified: the owner's exact file now parses all 2382 rows (`distributorId` → `"304077"`). Regression test O37 added. Gate: FE vitest **1499** · tsc 0. | ✅ fixed (pending push runtime-verify) |
| **K15b** | Owner request (bundled): **Beat and Metro should be optional**, not compulsory, in the outlet-master upload. | Dropped the row-level "Beat is required" / "Metro is required" errors (Metro still validates Yes/No **when filled**); `validateOutletUploadHeaders` no longer requires the Beat/Metro **columns** (new `OPTIONAL_OUTLET_HEADERS`) — they remain in the generated template so users can still fill them. Template DO/DON'T + guide HTML re-badged Optional. Tests O5a/O14a/O14b added. | ✅ done |

## 🟢 2026-06-25 SESSION (cont.) — sales dashboard "achievement not showing" in Target Achievement
| # | Found | Fix | Status |
|---|---|---|---|
| **K14** | The SO dashboard **Target Achievement** card showed the target (e.g. 0/100 Litre) but **achievement = 0**, even though achievement was uploaded. Diagnosed at runtime (in-VPC DB query): targets existed for **2026-06/07/08** but achievements (`outlet_sales_records`) only for **2026-06**; `getTargets` auto-picked the **latest TARGET month = 2026-08** (a future target, no period passed by the dashboard FE) and read achievement for August → none → 0. The real June achievement was on a month the card never displayed (and that's why the card oddly read "August 2026"). | New shared `reportMonth(period)`: when the caller names no period, anchor on the **CURRENT calendar month** (`currentMonthKey`, server-clock `YYYY-MM` — same basis as the FE's CURRENT_MONTH + the upload month-lock; **owner decision 2026-06-25**). Used by `getTargets` (dashboard) + `readOutletTargets` (outlets list / team drill-down). The outlets/team views pass an explicit period so they're unchanged; only the no-period dashboard auto-pick moves from latest-target → current-month. Regression test added (no-period → current month, no "latest" DB lookup). Gate: api jest **1057** · tsc 0. | ✅ fixed (pending push runtime-verify) |

## 🟢 2026-06-25 SESSION — Gifsy-admin KYC reject + uploaded-photo visibility (owner UAT on staging; diagnosed at runtime → fix → INDEPENDENT audit → full gate api jest 1056 · FE vitest 1492 · tsc 0)
| # | Found | Fix | Status |
|---|---|---|---|
| **K11** 🔴 | **Gifsy admin could not reject a KYC** — broken at BOTH layers: (a) `POST /v1/kyc/:id/reject` `@Roles(...)` listed all sales roles + `CLIENT_ADMIN` but **omitted `GIFSY_ADMIN`** → 403 before reaching the service (whose `reject()` already implements the Gifsy branch — any stage, exempt from routed-approver); (b) the admin detail page's Approve/Reject/Request-Reupload buttons were **local-only stubs** that flashed a success banner with **no network call**. | (a) added `GIFSY_ADMIN` to the reject `@Roles`; (b) `admin/kyc/[id]/page.tsx` handlers now POST `/reject` (`REJECTED` / `RE_UPLOAD_REQUIRED`) + `/approve`, refresh the submission, and surface a real error banner (no more silent success). Backend `reject()` + behavioral FE wiring tests added. | ✅ fixed (pending push runtime-verify) |
| **K12** | **Uploaded photos/docs not visible** in the portal review — runtime-proven on staging: docs ARE real GCS uploads (`fileUrl: https://storage.googleapis.com/…`) but the API returns **`viewUrl: NULL`** for every one. **V4 signed-URL generation throws on Cloud Run** (fail-closed `catch` nulls it) despite correct IAM (SA has `serviceAccountTokenCreator`, iamcredentials enabled); also impossible locally (no signing keyfile). | Stopped relying on signing: `resolveDocumentViewUrl` now **inlines bytes as a `data:` URL** via new `StorageService.downloadAsDataUrl` (object READ via the SA access token — the same grant uploads use — works on staging/prod/local). Bounded by an **8MB/doc + 24MB/response budget** (audit finding). Read failures **log** instead of silently swallowing. | ✅ fixed (pending push runtime-verify) |
| **K13** | On re-entry (KYC rejected by Gifsy admin **or** by the sales senior, or sent for re-KYC), only the TEXT fields pre-filled — the rep had to **re-upload every document and re-shoot every photo**, and the banner falsely claimed "everything is pre-filled". | The new-KYC wizard now fetches the previous submission (`GET /api/kyc/:kycId`, which inlines each doc as a data URL via K12) on re-entry and **pre-fills the documents, photos, board/payment geo, and the signature** — plus the existing text-field prefill; carried-over slots show a "From previous submission" hint, and the reviewer **remark** is shown prominently ("Reviewer remark — fix this:"). On resubmit the same GCS objects are reused (`fileKey`/`fileUrl`). FE-only — the data already existed post-K12. **Independent audit caught + fixed a HIGH** (switching to another outlet mid-flow leaked the first outlet's docs/geo/signature → added a per-outlet reset, regression-tested) + a MED (signature reuse now shows a "carried over — Clear to re-sign" hint; consent re-confirmed by the post-submit owner OTP) + a NaN-safe geo coercion. Gate: FE vitest **1495** · tsc 0. | ✅ fixed (pending push runtime-verify) |
> **K13 owner decision (defaulted, reversible):** the owner's prior **signature** is carried over (honoring "everything pre-filled") with a visible "carried over" hint, and the post-submit **consent OTP to the owner's phone re-fires on every resubmit** = the real consent proof. If you'd rather force a fresh wet signature on every resubmit, say so and I'll drop the signature carry-over (keep docs/photos/geo).

> **K12 known residuals:** (1) the **`reviewDump` xlsx export** still uses V4 `getSignedUrl` → blank doc-link columns in the same env (separate surface; data-URLs can't be clickable Excel cells — proper fix is IAM-layer signing or a per-doc redirect endpoint; fails CLOSED). (2) inline `data:` URLs have no expiry (acceptable for an authed review screen). (3) **PDF** docs (GST/shop-establishment) still won't render inside an `<img>` viewer — non-regression (a signed-URL-to-PDF wouldn't either); the photos (the owner's complaint) are JPEGs and render.

## 🔎 UAT-FOUND FIXES (2026-06-22, owner testing staging) — each diagnosed → executor → INDEPENDENT audit → gate → runtime-verify
| # | Found | Fix | Commit | Status |
|---|---|---|---|---|
| U1 | Employee Hierarchy upload "not a valid .xlsx" on the **downloaded template** | `XLSX.read(ArrayBuffer,{type:'array'})` needs a **Uint8Array** — dev-build tolerates it, **`next build` (staging) throws**. Coerce + un-swallow the real error + Playwright round-trip test. | `5601ba8` | ✅ pushed |
| U2 | Hierarchy upload `(raw[..]??'').trim is not a function` | SheetJS returns **numbers** for numeric cells; `String(v??'').trim()` in both parse paths + numeric-cell vitest. | `be9d685` | ✅ pushed |
| U3 | Sales-data/target upload values land on the **WRONG KPI** (when a cell is blank) — DATA INTEGRITY | parser mapped KPI cols POSITIONALLY off the row-1 merged month header → drift mis-assigns; rewrote to **name-keyed absolute-column** mapping + integrity guard. Covers target + achievement (shared parser). | `089270d` | ✅ pushed |
| U4 | Upload error lists are a 100-row on-screen table, un-fixable | shared **downloadable .xlsx error report** (`lib/error-report.ts` + button) on Outlet Master + Targets + Payout-UTR + KYC bulk-verify. | `a811fe2` | ✅ pushed |
| U5 | "Primary" KPI could be ticked on **multiple** rows (should be exactly one; "changeable not addition") | **4-layer single-primary:** UI radio (move) + backend `setPrimary`+`P2002→409` + **self-healing partial unique index** `20260622120000` + retired 2 **dead** KPI blob stores. Plan + impl independently audited. **Layer 4b (partner surface honors isPrimary) DEFERRED.** | `c17887c` | ✅ pushed |
| U6 | Cash redemption (UPI/Bank): partner saw **fabricated** bank details; should be one free-amount flow from KYC, one offline settlement file | **Unified cash payout:** real-KYC beneficiary on `/me`; admin cash-item free-amount-with-min (un-gated); settlement export = one file (all bank/UPI cols, blank where missing); **upstream beneficiary guard BEFORE debit.** Independently audited SAFE. | `4de8794` | ✅ pushed |
| U7 | Employee Hierarchy upload "shows N → **0 after refresh**" (regular template) | Snapshot stored `[[],[]]`: the global ValidationPipe (`whitelist+transform+enableImplicitConversion`) mangled the untyped `employees: any[]` into Array instances (JSON-serialize to `[]`) + stripped props. PUT now reads the **raw Express body** (passthrough blob) + 400-guards malformed elements. | `771276f` | ✅ pushed |
| U8 | KPI "name override" was a **dead setting** — template had no name column, nothing displayed | Wired end-to-end: template emits a name column per override KPI; parser stores per-outlet names in a reserved `targetValues.__names` map; partner Targets view shows custom-name-if-set-else-label. No schema change. Runtime-proven round-trip + independent audit SOUND. | `0fa9d92` | ✅ pushed |
| U9 | Employee Hierarchy **CHAIN** upload (18-col, →11 emps) "11 created → 0 after refresh" — **the real recurring bug** | Staging logs: `salesHierarchyLevel.upsert` P2002 on `(clientId,level)` → whole `$transaction` rolled back → snapshot lost; FE hid the 500 (fire-and-forget PUT + swallowed `.catch()`). Fix: free the level space before re-assign (collision-proof, orphan-safe) + tx timeout + **FE awaits PUT & surfaces errors** + reload-round-trip E2E. **A/B-proven** (same deoleo tenant: 500→200+GET 11). Audit SOUND. | `141c385` | ✅ pushed |
| U10 | Pace View **target numbers misplaced** when an outlet has a blank KPI cell | Backend correct (stores/returns by code); the FE Sales pace table built columns from `outlets[0]` but rendered each row's `kpis` array **positionally** → blank KPI shifts values left. Fix: union-of-codes column set + **by-code cell lookup** (blank → "—" in the right column). | `3b4209b` | ✅ pushed |
| U11 | CHAIN upload **STILL 500** after U9 (deployed `3b4209b`) — staging logs showed a **different** P2002: `salesUser.upsert` on **`userId`**. A phone already attached to a sales-user (seed/prior upload) under another code is re-used under a new code → `SalesUser.userId @unique` blows up → 500 → snapshot rolled back. | **Two-part fix.** Backend (`hierarchy-persistence.ts`): pre-write guard rejects, with a clean **400**, (a) a phone given to two codes in one file, (b) a phone already owned by a different existing code / a non-sales account, (c) blank/duplicate `emp.id` (closes a same-symptom **P2002 on employeeCode** the audit found); blank mobile → unique synthetic (no `(clientId,'')` collapse). Frontend (`employee-hierarchy.ts`, owner rule): same employee ID **blank in some rows but filled in others** → hard `BLANK_CONFLICT` in the error report (was silently healed). 7 BE + 3 FE tests; **runtime-proven on live `gifsy_dev`** (conflict→400, clean→persist, blank-id→400); independent audit = core fix SOUND. **Known limitations (recorded, not blocking):** a phone-swap between two codes in one file, and resign→re-hire-same-number, are rejected by policy (owner chose reject-with-clear-error). | `5153bcb` | ✅ pushed |
| U12 | Outlet Master "shows added but did NOT add" | Root cause (data): the **Deoleo tenant had ZERO enabled `OutletTypeClientConfig`** rows, so the backend `upsert` rejected every row as "Unknown outlet type", pushed per-row ERROR + `continue` (no throw) → **HTTP 200, created:0**. FE bug: `onConfirm` showed success on any 200, never reading `created/updated/errors`. Third defect: FE validated types against a **hardcoded** `VALID_OUTLET_TYPES` (also stale — had `SSS`, missing `RETAILER`). **Fix (FE code):** `GET /admin/outlets` now returns the tenant's enabled type codes; `validateOutletUpload` + the **download template + example rows + HTML guide** all source types from THAT (no hardcoded list); `onConfirm` reports success only when `created+updated>0`, else surfaces per-row errors; list re-fetches on success. Backend list() + 5 FE spec cases; runtime-proven (outletTypes query on gifsy_dev); independent audit caught + closed the template-still-says-SSS gap (C1). **NOTE (data, owner-deferred):** Deoleo staging still needs its outlet types **enabled** before outlets can be added. **Known follow-ups:** outlet-type catalog is **inconsistent across envs** (dev has both `SSS`+`RETAILER`; staging only `RETAILER`) — reconcile before #76; other hardcoded type lists remain in `campaign.ts`/`credits-payouts/fields` (L1); rekyc/deactivate `onConfirm` still drop per-row errors on PARTIAL failure (L2, they throw on total failure). | `1bd58e4` | ✅ pushed (deoleo types enabled on staging via guarded job) |
| U13 | **Feature (owner request):** Employee Hierarchy had no way to **download the currently stored hierarchy** | Added a **"Download Current"** button that exports the saved snapshot as the same 18-column chain workbook the upload accepts (download → edit → re-upload round-trip). Pure `buildHierarchyChainExportRows` (inverse of `parseHierarchyChainRows`) walks tree-leaves up via reportsToId; **completeness safety net** guarantees every stored employee appears in ≥1 row (no silent drop on malformed/seed snapshots — found by independent audit). 6 round-trip/edge tests; runtime-confirmed snapshot shape on gifsy_dev. | `c54b6a6` | ✅ pushed |
| U14 | **UAT tooling (owner request):** staging used real MSG91 OTP → slow testing (real SMS only) | FIXED_OTP re-enabled on staging behind a **hardened 3-layer gate** (`api/src/common/fixed-otp.ts` `isFixedOtpAllowed`): (1) hard-deny if DATABASE_URL is `gifsy_prod`; (2) allow any non-prod NODE_ENV; (3) on prod-NODE_ENV, allow ONLY when `ALLOW_FIXED_OTP=true` **AND** the DB is positively `gifsy_staging`. Replaced the bare `NODE_ENV!=='production'` check at all 4 OTP sites (login verify, redemption-money verify, MSG91 send, throttler). `ALLOW_FIXED_OTP=true,FIXED_OTP=123456` wired into `deploy-staging.yml` (staging only, never prod). 11 gate tests; **independent SECURITY audit = prod safe even if `ALLOW_FIXED_OTP` is fat-fingered onto prod** (prod DB provably matches the deny-regex; Prisma has no alternate connection vector). To revert to real OTP: unset `ALLOW_FIXED_OTP` in `deploy-staging.yml`. | `c0623c5` | ✅ pushed (verified on staging: 123456 logs in) |
| U15 | **Sales-team (and ALL tenant) login bounced** back to phone entry after a correct OTP | NOT auth — `resolveSlugFromHostname` didn't handle the staging **`uat.` prefix** on tenant domains: `uat.deoleoloyalty.gifsy.in` fell through to `subdomain='uat'` → the login Server Action sent `clientId='uat'` → backend "no account" → bounce. Hit every direct tenant-domain login on staging (admin/partner/sales); surfaced on sales because admin goes via the operator console (`uat.app.gifsy.in`, resolved OK). Fix: strip a leading `uat.` (after the operator-host check) so `uat.deoleoloyalty.gifsy.in`→`deoleo`, `uat.clientb.app.gifsy.in`→`clientb`. 4 resolver tests; **staging-verified** (clientId=deoleo→token, =uat→rejected); independent SECURITY audit = no cross-tenant/privilege risk (operator check precedes strip; backend backstops a wrong resolution → can only reject, never authenticate into another tenant). | `ebc6379` | ✅ pushed |
| U16 | **Outlet-type `SSS`-vs-`RETAILER` drift** (the U12 follow-up): FE was deliberately renamed `RETAILER`→`SSS` (~30 files + tests) but the seed/DB stayed `RETAILER`, so the Excel upload (uses the backend code) and dashboards disagreed | **Canonical = `SSS`** (aligning the data layer to the FE is far fewer edits than reverting 30 FE files; empty prod ⇒ the data rename is free). seed.ts catalog + demo-lookup `RETAILER`→`SSS`; **`/gifsy/outlet-types` made READ-ONLY** (its Add/Rename/Toggle were fake local-state with no backend — there's **no global OutletType CRUD by design** (owner Option-3); the real control is per-client on the client page). One-time guarded DB rename of the existing `RETAILER` row→`SSS` on staging (kept the id, so Deoleo's enabled config + outlets stay linked — verified `DEOLEO ENABLED NOW: [SSS,SSS_TOT,SUB_STOCKIST,WHOLESALER]`). **This is a long-term/structural fix** (future seeds incl. #76 are `SSS`), not a recurring patch. Backend 968/968, FE 1479. **Residual:** dev DB still has a stray `RETAILER` row (local housekeeping; staging/prod clean). | `a4852a5` | ✅ pushed |
| U17 | **Sales login STILL bounced to `/auth/login` after a correct OTP** (post-U15, fix deployed) — admin & partner fine, only sales | NOT clientId (U15 was deployed & correct) and NOT data (DB sales users ACTIVE/clientId deoleo/real phones/`SALES_*` roles; staging `verify-otp` returns a valid `SALES_ISR` token). Root cause: the **Next 16 edge proxy** (`platform/src/proxy.ts`, the renamed `middleware`) `ROLE_ROUTES['/sales']` held **stale legacy codes** (`HO/STATE_HEAD/ASM/SO/XSR/SALES_EXECUTIVE/…`) matching **no** `UserRole` enum value → an authenticated sales user hitting `/sales/*` got a **server-side 307 → /auth/login** before the page rendered (the client `RequireAuth` would have admitted them, but never ran). `/admin`+`/partner` lists were already canonical ⇒ only sales bounced. **2nd layer behind U15.** Fix: `/sales` → canonical `['SALES_HO','SALES_STATE_HEAD','SALES_ASM','SALES_SO','SALES_ISR']` (= `PORTAL_ROLES.sales`); export `ROLE_ROUTES` + regression test locking every list to the enum. Diagnosed via curl (staging 307 to login) + DB ground-truth (in-VPC job) + serving-SHA check; **independent audit = correct+complete for all 5 sales roles, no other server gate can re-block.** Gate: vitest 1483, tsc 0. | `6c8b4a2` | ✅ pushed (runtime-verify post-deploy) |
| U18 | **Two adjacent correctness bugs** surfaced by the U17 audit (neither blocked login) | (1) `proxy.ts` set `x-user-id` from `payload.userId` but the JWT user id is the **`sub`** claim → header always `undefined` (unconsumed today post platform-Prisma retirement; removes a latent footgun) → `payload.sub`. (2) `lib/sales-role.ts` `getRole()` returned `'SO'` for every freshly-logged-in sales user (it only read the demo-switcher localStorage key, unset at login) → managers (NSM/RSM/ASM) & XSRs all rendered as **Sales Officers**. Now derives the FE persona from the **real JWT role** via `roleFromBackend()` when no demo override is set (SALES_HO→NSM, SALES_STATE_HEAD→RSM, SALES_ASM→ASM, SALES_SO→SO, SALES_ISR→XSR); role-switcher override still wins. UI-only (real authz stays server-side). +8 tests; **independent audit = both SAFE, no regressions** (the one render-time `getRole` consumer is Suspense/searchParams-shielded from hydration mismatch). Gate: vitest 1491, tsc 0. | `57c8843` | ✅ pushed (runtime-verified) |
| U19 | **AF-1: partner dashboard hero showed FABRICATED data** (the "800 cases" + demo 4250 pts the owner flagged) | The hero read `usePartnerSession()` (DEMO_SESSIONS) + computed the target from `resolveConfig(DEMO_BEAT/DISTRICT/STATE/PERIOD)`; its one real fetch read a **dead pre-P4.1 SchemeTarget shape** (`json.data.targets`/`getPrimarySchemeTarget`) so the overlay silently no-op'd → demo numbers stuck. (Re-derived per owner: #54 had wired identity/wallet/profile/rewards/targets-PAGE to real data, but the DASHBOARD hero was missed.) **Fix:** backend `getTargets` now returns `isPrimary`+`unit` per KPI (KpiDef columns already existed, no migration); dashboard rewritten to consume real `/api/partner/targets` (aggregate the primary KPI by **summing target+achieved across the partner's outlets**) + real `/api/auth/me` wallet; outletType from the real outlet; **leaderboard card removed** (no real source); honest "No targets yet" empty state; dropped usePartnerSession/DEMO_SESSIONS/resolveConfig/getPrimarySchemeTarget + the achievements context. Crash-safe (empty outlets/zero targets/failed fetches; loading clears via `Promise.allSettled`). **Independent audit = fabricated data gone, real-data path correct, no regressions, tests assert summed real data + ABSENCE of demo 4250/800.** **RUNTIME-VERIFIED on staging:** `/api/partner/targets` returns primary `CONSISTENCY` target 900 Litre (real) + `isPrimary`/`unit`; `/api/auth/me` → 50,000 real pts. Gate: api jest 969/969, FE vitest 1490, tsc 0. **Residual (flagged):** AchievementChart 6-month series still mock history (needs a history endpoint). | `356c135` | ✅ pushed (backend runtime-verified; FE deploy landing) |
| U20 | **Sales rep saw NO outlets / NO KYC tasks** despite outlets assigned & due for KYC (XSR-M010) | `sales.service.ts buildOutlets()` filtered out assignments whose `outlet.partner` is null — but an outlet uploaded via the master file has **NO partner until KYC**, so a rep whose assigned outlets were all un-KYC'd saw an **empty list** and couldn't start enrollment. Ground truth (staging): XSR-M010 had **2 active assignments** (OUT-2026-003, OUT-2026-004), both `partnerId=null`. **Fix:** include partner-less outlets, projecting partner-derived fields null/0 + `kycStatus='NOT_STARTED'`; scoping unchanged (caller's own active assignments only). Redeem (B1) stays gated on a truthy partnerId (FE) + backend `MinLength(1)` on targetPartnerId. **Independent audit = scoping safe (no leak; team-path IDOR guard intact), no runtime-throw, redeem/enroll money paths safe, test genuine.** Gate: api jest 969/969, tsc 0. **Follow-ups (flagged):** `getMember` manager-detail metrics/embedded-outlets still use the old `partner!==null` filter (same bug class → kycPending should count NOT_STARTED) [spawned task]; enroll sheet shows partner-less outlets as enrollable (backend rejects; UX polish). | `ea47509` | ✅ pushed + **runtime-verified** (XSR-M010 `/api/sales/outlets` → OUT-2026-003/004, NOT_STARTED) |
| U21 | **No way to mark a support ticket Resolved** (backend had only create/escalate/reply → tickets sat open forever); the admin drawer's top header was also hidden under the operator banner (sticky z-60 over the z-50 drawer). | **Built Resolve/Close/Reopen end-to-end.** `POST /v1/tickets/:id/status` (RESOLVED/CLOSED/IN_PROGRESS/OPEN) gated to support admins at **3 layers** (`@Roles` + service `isSupportAdmin` + DTO `@IsIn`), tenant-scoped (no IDOR), settable set **excludes ESCALATED** (can't bypass the escalate assignee guard); writes status + a **partner-visible** (`isInternal:false`) system message + audit log in one tx. `addMessage` **reopens** a RESOLVED ticket when the creator replies (loop both ways); CLOSED stays terminal. Admin drawer: Mark Resolved/Close/Reopen + the banner-overlap fix. Partner/sales support pages already map RESOLVED→resolved/CLOSED→closed + render the message → their side closes unchanged. **Independent audit = no defects.** Note: `@RequirePermission('support:write')` is **decorative** under the fail-open PermissionGuard (RBAC_ENFORCEMENT off) — `@Roles` + the service check are the real gates (same as `escalate`). **RUNTIME-VERIFIED on staging:** admin resolve→201 + status RESOLVED + visible "marked Resolved" msg; partner→**403**; bogus status→**400**; reopen→201. Gate: api jest 976/976, FE vitest 1490, tsc 0. | `c836a11` | ✅ pushed + runtime-verified |
| U22 | **Partner targets page guessed the primary KPI by position** (`idx===0`, stale "backend doesn't return isPrimary" comment) → could disagree with the dashboard hero (which uses the real `KpiDef.isPrimary`). | Both partner screens now read the **real** `isPrimary` (OR-folded across outlets) + real `unit`; fall back to first only when none is flagged. Surfaced during the AF-1 hero diagnosis (owner's "hero shows the wrong KPI" turned out to be the data's flagged primary = CONSISTENCY; this closes the screen-to-screen inconsistency). Gate: FE vitest 1490, tsc 0. | `c836a11` | ✅ pushed |
| U23 | **Operator console (A2) showed "working in Deoleo" banner but 0 tenant data** — same page, Gifsy-operator view showed 0 positions while the Deoleo Client Admin saw 16. | NOT a backend/scoping bug — **backend verified correct on staging** (the assume-tenant token returns the tenant's 16 rows; a plain gifsy token returns 0). Root cause = **FE operator-context desync**: the orange banner is driven by a standalone localStorage `assumedBrand` key that drifted STALE vs the actual token — the browser was using the plain gifsy login token (correctly scoped to gifsy → empty) while a leftover banner still said "Deoleo". A fresh login didn't clear the operator-context keys (`exitTenant` does; login didn't), so the banner outlived its token. **Fix:** `getAssumedBrand()` now **self-heals** — returns the brand only while the ACTIVE token is an assumed-tenant token (JWT `assumed:true`, crash-safe base64url decode), else clears the stale home/brand keys and returns null → the banner ALWAYS follows the token; PLUS login clears `homeToken/homeUser/assumedBrand` on every fresh login. UI-only (the backend already enforces tenant scope from the JWT `clientId`). +5 tests. **Independent audit = no defects** (correct, crash-safe, no false-negative [a real assume still shows the banner], regression-free, security-neutral). Minor note: ignores JWT `exp` (banner lingers until a 401 forces re-login — acceptable, UI-only). Gate: FE vitest 1495, tsc 0. | `d3ce606` | ✅ pushed |
| U24 | **New-KYC outlet selector showed the internal Prisma CUID** (`cmqphsp8h0…`) where the human outlet **code** (`OUT-2026-001`) belongs — dropdown rows, collapsed trigger, selected card, step-2 chip, done screen, mobile-conflict message, and the self-declaration PDF. | The backend `/sales/outlets` already returns BOTH `id` (CUID) and `outletCode`; the FE mapped only `o.id` into its `outletId` field and displayed that. **Fix (FE-only): split identity from display** — `outletId` (CUID) still drives the React key, `data-testid`, selection equality, dismissed-set, and the Not-Interested POST body (backend matches `outlet.id`); a new `outletCode` (`o.outletCode ?? o.id`) feeds every user-facing surface + the search filter. Sibling sales pages already used `outletCode` (this selector was the lone outlier). +2 regression tests (distinct CUID vs code → renders code, never CUID). **Independent audit = no defects** (no identity usage flipped, no display leaks the CUID, fallback null-safe). Gate: FE vitest 1497, tsc 0. | `2154fbb` | ✅ pushed |
| U25 | **Every KYC document/photo upload returned a bare 500** ("Internal server error") on staging. | Staging log = `Error: Invalid bucket name: '<BOM>gifsy-platform-files'`. **Byte-proven root cause:** both `GCS_BUCKET` and `GCP_PROJECT_ID` Secret Manager values are stored as `EF BB BF` (UTF-8 BOM) + value + `0D 0D 0A` (CR CR LF) — a Windows Out-File/echo artifact, **same class as the MSG91 key BOM** at cutover. `@google-cloud/storage` rejects the BOM'd name; `.save()` throws a non-HTTP error → Nest renders 500. **Two-layer fix:** (1) **code** — `StorageService` sanitizes both values at read time (strip leading BOM + trim whitespace/CRLF, default-bucket fallback when blank), mirroring the MSG91 `.trim()` defense → a dirty secret can never break uploads again; (2) **data** — re-saved both secrets clean (byte-verified, no BOM/CRLF). +3 specs. **Independent audit = correct & complete for the proven contamination, edge cases safe, no ADC-projectId regression.** **RUNTIME-VERIFIED on staging (serving `b9e721b`):** `POST /v1/kyc/documents` → **201** with `fileKey`/`fileUrl` at the clean `gifsy-platform-files/...` bucket. Gate: api jest 979/979, FE vitest 1497, tsc 0. | `b9e721b` | ✅ pushed + runtime-verified |
| U26 | **Partner Support "Your Sales Team" showed hardcoded demo personas** (Anil Sharma / Rajesh Kumar) instead of the rep actually mapped to the outlet. | FE had a hardcoded `SALES_TEAM` const ("would come from the assigned team in production"). **Fix:** new `GET /v1/partner/sales-team` returns the REAL reps — active `SalesUserAssignment`s on the partner or its outlets → the assigned `SalesUser`(s) + each one's immediate reporting manager (field rep + escalation contact); caller-+tenant-scoped, inactive/deleted excluded, deduped, field-rep-first, manager-add has an explicit `clientId` tenant check (defense-in-depth). FE renders it, hides the call button when a rep has no phone, honest empty state instead of fake names. +6 BE/+3 FE specs. **Independent audit = no scope leak, caller+tenant scoped, no null-deref, PII limited to support contact.** Gate: api jest 985/985, FE vitest 1500, tsc 0. **RUNTIME-VERIFIED on staging (serving `644e7e6`):** `/v1/partner/sales-team` returns the real shape (fake Anil/Rajesh personas gone); for partner "Ravi Kumar" it's `team:[]` because their 1 outlet has **no active sales-user assignment** (honest empty state, not a bug — assign a rep to populate). | `f37f5d5` | ✅ pushed + runtime-verified |
| U27 | **Sales dashboard fabricated data (AF-2/AF-3)** + **two owner-reported bugs**: (a) 4 un-KYC'd outlets but **"no tasks"**; (b) header subtitle showed the role text, not the employee ID. | **Re-derived why #40/#54 ("retire demo-session") left it mock:** that work wired the sales *identity* (name/role from JWT) but never the *data widgets* (`lib/targets.ts` `OUTLET_ACHIEVEMENTS`/`resolveConfig(DEMO_*)`) nor the task status-gap. **(a) Root cause:** a master-loaded outlet has `kycStatus='NOT_STARTED'`, but the "Pending KYC" task group keys on `PENDING` (=submitted-awaiting-processing) → `NOT_STARTED` matched **no** group → "no tasks" despite enrollment being the rep's job. **Fix:** added a **"KYC to be done"** group for `NOT_STARTED` outlets (field roles). **(b)** header now shows the real **employeeCode** (e.g. `XSR-M001`) from new `GET /v1/sales/me`; **DEMO role switcher removed**. **Plus the widget de-mock:** Target card → new `GET /v1/sales/targets` (OutletTarget⋈OutletSalesRecord summed across the rep's outlets per KPI); chart → real 6-month primary-KPI trend + honest empty state (dropped `SalesAchievementChart` + demo program pills); profile stat tiles (Outlets/KYC-done) → real counts (profile identity was already real via `/auth/me`). +5 BE/+2 FE specs. **Independent audit = PASS** (tenant+caller scoped, trend month-math correct across year boundaries, NaN/null-safe). Gate: api jest 990/990, FE vitest 1497, tsc 0. **Remaining (flagged, out of scope this pass): AF-4** — the `sales/outlets` *list* page still uses `resolveConfig(DEMO_*)`+`targetPct:0` denominators (so `lib/targets.ts` can't be deleted yet); profile "Visibility" stat still 0. **RUNTIME-VERIFIED on staging (serving `644e7e6`):** `/v1/sales/me`→real `employeeCode "XSR-M001"`; `/v1/sales/outlets`→4 outlets ALL `NOT_STARTED` (so the new "KYC to be done" task shows 4 items — the reported bug); `/v1/sales/targets`→`outletCount:4, kpis:[]` (those outlets have no targets uploaded → honest empty state, not mock). | `7d8fed8` | ✅ pushed + runtime-verified |
| AF-4 | **Sales Outlets *list* page used fake target denominators** (`resolveConfig(DEMO_*)` KPI params with invented /800,/70 targets; achieved back-computed from a hardcoded `targetPct:0`) → every approved outlet showed 0% against fake targets. | **Fix:** new `GET /v1/sales/outlet-targets?period=` returns REAL per-outlet × per-KPI `{target,achieved,pace}` for the rep's assigned outlets (OutletTarget⋈OutletSalesRecord) + the KpiDef column set (primary-first), caller-+tenant-scoped. FE joins it onto the `/api/sales/outlets` roster by outletCode — real columns, real per-outlet achieved/target, real Team-Total sums, overall% from the primary KPI, honest "–"/empty states; dropped `resolveConfig`/`DEMO_*`/`GeoTargetConfig`/`TargetParam`/`targetPct`. `lib/targets.ts` STAYS (backs the admin targets-config system); only the sales-page mock use removed. +3 BE specs; `sales-pages-wiring` A2/A3/A4 flipped to enforce the real wiring. **Independent audit = PASS** (scope-tight, no fabricated-data remnant, no NaN/null-deref). **RUNTIME-VERIFIED on staging (`1a8aed7`):** `/v1/sales/outlet-targets`→ real (empty) shape for the un-targeted outlets → honest "No targets set". **Residual:** `team/[memberId]/outlets` page + the profile "Visibility" stat still pending. | `1a8aed7` | ✅ pushed + runtime-verified |
| U28 | **KYC Submissions list showed nothing** under "Pending KYC" despite 4 un-KYC'd outlets. | The page read only `/api/kyc` (submission records); a rep's `NOT_STARTED` outlets have **no submission**, so they never appeared — and the dashboard "KYC to be done" task deep-links here (`?status=NOT_STARTED`) to an empty page. **U20** made `/api/sales/outlets` carry NOT_STARTED + **U27** wired the dashboard, but this list was a 3rd surface never connected to the roster. **Fix:** merge the rep's NOT_STARTED outlets (from `/api/sales/outlets`, field roles only) as actionable "Pending KYC" entries → enrollment; "Pending KYC" filter now matches `{NOT_STARTED,PENDING}`; `?status=NOT_STARTED` resolves to it; fixed the always-blank outlet-code chip. Disjoint merge (NOT_STARTED have no submission → no double-count). +3 tests. **Independent audit = clean.** Gate: FE vitest 1500, tsc 0. | `52d970b` | ✅ pushed (runtime-verify post-deploy) |
| U29 | **Login "sat forever" after entering the OTP** (no error, spinner never cleared). | `handleVerifyOTP`/`handleSendOTP` awaited the `sendOTP`/`verifyOTP` server actions with **no try/catch/finally**. Those actions catch their *internal* fetch errors, but a **framework-level rejection** (the client→Next-server POST failing, a serialization error) rejects the promise → `setVerifying(false)` never ran → spinner forever, no error. Fix: wrap both so the spinner ALWAYS clears and an error ALWAYS surfaces. (Bug-1 diagnosis also confirmed `send-otp` returns 200 for any well-formed number by design → the form advances; verify-otp returns a clear error in ~0.35s.) Gate: FE vitest 1500, tsc 0. | `ecf4ee8` | ✅ pushed (runtime-verify post-deploy) |
| U30 | **KYC consent OTP "not found or expired"** — KYC could not be completed for ANY number (owner hit it with 9830011252); `123456` also rejected on staging. | **The consent OTP was NEVER sent** — no `otpCode.create` anywhere in the KYC module, so `consent()` always threw "OTP not found or expired"; it also didn't honor FIXED_OTP. **Fix:** new `POST /v1/kyc/consent-otp` creates a `KYC_CONSENT` OtpCode + sends via MSG91 — **PERMANENT = real MSG91 (prod)**; FIXED_OTP honored ONLY where `isFixedOtpAllowed()` (dev+staging, hard-refused on the gifsy_prod DB → no prod backdoor); `consent()` accepts the fixed OTP under that same gate (empty FIXED_OTP never bypasses). **Owner-requested phone validation (`assertPhoneAvailable`):** the outlet-owner mobile is rejected if it's already an enrolled partner/outlet in the tenant OR a sales employee's number (excludes the submission's own partner so Re-KYC doesn't self-collide; tenant-scoped). **FE:** submit now sends the OTP + only advances when create AND send both succeed; a duplicate/employee number (or any failure) shows a clear error instead of silently advancing; Resend actually resends. +6 BE specs (KycService now injects Msg91Service; sibling specs updated). **Independent audit = PASS** (no prod FIXED_OTP backdoor, no scope leak, no validation hole, no stuck spinner; residuals noted: `Outlet.phone` not checked [owner mobile maps to ChannelPartner.phone], OTP consumed-before-ownership-check is pre-existing). Gate: api jest 999/999, FE vitest 1500, tsc 0. **RUNTIME-VERIFIED end-to-end on staging (serving `c2bf2be`):** create KYC submission → `POST /v1/kyc/consent-otp`→**201** (OTP now actually sent) → `POST /v1/kyc/consent` with **`123456`**→**201 `{verified:true}`** (the reported bug — fixed) → employee number `9900000041`→**400 "belongs to a sales employee (Anil Sharma)"** (the requested phone validation). U28/U29 are FE-only (deployed `c2bf2be`; unit-verified — Chrome MCP can't drive uat.*). | `c2bf2be` | ✅ pushed + runtime-verified |

**Migrations (dev-applied):** `20260622120000_kpi_one_primary_per_client` (self-healing partial unique index). **U7–U10 carry NO schema change.** **Session-end gate (post-compact session):** api jest **950/950**, FE vitest **1469**, tsc clean both sides; e2e baseline **292/0/8** (the "50-failure" scare was the dev Cloud SQL proxy `--token` expiring mid-run, not product defects). **Recurring lessons:** (1) `next dev` (tolerant) ≠ `next build`/staging (strict) — verify against a prod build or deploy+retest; (2) **method-level `@UsePipes` does NOT override the global pipe** (pipes stack) — raw `@Req().body` is the reliable bypass; (3) a single `$transaction` welds the snapshot to the heavy relational write — any throw loses BOTH; (4) the FE's fire-and-forget PUT + swallowed `.catch()` made every write failure look like success (the root reason the hierarchy bug "kept coming back").

## 🔬 UAT AUDIT FINDINGS — OPEN, for later pickup (2026-06-22 hybrid UAT sweep)
> The planned 5-agent **runtime** UAT sweep was **blocked** (background subagents are denied shell, so they can't drive Playwright). Pivoted to a **hybrid**: orchestrator drives runtime; 4 background agents ran **source audits** (security/injection, money/concurrency, finance/TDS, fabricated-data/nav). The money · finance · RBAC/tenant/IDOR cores were independently **confirmed SOUND**. The items below are **NOT yet fixed** — pick them up next. Each has file:line in the agent transcripts; severity 🔴/🟠/🟡.

> ✅ **UPDATE 2026-06-23 (later same day): the PARTNER side (AF-1) is DONE + runtime-verified** — dev-RETAILER cleanup done; partner dashboard hero (U19) + targets page (U22) now on real data; `usePartnerSession`/`DEMO_SESSIONS`/`resolveConfig` dropped from the dashboard. **REMAINING = the SALES side (AF-2/AF-3):** U18 fixed the sales persona/designation (`getRole` → real JWT role), but the sales **dashboard** likely still reads `OUTLET_ACHIEVEMENTS` mock + `lib/sales-role.ts` personas + DEMO switcher → that's the next audit-first item. The original directive (kept below for context):
>
> ⭐ **OWNER DIRECTIVE (2026-06-23) — AF-1/AF-2/AF-3 partner/sales fabricated data is the NEXT post-compact task** (right after the dev-DB stray-`RETAILER` cleanup). **AUDIT-FIRST, do not just patch:** the owner recalls this was *supposedly already fixed* (task #40 / #54 "retire the demo-session layer" are marked COMPLETE), yet it's still mock and it's been logged-but-not-fixed across multiple audits. Re-derive **what #40/#54 actually shipped vs what's still mock, and why the gap persisted**, before fixing. **2026-06-23 diagnosis (confirmed live):** `usePartnerSession()` (`lib/partner-session.ts:115`) STILL returns the hardcoded `DEMO_SESSIONS['WHOLESALER']` (points `4_250`, name "Rajesh Kumar", firm "Kumar General Store"), switched by a localStorage demo key — and it's used by the **entire** partner shell (layout + dashboard + wallet + rewards + leaderboard), not just the hero. The hero **target** ("of 800 cases") comes from `resolveConfig(DEMO_BEAT,DEMO_DISTRICT,DEMO_STATE,DEMO_PERIOD)` (`dashboard/page.tsx:228`); the live `/partner/targets` fetch (`:834`) only overlays the *achieved* value (`p_sv`) and discards the real target/label/unit. **The real endpoints exist:** `/auth/me` → real `wallet.redeemablePoints` + partner name (`auth.service getMe`); `/partner/targets` → real target/achieved/name per KPI (but **lacks `isPrimary`+`unit`** — a backend `getTargets` edit to add them was started then reverted/stashed; redo it). Sales portal has the parallel issue: `lib/sales-role.ts` hardcodes personas (`XSR:'Anil Sharma'/'9900000041'`) + a DEMO role switcher. **Fix = make `usePartnerSession` hydrate from `/auth/me` + wire the hero to the real primary KPI** (and the sales shell to the JWT identity).

> ✅ **AF-2/AF-3 RESOLVED + PUSHED (2026-06-24, `e998282` + `02f6d52`).** All sales fabricated-data + dead-write items below are fixed, independently audited (CORRECT & SAFE), gate-green (api jest 1023/1023 · FE vitest 1483), and runtime-verified on gifsy_dev: ① KYC approve/reject now persist (real first-approve/reject + real status-history timeline; fabricated approver names gone); ② **the KYC re-entry flow** is wired end-to-end (P3 had left the pre-fill dead) — reject→remark→"Edit & Resubmit"→pre-filled form, with `existingKyc` gated to re-entry status + the rep's own assignments (no PAN/bank over-exposure); ③ mock target card removed (#104); ④ header shows the outlet ID not the CUID (#106); ⑤ the duplicate `/sales/kyc/[id]/edit` page + its accept-any-OTP retired (#108); ⑥ leaderboard hero + support author use the real identity (#105); ⑦ **#109** real tenant-scoped `GET /v1/kyc/phone-available` replaces the fake `EMPLOYEE_PHONES` roster (no PII); ⑧ **#107** dead `ROLE_*`/approver code deleted + the `loyaltybase_sales_role` FE role-spoof override gated to non-production. **✅ RESOLVED 2026-06-26 (`a525739`+`a272dca`):** the sales **leaderboard ("Ranks")** real ranking backend shipped — `GET /v1/sales/leaderboard` ranks same-level peers by team primary-KPI %, ZNM territory; FE page consumes it; tab is **live** (no longer "recommend hide"). Minor cosmetics noted: the pre-filled form's top chip still says "Re-KYC Required" for a senior REJECTED (banner copy is correct); a historical approval shows "Approved by —" (pre-dates the approver-role fix).
>
> ⭐ **AF-2/AF-3 SOURCE RE-DERIVATION (2026-06-24) — the table rows below are STALE; this block supersedes them for the SALES side.** Read every `platform/src/app/sales/**` page from source. Findings (tasks #102–#107; independent audit agent re-confirming in parallel):
> - **AF-2 row is STALE.** The sales **dashboard** (`sales/dashboard/page.tsx`) is **already REAL** — tasks/outlets/target-card/6-month-trend read `/api/sales/outlets` + `/api/sales/targets`; the layout identity reads `/api/sales/me` (U18). `OUTLET_ACHIEVEMENTS` is **no longer imported by the dashboard**. The one surviving live use is on the **KYC detail page** (`sales/kyc/[id]/page.tsx:389,482-488,578-635`). → **task #104**.
> - **🔴 NEW (table misses this entirely): sales KYC actions are DEAD WRITES.** `handleApprove`(:417-433), `handleReject`(:435-454), `handleSubmitForReview`(:456-462), `handleResubmit`(:464-476) all simulate success with `setTimeout` + local React state and **never call the backend**; they write hardcoded approver names "Rajesh Kumar"/"Sanjay Kapoor". Since KYC-APPROVED+active gates **both money rails (GLB-1)**, a fake first-approval is a **core-loop persistence break**, not cosmetic. Backend endpoints **already exist**: `POST /v1/kyc/:id/first-approve` (`kyc:approve`), `POST /v1/kyc/:id/reject` (`kyc:reject`). → **task #103 (🔴 highest)**.
> - **🟡 Fabricated identity strings:** ~~leaderboard hero `ROLE_NAMES`/`ROLE_TERRITORY`~~ **✅ leaderboard hero/identity + self row RESOLVED by the 2026-06-26 LB build (`a525739`+`a272dca`)** — the page now consumes the real `GET /v1/sales/leaderboard` (real `isMe` self row + ZNM-ancestor territory, no `ROLE_NAMES`/`ROLE_TERRITORY` mock); **only the support author `ROLE_NAMES` (`support/page.tsx:241`) remains** → **task #105**.
> - **🟡 Consistency:** sales KYC header still shows the CUID `{kyc.partnerName} · {kyc.id}` (`:503`) — mirror the admin fix `6f9642f`. → **task #106**.
> - **⚪ Cleanup (after the above):** `getRole()` honors a `loyaltybase_sales_role` localStorage override = FE role-spoof backdoor (backend still enforces via JWT; display-only risk); retire dead `ROLE_*` exports + FE-side approver mock (`resolveApprover`/`statusForApprover`). → **task #107**.
> - **AF-4 (sales/outlets hardcoded denominators):** independent audit re-read `sales/outlets/page.tsx` → **now REAL** (`GET /v1/sales/outlets` + `/v1/sales/outlet-targets`; filters are local-state, expected). AF-4 appears **already resolved** — verify at runtime, then close.
>
> **⚠️ Independent audit (agent a511a48, 2026-06-24) CONFIRMED the above AND found 3 MORE (now tasks #108/#109/#105):**
> - **🔴 NEW — accept-any-OTP on the KYC EDIT page.** `sales/kyc/[id]/edit/page.tsx:257` `handleVerifyOTP` sets `phoneVerified=true` for ANY 6-digit input, no backend call; `handleSendOTP`(:250) sends nothing. Security hole on a **bank-detail edit** screen. Wire to the real `POST /v1/kyc/consent-otp` + `/v1/kyc/consent`. → **task #108**.
> - **✅ RESOLVED 2026-06-26 (`a525739`+`a272dca`) — leaderboard was a 404 dead-read.** `sales/leaderboard/page.tsx` called `/api/sales/leaderboard` with no such sales route. **Now built:** `GET /v1/sales/leaderboard?scope=&period=` (live-computed same-level peers, ZNM territory); FE points at it; the board renders **real peers** (no longer empty). → folded into **task #105**.
> - **🟠 NEW — a SECOND fabricated-name roster.** `sales/kyc/new/page.tsx:76-83` `EMPLOYEE_PHONES` (names on phones `9800000001+`, distinct from `ROLE_PHONES`) drives a FE-side phone-uniqueness check that renders fake employee names; should be a backend constraint. → **task #109**.
> - Audit also noted: `OUTLET_ACHIEVEMENTS` is keyed `o1..o13` but indexed by **KYC id** (`[id]`), so for real ids it returns `undefined` and the target card silently hides — masking that it was never wired (#104). `approvalHistory` is always `[]` so the KYC timeline is local-only (part of #103). Outlet-photos lightbox = literal "demo mode" placeholder; profile menu items (Notifications/Privacy/Help) have no onClick (inert). `POST /v1/visibility/submit` exists but the sales visibility page only LISTS — possible submit gap, deferred.
> - **NOT findings (correctly classified by the audit):** view-mode / period / status-filter local state is expected client-side UI, not dead writes. Money path (`redeem-for-outlet`/`confirm`, OTP-gated) and ticketing are REAL.

| ID | Sev | Finding | Where | Note |
|---|---|---|---|---|
| AF-1 | ✅ **DONE** (U19 `356c135`) | **Fabricated data on the partner dashboard hero** ("800 cases" via `resolveConfig(DEMO_*)`, demo 4250 pts) — **FIXED + runtime-verified**: hero now consumes real `/partner/targets` (real primary KPI) + `/auth/me` wallet; dropped `usePartnerSession`/`DEMO_SESSIONS`/`resolveConfig`. U22 also aligned the partner **targets page** to the real `isPrimary`. **Still partial — sales side (AF-2/AF-3):** U18 fixed the sales **persona/designation** (`getRole` now derives the real JWT role), but the sales **dashboard** may still read `OUTLET_ACHIEVEMENTS` mock + `lib/sales-role.ts` ROLE_NAMES/EMP_IDS + DEMO switcher — audit-first before fixing. | partner ✅ / sales ⏳ | partner done; sales dashboard mock = remaining AF-2/3 |
| AF-2 | 🔴 | Sales dashboard "Target Achievement" card built entirely from a **mock `OUTLET_ACHIEVEMENTS`** map — no API path | `app/sales/dashboard/page.tsx:90-106` | same `lib/targets.ts` demo source |
| AF-3 | 🔴 | Partner profile → Payment Details always renders **hardcoded HDFC / a/c …3456** (`MOCK_PROFILE.bank`, even on API success) | `app/partner/profile/page.tsx:87,143,146` | the fake bank removed from rewards still lives here |
| AF-4 | 🟠 | Sales outlets table target denominators (/800,/70…) hardcoded; achieved back-computed against fake targets | `app/sales/outlets/page.tsx` | same demo-config family |
| AF-5 | ✅ **DONE** (`1bc9315` boundary + `f0d48bc` full sweep) | **CSV/formula injection** — `cellSafe` (=,+,-,@,tab,CR → leading apostrophe; idempotent) applied to **EVERY** xlsx export. Phase 1 (`1bc9315`): the `buildXlsx` serialisation boundary (all `/reports`, outlet master, points-ledger, ticket-aging, …) + the FE outlet templates. Phase 2 (`f0d48bc`): shared `cellSafe`/`jsonToSheetSafe`/`aoaToSheetSafe` in `common/xlsx.ts` + FE `lib/xlsx-safe.ts` applied to **every remaining writer** — backend kyc-review-dump, credits payout file, rewards fulfilment, targets template/export, visibility error file; FE all `lib/*-export.ts` + inline page downloads. (TDS/invoice already had their own cellSafe; parse paths untouched.) **Independent audit (`f0d48bc`): SHIP — no raw sink remains, no non-xlsx/CSV sink, high-risk PII cols escaped, no structural breakage.** Gate api jest 1123 · FE vitest 1533 · tsc 0. *(NOTE: the K12 `reviewDump` V4-signed-URL **doc-link** issue is a SEPARATE K12 surface — unrelated to formula injection, which is now fully closed there too.)* | `common/xlsx.ts` + `lib/xlsx-safe.ts` (all writers) | ✅ fully done |
| AF-6 | 🔴 | httpOnly cookie defeated — the **same JWT is also written to `localStorage`** (7-day TTL) → any stored-XSS exfiltrates a bearer token; the cookie is never used for auth | `auth/login/actions.ts:107`, `login/page.tsx:121`, `api-client.ts:20` | drop the redundant token (cookie OR localStorage, not both) |
| AF-7 | 🟠 | **GSTIN** input capped at **12 chars** (real GSTIN=15) + **no checksum** anywhere; server DTO accepts any string → garbage state-code/PAN derivation for invoices/TDS | `sales/kyc/new/page.tsx:413`, `kyc/dto/kyc.dto.ts:91` | add server-side `@Matches` + checksum |
| AF-8 | 🟠 | Invoice numbering: on a number collision the handler **skips the outlet** (→ no invoice) instead of retrying; sequence not gap-free/transaction-safe | `invoices.service.ts:147-221,269` | make the per-client sequence tx-safe |
| AF-9 | 🟡 | `dangerouslySetInnerHTML` brand-style sink (the only one in the FE) — 🔴 only if `brandStyle` interpolates unsanitized tenant config | `app/layout.tsx:44` | trace `brandStyle` source |
| AF-10 | 🟡 | OTP generated with `Math.random()` (non-CSPRNG); send-throttle is in-memory per-instance (multi-instance bypass); 7-day access token TTL + no per-device logout; KYC upload trusts mimetype (no magic-bytes); ~~legacy `/reports/tds` export omits `cellSafe`~~ **(✅ now covered by AF-5 `1bc9315` — every string cell in `buildXlsx` is `cellSafe`'d)** | various (see security-audit transcript) | low-risk hardening cluster |
| AF-11 | 🟡 | Achievement template reuses the targets template builder, so an override-name column appears on it and lands in `OutletSalesRecord.kpiValues.__names` — **already stripped on write** (U8 O-1 hardening), inert; noted for future `kpiValues` consumers | `targets.service.ts uploadAchievements` | done-as-hardened; watch if a new reader enumerates `kpiValues` keys |
| AF-12 | 🟡 **DEFERRED** (owner 2026-06-23: "deal with this later") | **`@RequirePermission` permission-guard is fail-open** (`RBAC_ENFORCEMENT` off) — the fine-grained permission lock is inert; access is governed by `@Roles` + in-service role/tenant checks. **Verified SAFE for launch:** every privileged admin controller has a class-/method-level `@Roles` (the active gate); the no-`@Roles` routes are by-design "any authenticated user" (leaderboard reads, KYC/ticket self-service, scheme/visibility reads) relying on service-level tenant/ownership scoping. **Not a blocker.** Keep RBAC OFF for go-live. The one open item: before EVER flipping `RBAC_ENFORCEMENT` on, re-run the gap-#2 route-coverage audit to confirm no privileged WRITE sits on a no-`@Roles` route (flipping it on without a complete role→permission map would 403 legitimate admins). | api controllers + `permission.guard.ts` | full writeup + 2026-06-23 re-verification = `RBAC-ENABLEMENT.md` |

**Also still open (pre-existing):** gap-#57(a) — the 4 `/admin/dashboards/{payments,engagement,redemptions,kyc}` routes still render mock data (owner-deferred; hide-nav vs wire); Layer-4b partner surface honoring `isPrimary` (deferred). **NOT a defect:** a sales/client employee must log in on the **client's branded domain** (e.g. `uat.deoleoloyalty.gifsy.in`), not the Gifsy operator console (`uat.app.gifsy.in` → tenant `gifsy`) — tenant isolation working as designed (worth a clearer "no account on this portal" message — minor UX, see AF backlog).

## ✅ FIX WAVE RESULTS (2026-06-22)
**Gate (all green):** backend `tsc` ✅ · backend jest **921/921 (42 suites)** ✅ · FE `tsc` ✅ · FE vitest **1459 passed** ✅
· Playwright E2E **green** (287 passed; the 2 flagged were a load-flake [passes in isolation] and a brittle selector on an
unchanged page [hardened, re-ran 11/11]). The 3 FE unit tests for the rewritten payouts/scheme pages were updated to the new behavior.
**Runtime proofs (real `gifsy_dev`):** GLB-3 — two same-file-hash TDS rows persist (old coarse index would drop the 2nd =
the live bug) + exact-dup still rejected; backend boots clean (new `main.ts` `POINTS_CONVERSION_RATE>0` boot check passes);
money rails compile + serve with the canonical KYC resolver + `REVERSED` state live.

| Blocker | Resolution | Verified |
|---|---|---|
| **GLB-1** eligibility both rails | `kyc-eligibility.ts` canonical `resolveEffectiveKycStatus`+`isPartnerPayable` used by credit bank-file, `confirmRedeem`, `confirmRedeemForOutlet`, `processBatch`; non-APPROVED/inactive → held/excluded; `?? 'APPROVED'` gate-default removed | audit + jest |
| **GLB-2** zero-value redemption | cash `valuePaise==0n` hard-fails inside the tx (rolls back); `main.ts` boot check rejects non-finite/≤0 rate | audit + jest + boot |
| **GLB-3** stale coarse TDS indexes | migration `20260621120000` drops both; fine-grained dedup retained | **runtime-proven** |
| **GLB-4** privilege escalation | per-caller assignable-role allow-list in `createUser`/`updateUser`; `...dto` spread removed; tenant-scoped writes | audit + jest |
| **GLB-5** scheme enrollment | enroll WRITE wired to real `POST /v1/schemes/:id/enroll` (SELF/SALES, partnerId); CATALOG wired to real `GET /api/schemes` (no demo IDs); excludes already-enrolled; `sales/dashboard` demo source removed | audit + E2E |
| **GLB-6** payout settlement UI | full lifecycle on real endpoints (create→assign→process→UTR template/upload→reconciliation); mock fund card + setTimeout stub gone | audit + E2E |
| **GLM-1** PAYOUT-reversal clawback | reversal marks ALL matching entries `REVERSED` (new state) + records receivable for PAID | audit + jest |
| **GLM-2** FAILED-credit re-bank | download selects `status in [PENDING,FAILED]`; `REVERSED` excluded (the clash fix) — was NEVER implemented in pass 1, caught by re-audit | audit + jest |
| **GLM-3** beneficiary validation | `processBatch` rejects mode-missing UPI/bank fields | audit + jest |
| **GLM-4** one-payout-per-order | migration partial-unique on `PayoutTransaction.redemptionOrderId` | runtime-applied |
| **GLM-5** fake `/admin/kyc` bulk-approve | removed (no real bulk-by-ids endpoint); export wired to real `GET /v1/kyc/review-dump` | audit |

**Residuals (post-launch, non-blocking):** null-`partnerId` KYC submissions are safe-over-cautious (held, never wrongly paid) on all rails; enrollment-form fetch for real schemes; N+1 `my-enrollment` checks at typical scheme counts; minor RBAC defense-in-depth (in-service GIFSY re-assert on ticket escalate).

---

> **Original register (pre-fix-wave) below for traceability.** Each item has an ID, severity, file:line, the fix, and its
> runtime/E2E verification status.

## S0–S6 E2E runtime audit results (2026-06-21)
| Stream | What | Runtime verdict |
|---|---|---|
| **S0/S4** | TDS file-hash dedup | ❌ **BROKEN — GLB-3 confirmed live**: a 2-row off-platform upload **reported `succeeded:2` but stored only 1** (194R shows 1 PAN / ₹10k; PAN B's ₹20k silently dropped). Stale coarse baseline index. |
| **S1** | redemption UTR ingest + BUG-1 | ✅ verified: redeem→confirm debits exactly; UTR=FAILED reverses + idempotent re-upload; UTR=PAID→DELIVERED no wallet change; manual cash cancel/return/fail → 400. |
| **S2** | auth (atomic refresh + JWT-binding + FIXED_OTP) | ✅ verified: old access token → 401 after refresh; reused refresh token → 401; harness logs in every role. |
| **S3** | KYC approval → outlet activation | ✅ verified (DB ground-truth): approving `seed-kyc-1` flipped null-intent outlet O001 `isActive=false→true`. |
| **S5** | achievement Excel round-trip | ✅ verified: backend template → fill → upload → stored (was 0 rows before). |
| **S6** | FE mock-data removal + guards | ✅ harness no-fabricated-data assertions green + FE tsc/vitest gate. |
| **harness** | full role×page×data matrix | ✅ **292 passed / 0 failed / 7 skipped** against current code (auth, KYC reads, scoping, RBAC write-denials). |

**Takeaway:** 6 of 7 S0–S6 streams runtime-CONFIRMED good; **S4/S0 (TDS) is runtime-CONFIRMED broken (GLB-3)** — it slipped because S4 was jest-only (Prisma mocked). Lesson reinforced: runtime-verify money/DB changes against the REAL dev DB.

---

## 🔴 BLOCKERS — must fix before any real money / UAT (FIX WAVE)
| ID | Sev | Area | Issue | Fix | Status |
|---|---|---|---|---|---|
| **GLB-1** | BLOCKER | money | Eligibility (KYC-APPROVED + isActive) gate MISSING on BOTH rails. `credits.createPayoutDownload` hardcodes `kycStatus:'APPROVED'` (`credits.service.ts:567`), no join to real `KycSubmission.status` → RE_KYC'd/deactivated partners land in the payable bank file with a false APPROVED cert. Redemption rail (`confirmRedeem`/`assignPending`/`processBatch`) has ZERO KYC/active check. A partner moved APPROVED→RE_KYC_REQUIRED still gets paid. | Exclude non-APPROVED + inactive/deleted at the bank-file build (write the REAL kycStatus); mirror at `confirmRedeem`/`confirmRedeemForOutlet` (UPI/BANK) + `payouts.processBatch`. | CONFIRMED (static); needs fix + runtime-verify vs a partner moved APPROVED→RE_KYC mid-flow |
| **GLB-2** | BLOCKER | money | Zero-value redemption: `conversionRate=0` → `valuePaise=0` → points debited, ₹0 payout, order unsettleable (`rewards.service.ts:891/579`). | Hard-fail zero-value cash redemption inside the confirm tx (rollback the debit); validate `POINTS_CONVERSION_RATE>0` at boot. | CONFIRMED (static) |
| **GLB-3** | BLOCKER | data/tds | Stale coarse baseline indexes `tds_off_platform_entries_client_batch_key` + `tds_deposits_client_section_batch_key` (`(clientId,uploadBatchId)`) drop all-but-first row of every multi-row TDS upload (S4 file-hash batchId shared by all rows) → understated 194R/194C base, **reports false success**. | New migration DROPping both stale indexes; runtime-verify a multi-row upload persists ALL rows. | ❌ **CONFIRMED LIVE (E2E)** |
| **GLB-4** | BLOCKER | auth | Privilege escalation: `admin-core.createUser`/`updateUser` write `dto.role` with no allow-list; `/admin/users` open to CLIENT_ADMIN → a tenant admin creates/promotes a user to GIFSY_ADMIN (global super-admin, bypasses @Roles + flips every tenant filter to all-tenants) → full cross-tenant breach. | Per-caller assignable-role allow-list (non-GIFSY callers can't assign GIFSY_ADMIN/CLIENT_ADMIN); stop spreading `...dto` into role. | CONFIRMED (static); needs fix + runtime-verify |
| **GLB-5** | BLOCKER | core-loop | Scheme enrollment writes `localStorage` only — partner (`lib/schemes.ts:253`) + sales (`schemes.ts:283`) never call the real `POST /v1/schemes/:id/enroll` (which EXISTS + is E2E-proven). Enrollments never persist. | Wire both to the real route; runtime-verify persistence + admin export shows it. | CONFIRMED (static) |
| **GLB-6** | BLOCKER | core-loop | Payout settlement has NO working operator UI. `admin/payouts` "Process Batch" is `setTimeout+alert` (`page.tsx:204`); no FE creates/assigns/processes/UTR-uploads a batch. The S1 settle endpoints (real, runtime-proven) have no FE driver → operators cannot settle redemptions. | Build the GIFSY settlement UI on the existing endpoints (create→assign→process→UTR-template→UTR upload). | CONFIRMED (static) |

## 🟠 MAJORS
| ID | Area | Issue | Fix |
|---|---|---|---|
| GLM-1 | money | Credit `CreditReversal` `awardType:'PAYOUT'` is a silent no-op even after the cash was already PAID (`credits.service.ts:821`) — no clawback/receivable. | On PAYOUT reversal: if entry still PENDING/PROCESSING pull it from payability; if already PAID record a recoverable receivable + surface it. |
| GLM-2 | money | A FAILED credit `CreditPayoutEntry` never returns to PENDING (`credits.helpers.ts:695`) → a legitimately-owed award is un-bankable (re-download selects PENDING only). | Reset FAILED→PENDING (or include FAILED in re-download); document corrected re-upload flips FAILED→PAID intentionally. |
| GLM-3 | money | Beneficiary bank fields (UPI id / acct+IFSC) never validated → blank-beneficiary rows in the bank file (`payouts.processBatch` checks only PAN+amount). | In `processBatch` reject txns missing mode-required beneficiary fields. |
| GLM-4 | data | No DB unique on `PayoutTransaction.redemptionOrderId` — one-payout-per-order rests only on an app-layer claim. | Add partial unique `(redemptionOrderId) WHERE NOT NULL` (in the GLM migration). |
| GLM-5 | core-loop | `/admin/kyc` list page fake bulk-approve + export (`setTimeout`+alert; real path exists at `/admin/kyc/[id]` + `/approvals`). | Remove or wire to `POST /v1/kyc/bulk-verify`. |

## 🟡 MINORS
| ID | Status | Issue |
|---|---|---|
| GLm-1 | ✅ FIXED (`c40a420`) | FIXED_OTP send-side (`msg91`) + rate-limit `skipIf` (`app.module`) + DEMO_MODE (`admin-core.bulkEditUsers`) now all require `NODE_ENV!=='production'`. |
| GLm-2 | OPEN | `tickets.escalate` assigns `dto.escalateTo` without validating the assignee is in-tenant (`tickets.service.ts:114`). |
| GLm-3 | OPEN | `kyc.slaMetrics` has one un-tenant-scoped histogram (`:1205`) — GIFSY-only, defense-in-depth tighten to `kycTenantFilter`. |
| GLm-4 | OPEN (post-launch) | Legacy `sid`-less JWTs match on `(userId,clientId)` until they expire — do a hard cutover (reject `sid`-less) post-launch once old tokens age out. |
| GLm-5 | TRACK | In-memory per-instance throttler (limits scale with Cloud Run instances); dead `REDIS_URL` bound but unused. |
| GLm-6 | VERIFY | Confirm prod env has `DEMO_MODE` unset (proxy injects a GIFSY_ADMIN header when set); the proxy `DEMO_MODE` path must be off in prod. |
| GLm-7 | NOTE | TDS file-hash audit flagged a "collision" — MISREAD: it already uses a `\x01` delimiter (od-verified) + fixed 4-field arity = collision-safe. No change. |

## ⏭️ Deferred follow-ups / feature gaps (NOT launch blockers)
- **Schemes enrollments list/stats endpoint** doesn't exist → `/admin/schemes/:id/enrollments` is honestly empty-stated (only the xlsx export is real). Build the backend list/stats endpoint (separate from GLB-5 which wires the *write*).
- **S6 dead code:** `admin/visibility` `VISIBILITY_QUEUE` dead const; wire the real fraud-log (`GET /v1/visibility/fraud-log`).
- **S3 NIT:** RE_UPLOAD `ConflictException` logs nothing server-side — add a `logger.warn` with the submission id before throwing.
- **scheme-builder** `KYC_OUTLET_IDS = new Set() // TODO` (`scheme-builder.tsx:171`) — targeting-preview only.
- **lib sample data** (`lib/invoice.ts MOCK_VISIBILITY_INVOICES`, `lib/points-ledger-export.ts DEMO_OUTLETS`, `lib/campaign.ts MOCK_*`) — confirm not imported by a live page (the core-loop audit traced them as not on live surfaces).
- **#76 prod data load** must create the first GIFSY_ADMIN + the OutletType master rows via API/load-script (the seed is prod-firewalled).
- **#74 owner ops:** Cloud Monitoring alert, automated backups + PITR on `gifsy-db`, prod secret rotation.

## ✅ What the audits CONFIRMED SOUND (do not re-audit)
Money atomic-claims + reversal idempotency (S0/S1 — "could not break it"); tenant data-isolation (no unscoped query across 33
services); RBAC enforcement (one exception = GLB-4); schema↔migration parity (73 tables / 50 enums); BigInt/JSON serialization;
secrets fail-closed (`JWT_SECRET`); money FKs `onDelete: Restrict`; the S0–S6 hardening (S1/S2/S3/S5 runtime-proven; S4 broken per GLB-3).

## Sequence to go-live
1. **FIX WAVE** — close GLB-1..6 + GLM-1..5 (parallel disjoint streams; runtime-verify each against the REAL dev DB).
2. **Re-audit** the changed money/auth/data/core-loop paths + re-run the E2E harness.
3. **#76** load real Deoleo data into empty prod (via API, not seed).
4. **Owner UAT** on staging (real OTP) of the full loop incl. enrollment + settlement.
5. **#74** owner ops (monitoring/backups/cred rotation).
